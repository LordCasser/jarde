package absent;

/** The stub `MissingDependency` was compiled against; it is provenance, not part of the fixture. */
public class Library {
    public static int grow(int seed) {
        return seed * 2;
    }
}
