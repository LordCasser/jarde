# 匿名子类在基类构造期间的捕获可见性

源码声明顶层基类 `Base`。它的构造器立刻调用可覆盖的 `observe()`；匿名子类实现该方法并读取捕获局部。目录中的 `.class` 是源码经 `javac --release 8 -g` 编译后冻结的副本。

`run.sh` 在临时目录重编译、用 `-Xverify:all` 执行源码，并于退出时清理临时目录。输出检查虚调用发生在 `Base` 构造器返回之前，且此时捕获值已经可见。
