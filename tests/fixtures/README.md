# P1 fixture index

Every input the P1 acceptance tests read lives in this directory. There are two kinds, and
the index marks them differently.

- **Real compiled samples** are checked-in `.class` files produced by a fixed compiler from
  the `.java` files next to them. Each directory's own `README.md` records the compiler
  version, the exact command, the target dialect, the byte count and the SHA-256 of every
  output. The compiler is a generation-only input: it is not checked into the repository
  and no test needs it at run time.
- **In-memory fixtures** are bytes built by a named generator function inside one test
  file. They exist where a compiled sample cannot pin exact constant-pool indexes, BCIs,
  opcodes or class-file spans. The tests assert those coordinates directly from the bytes
  the generator produced. The five inputs under `p1-golden/` additionally record and
  assert a blake3 digest of the generated bytes, so a changed builder fails the golden
  test instead of silently moving the expectation.

## Real compiled samples

| directory | compiler / command | outputs and digests | read by |
| --- | --- | --- | --- |
| `historical/ecj-4.6.1/` | ECJ 4.6.1 (`java -jar ecj-4.6.1.jar`), `-source 1.3`, targets 1.1–1.8, Temurin 8 `rt.jar` bootclasspath | `v45`–`v52` `HistoricalControlFlow.class`, 45.3–52.0; SHA-256 in `historical/README.md` | `tests/historical_classfile_corpus.rs`; `tests/p1_xref_code.rs` (A02 invocation coordinates, cross-checked against public `inspect_method_bytecode`) |
| `r2-annotation-positions/` | `javac 23.0.1`; `javac --release 17 -g:none -d v17 RecordOnly.java`; `javac --release 8 -g:none -d v8 CodeOnly.java` | `v17/RecordOnly.class` (61.0, SHA-256 `11342150…f636e293`), `v8/CodeOnly.class` (52.0, SHA-256 `8aabf4f4…e982ce29`) | `tests/p1_xref_metadata.rs` (R2/A03 nested annotation positions); `tests/p1_xref_golden.rs` (`metadata.json` golden uses the hand-built metadata fixture, not this sample) |
| `method-signature-grammar/` | `javac 23.0.1`; `javac --release 17 -g:none -d v17 GenericReturn.java TypeVarResult.java ThrowsVar.java ThrowsMixed.java` | four 61.0 classes, SHA-256 in that directory's `README.md` | `tests/p1_xref_metadata.rs` (method `Signature` grammar, 2.2 follow-up fix) |
| `b2-bootstrap-descriptor/` | `javac 23.0.1`; `javac --release 8 -g:none -d v8 LambdaSample.java` | `v8/LambdaSample.class` (52.0, 731 bytes, SHA-256 `9588c94b…fac4083`) | `tests/p1_xref_bootstrap.rs` (B2/A04) and `tests/p1_xref_golden.rs` (`bootstrap.json`) |
| `JvmBytecodeOracle.java` | JDK 25 Class-File API, run by the ignored oracle test | generated at test time in a temp directory | `tests/jvm_bytecode_oracle.rs` (ignored unless a JDK 25 toolchain is present) |

## Golden expectations (`p1-golden/`)

Each JSON file records one representative input and one or more replays:

```text
fixture.name / fixture.source / fixture.blake3 / fixture.bytes
replays[]: { name, kind: "query" | "multi_release", request|view, report }
```

`tests/p1_xref_golden.rs` rebuilds the input from the same generator, checks the blake3
digest against `fixture.blake3`, deserializes the recorded request or runtime view, runs
it through the public entry point and compares the serialized report with `report`.

- `code.json` — `code_fixture` (used and unused `Methodref`, one string and one integer
  `ldc`): X0 candidate, X1 no-call contrast, invocation coordinates, literal value.
- `metadata.json` — `metadata_fixture` (superclass, interface, field descriptor, method
  `Signature`, class annotation whose type has no `CONSTANT_Class`): single-category and
  combined requests.
- `bootstrap.json` — real `LambdaSample.class`: implementation handle, descriptor type of
  the metafactory handle, category isolation, dynamic site.
- `multi-release.json` — `mr_fixture` (base/v11/v17 `p/Join.class`): Java 8/11/17
  selections next to one physical query over the same snapshot.
- `nested.json` — `nested_fixture` (the same jar bytes at two `WEB-INF/lib` entries, one
  STORED and one DEFLATED, each holding the same `p/Dup.class`): distinct origins.

The only normalization applied to a report before comparison is deleting
`elapsed_millis`, the wall-clock number every usage snapshot carries. Origins, coverage
ranges, diagnostics, `via` paths, complete symbols, BCI/opcode/CP index and spans, and
item order are compared as published. The files are static: no test writes or updates
them, and a missing or changed file is a test failure.

## In-memory generators

| generator (test file) | shape | tests |
| --- | --- | --- |
| `p1_xref_code.rs::fixture` | one instruction per code consumer, an unused `Methodref`, an `invokedynamic` site, an exception table, an abstract method | A01, A02 cross-check, category isolation, billing, paging |
| `p1_xref_code.rs::method_type_fixture`, `descriptor_consumers_fixture` | `MethodType`/`MethodHandle`/`Dynamic` entries with and without a real use site | A03/R3 descriptor types |
| `p1_xref_code.rs::broken_member_fixture`, `dead_code_fixture`, `trailing_dead_code_fixture` | one undecodable member, a `goto` over a call, bytes after an unconditional `return` | A17, member-level failure isolation |
| `p1_xref_code.rs::calling_class`, `zip_with_deflate` | nested STORED/DEFLATED tree with a truncated candidate and readable siblings | A08/A14 gap test `a_nested_truncated_candidate_bounds_pages_budgets_and_cancellation` |
| `p1_xref_metadata.rs::meta_fixture`, `annotation_fixture`, `hierarchy_fixture`, `record_annotation_fixture`, `code_annotation_fixture`, `signature_fixture`, `module_fixture`, … | metadata-only references at flat and nested positions | A03, R2, signature grammar |
| `p1_xref_bootstrap.rs::real_lambda_fixture`, `descriptor_bootstrap_fixture` | linkable `metafactory` shapes, condy chains, shared subgraphs, unused table entries | A04/A05 |
| `p1_query_api.rs::fixture`, `ClassFile`, `literal_archive` | manifest/services/class archive, literal kinds, target-bound cursor matrix | A14, A18, R1 |
| `p1_multi_release.rs::a06_fixture`, `class_with` | base/v11/v17 MR-JAR and single-class variants | A06, compliance and budget cases |
| `p1_artifact_tree.rs::zip` | Boot/WAR/ordinary nested trees with duplicate and malformed children | A08, A07 origin identity |
| `p1_xref_properties.rs::build_class(shape, extras)` | structured class: member counts, constant-pool entries, instruction sequence; `extras` appended unused entries | the four proptest properties |
| `tests/navigation.rs::class_file`, `zip_of`, `two_origin_war` | hand-built classes (fields, `Code`, `abstract`/`native`, one deliberately damaged member record) inside WARs whose upper directory entry and explicitly expanded nested library hold the same class name — the same bytes in one case, different bytes in others | `add-artifact-navigation`: candidate/confirmed listings, duplicate origins, path-vs-declaration mismatch, damaged candidate and damaged member stops, identity handoff |
| `p1_xref_golden.rs::code_fixture`, `metadata_fixture`, `mr_fixture`, `nested_fixture` | the five golden inputs above, each with an asserted blake3 digest | golden replays |

The real `zip()`/`rawzip` writer produces all archives, so enumeration, CRC and
compression are the production paths, not a test double.

## P3 real compiled samples

| directory | compiler / command | outputs and digests | read by |
| --- | --- | --- | --- |
| `p3-local-rewrite/` | javac 23.0.1; `javac --release 8 -g:none -d v8 LocalRewrite.java` | `v8/LocalRewrite.class` (52.0, 650 bytes, SHA-256 `f755f062…6158a6`), with the bytecode of every member in that directory's `README.md` | `tests/p3_local_rewrite.rs` (P3-R1: a slot written between a load and its reader at a return, a store and a branch; P3-R2: the producer behind a presented cast) |
| `p3-corpus/` | javac 23.0.1; `Flags.java` under `--release 8 -g:none`, `-g`, `-g:lines,source` and `-parameters`, plus `-source 8 -target 8` (which produces the `-g:none` bytes byte for byte), and `MissingDependency.java` compiled against a stub that is **not** shipped | five 52.0 `Flags.class` (325–597 bytes) and `v8-missing-dep/MissingDependency.class` (264), SHA-256 in that directory's `README.md` | `tests/p3_execution_comparison.rs` (P3 3.3: the flag matrix, the missing dependency, and the recorded boundary list) |
| `p3-nested-eval/` | javac 23.0.1; `javac --release 8 -g:none -d v8 NestedEval.java` | `v8/NestedEval.class` (52.0, 361 bytes, SHA-256 `141c3dcb…f0c5c2e9`), with the bytecode of every member in that directory's `README.md` | `tests/p3_eval_context.rs` (P3-R8: a nested expression is checked where its text is evaluated); `tests/p3_execution_comparison.rs` (the two refusals and the two controls) |
| `p3-refused-cast/` | javac 23.0.1; `javac --release 8 -g:none -d v8 RefusedCast.java` | `v8/RefusedCast.class` (52.0, 666 bytes, SHA-256 `d7d2ecf5…3ac588`), `v8/External.class` (52.0, 427 bytes, SHA-256 `dbccbc01…0f0c3c84`), `v8/Holder.class` (52.0, 158 bytes, SHA-256 `7f0dc855…f6bc66d5`), with the bytecode of every member in that directory's `README.md` | `tests/p3_eval_context.rs` (P3-R9: a quote keeps the observable producers its refused expression depended on); `tests/p3_execution_comparison.rs` (the refused reads and the composed controls) |
| `p3-declaration/` | javac 23.0.1; `javac --release 8 -g:none -d v8 Shape.java Holder.java` | `v8/Shape.class` (52.0, 191 bytes, SHA-256 `91f9a3a0…f32e`) and `v8/Holder.class` (52.0, 359 bytes, SHA-256 `11cbb8c0…b5ea`), with the bytecode and flags of every member in that directory's `README.md` | `tests/p3_declaration_handoff.rs` (the member forms a class file's own flags decide, through `Engine::recover_method`) |
| `p3-nested-arithmetic/` | javac 23.0.1; `javac --release 8 -g:none -d v8 ModLike.java` | `v8/ModLike.class` (52.0, 568 bytes, SHA-256 `a5827ef4…d2891158`), with the bytecode of every member in that directory's `README.md` | `tests/p3_eval_context.rs` (the printer's grouping: a nested arithmetic's text parses back into the tree it was printed from, and the two shapes the defaults already state gain no parentheses); `tests/p3_execution_comparison.rs` (all eight members executed against the original, `inverse32(-1)` included) |
| `p3-receiver-grouping/` | javac 23.0.1; `javac --release 8 -g:none -d v8 ReceiverGrouping.java` | `v8/ReceiverGrouping.class` (52.0, 1038 bytes, SHA-256 `8e030b53…30cc8c00`), with the bytecode of every member in that directory's `README.md` | `tests/p3_eval_context.rs` (the printer's grouping as a property of the **position**: a call's receiver, a three-operand chain in receiver position, and the controls that gain nothing); `tests/p3_execution_comparison.rs` (all eight members executed against the original, `call("a", "bc")` included) |
| `p3-boolean-contexts/` | javac 23.0.1; `javac --release 8 -g:none -d v8 BooleanContexts.java` | `v8/BooleanContexts.class` (52.0, 1114 bytes, SHA-256 `e6a85f9a…6bfb2bf0d`), with the bytecode of every member and the pre-fix `javac` refusals in that directory's `README.md` | `tests/p3_boolean_contexts.rs` (a boolean context is typed by its descriptor: a `Z` method's `return` is `true`/`false`, a proven-boolean condition is a truth test, a local a write proved boolean is declared `boolean` with its later uses printing as that boolean, the `int` controls keep their shape, and an unprovable value is refused at each of the three sites); `tests/p3_execution_comparison.rs` (sixteen members compiled under a declaration derived from the run's own facts and executed against the original, the one remaining refusal recorded as a boundary) |
| `p3-array-types/` | javac 23.0.1; `javac --release 8 -g:none -d v8 ArrayTypes.java` | `v8/ArrayTypes.class` (52.0, 511 bytes, SHA-256 `84cea788…a0a3ac59`), with the bytecode of every member and the pre-fix `javac` refusals in that directory's `README.md` | `tests/p3_array_types.rs` (a type position states a legal Java type: an array descriptor becomes `byte[]`/`java.lang.String[]`/`int[][]`/`java.lang.String[][]` with the element spelled by the existing object-name rule, an object type's declaration is byte-for-byte unchanged, a descriptor with no Java spelling is refused at the BCI that would have carried it, and the two positions that name a class record what an array-typed pool class does there); `tests/p3_execution_comparison.rs` (six members compiled under a declaration derived from the run's own descriptor and executed against the original, all six executed with identical traces) |
| `p3-array-initializers/` | javac 23.0.1; `javac --release 8 -g:none -d v8 ArrayInitializerProbe.java` | `v8/ArrayInitializerProbe.class` (52.0, 1281 bytes, SHA-256 `998bdb54…5c86`); source and 14-line execution baseline in that directory's `README.md` | `tests/p5_corpus_fingerprint.rs` pins the input; `recover-array-initializers` execution tests are pending implementation |
| `p3-local-postfix-array-elements/` | Java 8 正例、`iinc +2` 冻结补丁及不同槽补丁；生成命令见目录 README | 三个 verifier-valid 52.0 class；不同槽 class SHA-256 `e281d549…44cb`，原运行 `[1, 0, 0]` | `tests/p3_local_postfix_array_elements.rs` 检查后置自增、拒绝边界、来源和五行验证运行；语料指纹固定八个输入 |
| `p3-interface-field-initializers/` | javac 23.0.1; positive: `javac --release 8 -g:none -d v8 InterfaceInitProbe.java InitEffects.java InitRunner.java`; two verifier-checked classfile patches replay via `python3 replay_negative.py` | nine version-52 classes with 24 Code attributes, hashes in the fixture `README.md`; original and both boundary execution goldens | `tests/p5_corpus_fingerprint.rs` pins all 21 non-README/script inputs; `recover-interface-field-initializers` 1.3 positive and effect/order boundaries |
| `p3-immediate-functional-receivers/` | javac 23.0.1; `javac --release 8 -g` for each isolated subject and source-only runner | three version-52 subject classes, nine Code attributes; exact SHA-256 and original `-Xverify:all` outputs in `v8/class-sha256.json` | `type-immediate-functional-receivers` direct array/method failures and bound-local control; corpus fingerprint pins 10 non-README inputs |
| `p3-int-comparisons/` | javac 23.0.1; `javac --release 8 -g:none -d v8 IntComparisons.java` | `v8/IntComparisons.class` (52.0, 647 bytes, SHA-256 `8b535ab8…3583ff6`), with the bytecode of every member, the pre-fix recovered texts and the `javac` refusals in that directory's `README.md` | `tests/p3_boolean_contexts.rs` (an integer binary comparison keeps each operand's own spelling — `1 == n`, `0 < n`, `1 < n` with the constant on the left, `n == 1`, `n > 0` with it on the right, `n != 0` with two variable operands — the predecessor change's boolean shapes stay the controls, and the mixed pair comparison `flag() == 1` is refused rather than published, the disposition its predecessor handed to this rule); `tests/p3_execution_comparison.rs` (nine members compiled under a declaration derived from the run's own facts and executed against the original, all nine executed with identical traces) |
| `p3-hoisted-boolean/` | javac 23.0.1; `javac --release 8 -g:none -d v8 HoistedBoolean.java` | `v8/HoistedBoolean.class` (52.0, 743 bytes, SHA-256 `b9184230…d64d1640`), with the bytecode of every member, the pre-fix recovered texts and the `javac` refusals in that directory's `README.md` | `tests/p3_hoisted_boolean.rs` (one local's type is decided once, before its statements are built: a hoisted local copied from a local the body declared `boolean` is `boolean` and its condition is a truth test, the same shape with the arms exchanged gets the same answer, a copy chain through two more locals stays `boolean`, a literal-only local and a pure-`int` local keep their text, the plan's own entries are billed to the run's `IrItems` bound, and a write that cannot be spelled as the decided type or a value the evidence cannot type where a boolean is required is refused with its bytecode quoted); `tests/p3_execution_comparison.rs` (six members compiled under a declaration derived from the run's own facts and executed against the original with identical traces, the two refusals recorded as boundaries) |
| `p3-concat-conversion/` | javac 23.0.1; `javac --release 8 -g:none -d v8 ConcatConversion.java` | `v8/ConcatConversion.class` (52.0, 1461 bytes, SHA-256 `0e999005…7803b932`), with the bytecode of every member, the pre-fix recovered texts and the two sides' executed values in that directory's `README.md` | `tests/p3_concat_conversion.rs` (a presented chain keeps each `append`'s own conversion and starts in a string context: `"" + arg0 + arg1 + "!"` for two numeric parts, `"" + (arg0 + arg1) + "!"` for one part that is itself an addition, a `boolean` part spelled `true`/`false` by its descriptor, `null` and object parts by theirs, the observable and throwing parts in the bytecode's order, and the all-`String`/numeric-last controls unchanged); `tests/p3_execution_comparison.rs` (every member compiled under a declaration derived from the run's own facts and executed against the original, with the counter's moves compared around each call); `crates/jarde-cli/tests/task_cli.rs` (the deep chain the pre-fix printer aborted on, through the CLI subprocess) |
| `p3-edge-settlement/` | javac 23.0.1; `javac --release 8 -g:none -d v8 Settled.java` | `v8/Settled.class` (52.0, 656 bytes, SHA-256 `17d173ad…cd64cf9c`), with the bytecode of every member in that directory's `README.md` | `tests/p3_edge_settlement.rs` (P3 2.15: how one exception edge settles the block it leaves — a row whose range holds no `may_throw` instruction cannot be left through and does not quote the block, a row whose range begins **inside** its block settles it where the row names a `catch` type, and `noThrowFinally` names the dead copy by the uncovered-blocks quote) |

### Java syntax audit additions (2026-09-23)

| directory | frozen class | source-only support and tests |
| --- | --- | --- |
| `p3-throw/` | `v8/ThrowProbe.class`, 946 bytes, 9 Code attributes, SHA-256 `0b7a0e314fc3c2ee0ecd2bd226033aa735374d9456bc502bfba0ac7b5845e8a3` | `ThrowEffects`, runner, guard/stack source probes; `tests/p3_throw.rs` applies exact method patches in memory for refusal boundaries |
| `p3-final-static/` | `v8/FinalStaticProbe.class`, 1089 bytes, 5 Code attributes, SHA-256 `2bdb603ff8b3638d48256163fb729a0d5ba34a07a65161221b339ecb7ce59a45` | support and runner stay source-only; `tests/p3_final_static.rs` checks blank final assignments, name collisions and execution order |
| `p3-instanceof/` | `v8/InstanceOfProbe.class`, 1548 bytes, 15 Code attributes, SHA-256 `d8f4a437d0cd0f3ec12672791f27b1c11cfbcdc6885d10eddcd3114aa0838e56` | support and runner stay source-only; `tests/p3_instanceof.rs` checks static types, arrays, functional targets and boolean consumers |
| `p3-bitwise/` | `v8/BitwiseProbe.class`, 1237 bytes, 22 Code attributes, SHA-256 `20a3b3cb465564ca4b6a3e4eb4af9b32cda7805c750ec503ea4e5265216a3bda` | source-only effects and runner; `tests/p3_bitwise.rs` covers integral and boolean expressions, type flow and complete-class execution |
| `p3-deferred-value-order/` | `v8/DeferredValueOrder.class`, 1338 bytes, 19 Code attributes, SHA-256 `44eaaf23c354566f25ba429e49f7d34a73a114c7de35909360b705dcc63c3d04` | 14 exact Code patches insert an independent call after each producer; source-only helper/runner covers 83 independent runtime cases; `tests/p3_deferred_value_order.rs` |
| `p3-floating-constants/` | `v8/FloatingConstants.class`, 1722 bytes, 29 Code attributes, SHA-256 `7d557979522ebda315a93715c37ba50b480f6780eb8c4be86ae6a14e540b9652` | source-only helper/runner compares raw float/double bits, overloads and consumers; `tests/p3_floating_constants.rs` |
| `p3-lambda-adaptation/` | `v8/LambdaAdaptationProbe.class`, 2205 bytes, 9 Code attributes, SHA-256 `1f76c28361a2c54405e82950812863da1ea2f5a1012ef3a07e1049e5e4d45c6f` | source-only helper/runner covers 27 dynamic argument, receiver and constructor cases; `tests/p3_lambda_adaptation.rs` |
| `p3-type-qualifier/` | `v8/TypeQualifierProbe.class`, 479 bytes, 6 Code attributes, SHA-256 `0b8257791ce1d3544a42e5df31cfe8b9eee9ea5804b59c678caffb41f416634a` | source-only owners/runner covers 8 static target and field cases; `tests/p3_type_qualifier.rs` |
| `p3-primitive-conversions/` | `v8/PrimitiveConversions.class`, 1720 bytes, 31 Code attributes, SHA-256 `553c412824027fd41ad1176034bae95d3f2cf72fee7eb4fc2a06f02188b2179d` | 15 conversion opcodes, overloads, rounding chains and effects; 41 original runtime lines; `tests/p3_primitive_conversions.rs` |
| `p3-narrow-integer-returns/` | `v8/NarrowIntegerReturns.class`, 1288 bytes, 14 Code attributes, SHA-256 `90219c1ac7c93b53bee50da43ac792cd7e628dba4bff7b0f697a8b64a9b5d14c`; three 379-byte major-49 stack-join variants and two boolean boundary classes | 12 main-method descriptors patched while all Code remains identical; 49 verifier-checked main cases. Java 8 source plus controlled patches reproduce B/C/S shared-`ireturn` joins and verifier-valid raw-2 boolean arguments; `tests/p3_narrow_integer_returns.rs`, `tests/p3_narrow_return_boundaries.rs`, and fixture README |
| `p3-partial-array-allocation/` | `v8/PartialArrays.class`, 777 bytes, 10 Code attributes, SHA-256 `487389b770dbfdd226ae9ae3ff87c9d6be9df4e7df01781505f5d2a55e10e8f2` | source-only effects/runner cover 69 verifier-checked allocation cases; `tests/p3_partial_array_allocation.rs` |
| `p3-narrow-field-stores/` | `v8/NarrowFieldStores.class`, 1526 bytes, 19 Code attributes, SHA-256 `d4797140eed55121de091518e7db4d9dcb5052d0c9a451ef6ded07e1589af8ef` | six B/C/S field and Fieldref descriptors patched with Code byte-identical; source-only effects/runner cover 267 verifier-checked cases; `tests/p3_narrow_field_stores.rs` |
| `p3-narrow-array-stores/` | `v8/NarrowArrayStores.class`, 760 bytes, 10 Code attributes, SHA-256 `a3464f4b62da257e4cd4ff70970a05360475e51502a938c675392b334b359803` | six B/C/S descriptor and store-opcode patches; source-only effects/runner compare 147 verifier-checked rows, with separate legal Z/unknown/boolean boundaries; `tests/p3_narrow_array_stores.rs` and fixture README |
| `p3-narrow-array-store-order-e2e/` | generated 599-byte B/C/S class, SHA-256 `eaf27ba0badd9cf6649a829a6fccf17ad06081f6523395f0dff7179fc9d51470` (no committed class) | patched helper call descriptors and stores preserve array/index/value order, exceptions and once-only effects across 24 verified rows; `tests/p3_narrow_array_store_order.rs` |
| `p3-boolean-field-stores/` | `v8/ZFieldStores.class`, 626 bytes, 11 Code attributes, SHA-256 `671ee4999373f8d95e989a480ded9a648b9733c44ddb5c12f408b24e3b4b1e96` | two Z field and Fieldref descriptors patched with Code byte-identical; source-only effects/runner cover 40 verifier-checked cases; `tests/p3_boolean_field_stores.rs` |
| `p3-boolean-array-stores/` | Java 8 `RawBool` / `Order` patched `[Z` classes, SHA-256 `32286d138ace6a328a8c5ba7d0a0d66fadb4bf9dbbab79385e6dc3a01dcd6c2b` / `31d8fc3394c39cfe9a9e62da435af9245d2f26cd24e62e077e7310e1bf97d414` | descriptor + `iastore→bastore`/`iaload→baload` exact patches; original/Jarde complete classes match 8 boundary rows and 4 effect/exception rows under `-Xverify:all`; `tests/p3_boolean_array_stores.rs` and fixture README |
| `p3-boolean-array-initializers/` | Java 8 patched `BoolInit` / `BoolInitEffectful` classes, SHA-256 `e0e8cf5cf99fdb3e402db16e6d0b86db1b8b8672df2e684523c72a67df724a3f` / `ea2175192c0967da85ef5d78253bd458fb8a5543bcaf21f622b20a2caf59a8d0` | descriptor + `newarray` atype + `iastore→bastore` exact patches; original/Jarde complete classes match five low-bit elements and five effect/exception rows under `-Xverify:all`; `tests/p3_boolean_array_initializers.rs` and fixture README |
| `p3-integer-boolean-returns/` | Java 8 source-only `IntegerBooleanReturns.java`, `v8/IntegerBooleanReturns.class` SHA-256 `5b993e7c5b28588ded97dc53b6982608895745fdd625a77e0feb69ee6783eade`; major-49 shared `ireturn` variant SHA-256 `3b727e5b7bb8b630eca95ada5f0ff6516063e58fa94718ba36cf1c649ca107e2` | six return descriptors patched to `Z` with unchanged Code; 49 original/Jarde verifier-checked rows cover low-bit values, one call, field updates and synchronized null; exact patches, replay, JADX stage and switch boundary in the fixture README and `tests/p3_integer_boolean_returns.rs` |
| `p3-popped-static-qualifier/` | `v8/StaticQualifierProbe.class`, 721 bytes, 7 Code attributes, SHA-256 `21cdace20a84accd06e52567c250b0359ef76e059df993cc6c00a9fe1d6b7266` | source/runner preserve the four-line pre-fix whole-class comparison; `tests/p3_popped_static_qualifier.rs`; reproducible Jarde/JADX replay in the fixture README |
| `p3-compound-lvalue-updates/` | `v8/CompoundProbe.class`, 1113 bytes, 12 Code attributes, SHA-256 `889f36d3a5c06951d32762c8914829c059d8b1d6692001c08df73404c92f673f`; boundary probe has 24 Code attributes | source-only before/after baseline, five verifier-checked identity/consumer variants, ordinary assignment, wide-type refusal boundaries, null/bounds order and same-lvalue snapshots; see fixture README and the OpenSpec replay directory |
| `p3-bridge-projection/` | javac 23.0.1 `--release 8 -g`, then documented equal-length name/flag patches for negative and orphan cases | positive `BridgeProbe` plus `BridgeApi`, patched effectful `FakeBridge`, and patched pure-forward `OrphanBridge`; SHA-256, rebuild steps, `-Xverify:all` outputs, reader census and corpus fingerprint in the fixture README |
| `p3-conditional-values/` | `v8/TernaryCore.class` (424 B, 6 Code), `v8/TernaryValues.class` (1414 B, 15 Code), and `v8/ConditionalBoundarySwitch.class` (545 B, 7 Code), javac 23.0.1 `--release 8 -g:none` | source-only runners fix two minimal, 16 full conditional-value and four legal three-way switch rows; original/JADX/Jarde 的当前完整类重编执行一致，冻结的 Jarde 修前失败留作历史证据；见 fixture README 与 OpenSpec `recover-conditional-values/verification-3.1.md` |
| `p3-assert-core/` | `v8/AssertCore.class` (1187 B) and two verifier-valid one-instruction/two-instruction `<clinit>` patches | source-only runner compares `-ea`/`-da`, wrong assertion-status owner and non-0/1 boolean-field writes; frozen three-way complete sources and class SHA in fixture README |

These Java syntax audit inputs use javac 23.0.1 `--release 8`; fixture-specific generation commands
and patches are documented beside the inputs. The reader census walks every committed `.class`
under this directory, validates the complete class and member views, and counts each decodable Code
body, handler and control-flow target. The 2026-09 syntax-recovery expansion's current corpus
measures 316 classes, 1,674 Code attributes, 153 handler records, 902 branch/switch targets and
eight historical subroutines. Since the 310 / 1,645 / 144 / 880 baseline recorded in
`54e432ed`, two frozen inputs for conditional boolean field stores add five Code bodies and six
branch targets each; four `p3-precise-rethrow/v8` inputs add 19 Code bodies, nine handlers and ten
branch targets. Together these six classes account for the 29 bodies, nine handlers and 22 targets
in the updated census. This is a structural input census, not a claim that every fixture is fully
recovered.
`corpus-fingerprint.json` records the current corpus file count and digests; adding a fixture input
requires regenerating and reviewing that manifest. Structural counts do not imply syntax recovery
acceptance.

### The replayable compile-and-execute comparison (P3 3.3)

`tests/p3_execution_comparison.rs` is the shell procedure the earlier P3 records describe, as one
command:

```text
cargo test --test p3_execution_comparison --locked -- --ignored --nocapture
```

It reads every fixture above that P3 3.3 names (the `p3-local-rewrite`, `p3-scope`, `p3-handlers` and
`historical/ecj-4.6.1/v52` samples and the whole `p3-corpus/` matrix), builds a method declaration for
each member **from the run's own facts** (the payload's descriptor, access flags and parameter-slot
count, and `MethodFacts::parameter_types`), compiles the recovered text under it with
`javac --release 8`, and runs the original and the generated body side by side, comparing their
traces line for line: return values, the order of the fixture's own output, the times its counter
moved, and every exception with its suppressed exceptions. It is `#[ignore]`d so that `cargo test`
stays green without a JDK, and CI's `stable` job runs it with `-- --ignored`. The samples' provenance,
the flag matrix, the recorded tables and every boundary it found are in `p3-corpus/README.md`.

## P4 modern samples (`p4-modern/`)

| directory | compiler / command | outputs and digests | read by |
| --- | --- | --- | --- |
| `p4-modern/` | `javac 23.0.1` (`/usr/bin/javac`); `--release 16` for the record and its annotation type, `--release 17` for the sealed, nest, module and concat samples, `--release 8` for the Java 8 contrast | eleven `.class` files at 52, 60 and 61, SHA-256 and byte counts in `p4-modern/README.md` | `tests/p4_modern_facts.rs` (P4 1.2 modern structural facts); the reader's own `repository_class_fixtures_validate_without_false_target_rejections` sweep also reads every one of them |

The directory's `README.md` records what each output carries, and what this compiler does **not**
produce: `javac 23.0.1` emits no `CONSTANT_Dynamic` at all, so the constant-dynamic graph's
fixtures are hand-built in `tests/p4_modern_facts.rs` (`condy_fixture`), exactly as P1's
`p1_xref_bootstrap.rs` builds its condy samples.

## P4 golden diagnostics (`p4-golden/`)

Each JSON file records one group of replays for one input: its generator, its provenance, the blake3
digest and length of the bytes that generator produces, and the answer the public reader entries
returned for it — the header planes under `forensic` and the code of the `strict` refusal, the
modern-fact planes, and every diagnostic's code, severity and message verbatim.
`tests/p4_golden.rs` rebuilds every fixture from the named generator and asserts the digest, so a
changed builder or a rewritten expectation fails here instead of being absorbed; the files never
regenerate themselves.

| file | entries |
| --- | --- |
| `illegal-modern.json` | readable class files that break a release rule of their own release: a `Record` entry at 59, `PermittedSubclasses` at 60, `NestMembers` and `ConstantValue` inside a `method_info`, the `ACC_MODULE` bit at 52, `jsr` at 52, `invokedynamic` at 50 |
| `version-boundaries.json` | a `Record` sample at major 72 (above every registered release) and one with a `minor_version` the format rule rejects |
| `legal-modern.json` | the unpatched `p4-modern/` samples, whose legal placements and empty diagnostic list are the negative space of the entries above |

Only two entries have checked-in bytes of their own, and both are the committed samples themselves:
the version-patched fixtures, whose bytes are `p4-modern/`'s with the two version fields changed by
`version_patched`, and so keep the sample's own SHA-256 as their provenance. Everything else is
assembled by the named generators in `tests/p4_golden.rs`, because `javac` cannot be asked to emit
an illegal release shape.

## Corpus fingerprint (`corpus-fingerprint.json`, P5 1.1)

Every file below `tests/fixtures/` and `fuzz/corpus/` — the checked-in samples and archives, the
golden documents, and the committed fuzz seeds — is fingerprinted in one document.
`corpus-fingerprint.json` records a blake3 digest and a byte count for each of the 767 files it
covered when it was written — the count is data, not a promise — and beside them the index P5's
measurement baseline reads: the eight corpus dimensions (versions,
compiler, packaging, identity, bytecode, recovery, degradation, adversarial), the carrier behind
each, and the acceptance row each input feeds. The document is rendered from the tables in
`tests/p5_corpus_fingerprint.rs`, which also verifies it, so it cannot drift from either the bytes
or the classification.

```text
verify:      cargo test --test p5_corpus_fingerprint --locked
with output: cargo test --test p5_corpus_fingerprint --locked -- --nocapture
regenerate:  cargo test --test p5_corpus_fingerprint --locked -- --ignored regenerate_corpus_fingerprint
```

A changed, missing or unlisted corpus file fails the verify command and names the file with the
recorded and current digests; the corpus is the thing to fix, and the regenerator is for a change
that was intended. Two kinds of file sit deliberately outside the walk: a directory's `README.md`
(provenance prose is a record *about* the corpus, not an input to it — otherwise fixing a typo
would move the fingerprint) and the two `fuzz/corpus/*.py` seed generators. Neither is pinned;
neither is read by a test at run time.

The manifest is an index, not a second pin: a generated fixture's digest stays asserted where it
already was (its golden document), and the manifest only points at it. `builder` and `record`
carriers are not byte-pinned either. What the fingerprint therefore does *not* fix is listed in
its own `known_gaps`: the WAR, Boot-executable and ZIP64 trees and most duplicate-origin shapes
exist only as bytes `tests/p1_artifact_tree.rs` builds, the two proptest suites generate their
cases per run (CI's fixed `PROPTEST_RNG_SEED` makes that reproducible, not pinned), and the JDK 25
oracle's reading comes from whatever JDK runs it.

## Consuming the fingerprint (`tests/p5_benchmark.rs`, P5 1.2/1.3)

P5 1.2's benchmark is the first consumer, and it reads this document rather than restating it: it
names two of the files listed above as its subjects (`fuzz/corpus/query/minimal-jar`, which A15's
own corpus list carries, and `historical/ecj-4.6.1/v52/HistoricalControlFlow.class`, a carrier of
the `recovery` dimension), checks each one's blake3 and byte count against `files` before the first
run, and asserts that the fingerprint still classifies each subject under the row or dimension the
harness names. The bytes a row runs over are therefore the bytes the fingerprint fixed, and a
rewritten fixture fails the benchmark instead of moving the baseline it is filed against. It also
records `blake3(corpus-fingerprint.json)` — the document cannot hold its own digest, so the
consumer computes it — as the corpus version of every row's measurement context.
