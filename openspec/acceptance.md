# 架构验收与阶段映射

来源：[最终架构 §20.3](../JVM_Rust_Engine_Final_Architecture.md#s20)。P0/P1/P2、分层与 P3 均已归档（P3 12/12，归档提交 `250fe1f`）。下表是持续适用的门槛，必须按阶段区分历史通过与尚未闭合的公开行为；OpenSpec 格式检查不能替代语料、编译或行为对照。

| ID | 验收主题 | 首次负责阶段 | 必须保留的证据 |
| --- | --- | --- | --- |
| A01 | 未使用 Methodref 不是调用 | P1 | 同 fixture 的 CP 命中与 X1 consumer 结果对照 |
| A02 | invokevirtual 位置精确 | P1，P0 先验 bytecode spans | 原 entry、方法完整 descriptor、BCI 50、opcode、CP index |
| A03 | annotation/signature/catch 中独有类型 | P1 | 分类别正例、候选过滤与不经过 Class CP 的样本 |
| A04 | LambdaMetafactory 与任意 bootstrap | P1 结构；P3 恢复；P4 增量语义 | implementation handle 和创建/调用的区分；未知最终目标 |
| A05 | nested condy 与共享图 | P1 结构；P4 语义 | visited/cycle、边数/深度预算、共享节点和各 use-site 的 via 路径 |
| A06 | root/11/17 MR 选择 | P1；P4 RuntimeMatrix | Physical 全量与 Java 8/11/17 选择；Manifest 条件及不合规诊断 |
| A07 | WAR 同名类 | P1 物理/选择；P2 解析 | 不同 ordinal/origin、显式 loader/order 与歧义状态 |
| A08 | DEFLATED nested JAR | P1 | 限内物化、超限 Partial、STORED 与 DEFLATED 成本区别 |
| A09 | 历史 jsr/finally | P0 指令；P1 原始 X1；P2 规范化；P3 恢复 | 无需 normalize 的 X1；3.4b 的真实 token/嵌套覆盖反例；3.5 克隆/origin 与可靠 fallback；P3 恢复另验收。P2 证据见 [3.4b/3.5](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-3-4b) 与 [5.3 golden](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-5-3-golden) |
| A10 | 缺失 StackMap/debug | P2；P3 无 debug 命名 | 独立 Frame 推导与版本合法性诊断；不得宣称 verifier 通过。P2 证据见 [4.1](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-4-1-frame)、[4.3a](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-4-3a-ssa)、[4.2b/4.3b](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-4-2b-4-3b)；`verification` 仍恒为 NotPerformed |
| A11 | Base.foo 声明、Sub CP owner | P2，P4 深度扩展 | symbolic 原 owner；宽候选/继承扩展后 resolves_to；缺失依赖不能变否定。P2 证据见 [2.4](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-2-4-decl-refs) 与 [0.1](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-0-1-loader) |
| A12 | accessor/concat 恢复隐藏调用 | P3；P4 现代恢复 | 恢复前后相同 X1 原始边、派生关系与多 origin source map |
| A13 | 成员级失败 | P2/P3 | 同类正常与失败方法并存；representation、quality、execution、诊断分开。P2 证据见 [5.1](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-5-1-cli) 与 [3.5](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-3-5-clones) |
| A14 | 全范围中断/缺失依赖 | P0 预算；P1 查询；后续各阶段回归 | 已扫描/未扫描范围、终止维度、取消与 resolution coverage，不假 Complete。P2 证据见 [1.3](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-1-3-budget)、[2.5](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-2-5-dispatch)、[4.2b/4.3b](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-4-2b-4-3b)、[5.1](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-5-1-cli)、[5.2](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-5-2-counts)、[5.3](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-5-3-golden) |
| A15 | 冷/热/关缓存完整结果一致 | P5，每个缓存引入时 | 固定 snapshot/query/view、完整运行语义 fingerprint；中断子集不要求相同。**P5 部分通过**：CP/Header facts cache 是今天唯一存在的第二条路径（默认 off）。**已成立**：冷/热/暖的 status、发布顺序、coverage、diagnostics **逐字段相等**，**差异只落在 `usage`**（逐路径枚举证明），**两个暖运行之间整档 fingerprint 相等**（暖路径是输入的纯函数）；跨 8 次进程内重复与 3 个独立进程一致。**未成立**：把「整档 fingerprint」读成必须相等——那要求 cache 什么都不省或伪造 usage，而本行列举的必须相等平面里**没有资源**。证据：`tests/p5_facts_cache.rs`（`the_cache_path_publishes_the_same_result_and_reports_what_it_saved`）与 `tests/p5_benchmark.rs` 的八面差分门禁（`a_candidate_may_be_enabled_only_while_its_differential_is_equivalent`）。**未启用**：缓存收益只在 charge 维度上确定，同机重复带宽 ~10%，故保留 disabled 并记录触发条件——见 [P5 验证记录](changes/p5-measured-optimization/verification.md)。另：**index 路径不存在**，故「关缓存」的第三种形态无对象 |
| A16 | 单方法按需边界 | P2/P3，P0 先验局部 bytecode | Header/Body 物化记录、扩展理由，不读无关 Body、不建全局 XRef。P2 证据见 [2.2](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-2-2-closure) 与 [5.2](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-5-2-counts)。P3 侧：一次恢复请求 = 一次 header + 一次 body + 一条 read 且 `analysis.reads.len()==1`（`tests/p3_recovery_entry.rs`）；按需 callee 让同次运行 `method_bodies==3`，而 fixture 声明的带体成员多于它、没被任何调用点命名的成员不读（3.2，`tests/p3_accessor_edges.rs`）；恢复层存在时同一 fixture 的 `Engine::query` 仍为 0 header / 0 body（`tests/p3_isolation.rs`）。P5 侧：同一 303 B 语料上局部行 `class_headers=1`/`method_bodies=1`/`read_bytes=303`，全范围行 `read_bytes=909`/`class_bytes=909`（`tests/p5_benchmark.rs`） |
| A17 | X1 零 CFG/SSA/AST | P1，P2–P5 持续回归 | 构造计数和编译依赖边界；不能仅以“没有输出源码”代替证明。P2 证据见 [5.2](changes/archive/2026-09-19-p2-jvm-ir/verification.md#verify-5-2-counts)：构造计数是预算代理，P2 验收时 Region/Java AST 尚无构造路径；P3 已有这两层，真实恢复路径的隔离回归**已完成**：同一 fixture 一次 `Engine::query` 的 IR/边/步/克隆全为 0 且 header/body 为 0，一次 `Engine::recover_method` 同一批维度非 0、header 1/body 1（`tests/p3_isolation.rs`），源码侧守卫已从 `jarde-java` 的公开类型声明派生恢复层禁用表并覆盖同一批受守卫文件（`tests/p2_contracts.rs`）；它是分层依赖拒绝的**补充而非替代**——两处会给出该能力边的依赖（`jarde-reader`/`jarde-query` → `jarde-java`）都被 cargo 在解析期以 `cyclic package dependency` 拒绝，守卫负责的是这条拒绝在未来重排后仍被复核 |
| A18 | 分析期间输入变化 | P0/P1，各缓存/并行阶段回归 | 固定字节源或可检测变化中止；跨快照 token/缓存隔离。**P5 缓存半已关闭**：facts cache 的 store 跨请求共享，故必须证明两个快照互不代答——(a) 持不同内容的两快照各自等于自己的 direct 运行；(b) 同内容不同 origin 的两快照共享 entry 但仍各发布自己的 identity；(c) 取消的请求不给另一个快照留任何东西（`tests/p5_facts_cache.rs::one_store_serves_two_snapshots_without_answering_either_with_the_other`）。token/cursor 半为既有 `tests/p1_query_api.rs`。**并行半无对象**：仓库不存在并行路径，故不声称验过 |

P2 出口时 A09、A10、A11、A13、A14、A16、A17 已在 P2 范围内通过，逐条现状与证据链接见 [P2 验收映射](changes/archive/2026-09-19-p2-jvm-ir/verification.md#p2-acceptance-map)；A11 的 P4 深度扩展、A09/A12 的 P3 恢复均已交付。P5 出口时的状态：**A01–A14、A16、A17 现已通过**；**A15 部分通过**（缓存半的语义条件成立，见上；整档 fingerprint 相等的读法不成立且不应作为条件）；**A18 部分通过**（P5 缓存半已关闭、并行半无对象）；A02 的 JDK 25 oracle 交叉检查**本机未运行**（环境缺口，CI 的 JDK job 负责）。表中不带 P2 状态的行不因 P2 的交付而改变。

## 语料与发布要求

每个 fixture 记录来源、生成命令/编译器版本、输入摘要、目标 dialect、runtime/output profile 和预期能力。真实历史 javac/ECJ 样本与手工构造边界样本分别标记，现代 `--release 8` 不替代历史 codegen。

输入矩阵覆盖 CLASS/JAR/WAR、Boot executable/deployed、MR、ZIP64、STORED/DEFLATED nested、同名/同字节多 origin、45–52 历史版本和 53–71 的逐 feature 注册。对抗矩阵覆盖截断、未知 CP/opcode、switch/wide、过长 attribute、非法索引、循环和超预算。

输出支持矩阵分别记录 parse、X1、resolution、decompile-quality、output-level。语法检查、重编译和语义验证各自记录；生产引擎不执行输入，只有已知受控 fixtures 参与隔离动态对照。完整执行与预算中断分别比较，冷/热一致性只适用于相同语义配置的完整执行。

当前 P0 的 Foundation 2.1–2.5、容器回归 3.1、classfile 对抗/性质回归 3.2、预算/取消/局部性门禁 3.3，以及 README、五维支持矩阵、低内存 CI、公共示例和实际测试记录 3.4，均已由对应任务和证据证明。公共 Engine 与有界 JSON CLI 已通过真实二进制入口验证；bytecode 边界另由受控 JDK 25 oracle 交叉检查，并以固定 ECJ 4.6.1 生成的 45.3–52.0 历史目标语料验证版本读取和 `jsr`/`ret` 边界。CLASS/JAR/WAR、重复项、小型真实 ZIP64、压缩、CRC、快照、MUTF-8、未知/嵌套 attribute、switch/wide、截断和属性/指令性质矩阵已复核；P0 的十一项请求 limit 均有边界证据，其中 `nested_depth` 是非累加高水位，其余十项维持累计/elapsed 语义（P2 1.3 另加入六个计费维度与第二个高水位 `dependency_depth`，当前 limit 面共十八项，见支持矩阵），公共入口预取消及枚举/读取/CP/handler/instruction 中途取消不伪造 Complete，未请求方法的非法 Body 不影响所选方法，P0 生产模块和依赖中不存在 XRef/CFG/SSA/AST/IR。Linux x86_64 的 stable、MSRV 1.88 和 supply-chain CI 已在 run `35168810088` 及后续完成记录 run `35169222351` 全部通过；Linux aarch64 有本地单作业证据。P0 3.5 的最终门禁、主规格同步与归档已完成，归档记录位于 `changes/archive/2026-09-17-establish-p0-foundation/`，已生效规格位于 `specs/`。P1 1.1 已建立 query relation、canonical consumer schema、PhysicalView/RuntimeView、RuntimeProfile、LoadDomain、显式 standalone/archive class location 与有向 nested origin chain，并以 A06/A07 类型测试证明身份、未知策略和序列化边界。P1 1.2 已用真实 DEFLATED nested fixture 验证 A08：显式 artifact-tree 保留完整 origin、Boot/WAR 物理 evidence、STORED/DEFLATED 重读、`nested_depth`/entry/bytes/cancel Partial coverage 和失败 sibling 隔离；普通 enumerate 仍不递归，也不声称 MR/runtime selection。P1 1.3 以 Manifest evidence 与唯一 winning level 派生标准 MR 选择与合规诊断（A06）。P1 2.1–2.4 实现结构 XRef：共享 class candidate 规则与损坏候选诊断、code/metadata/bootstrap/resource consumer（含 Record component 与 Code 内注解、实际使用点 descriptor 类型）、deferred bootstrap 图与 `UnsupportedAnalysis` 证据保留（A01–A05、A17）。P1 3.1–3.2 实现 target-bound 游标与分页、CLI `query`；3.3 建立验收语料索引、结构 XRef golden、proptest 性质与有界 fuzz 门禁（A14、A18）；3.4 完成文档同步、完整 CI 与归档，主规格新增 `artifact-views`/`query-api`/`structural-xref`。P1 的最终候选 CI run `35233298026`（stable/MSRV/supply-chain/fuzz-smoke 四个 job 全部 success）与逐项证据见 `changes/archive/2026-09-17-p1-query-xref/verification.md`。P2 已交付 3.x 的 raw/canonical 规范化、4.x 的 Frame/初始化/SSA、5.1 的方法分析库与薄 JSON CLI、5.2 的入口计数、5.3 的 golden/性质/有预算 fuzz，并已关闭复核新增的 4.2b（R9 异常固定点）与 4.3b（R10 派生存储计费）；拆包 7/7 完成。P2 归档时已把 spec deltas 同步进 `openspec/specs/`：`demand-resolver`、`jvm-ir`、`conservative-output` 三份新增能力，`analysis-contracts` 的 Purpose 与 `Provenance and execution are explicit` 修改，以及 `query-api` 过时阶段承诺的修正（`Engine::query` 的 `references_definition`/`may_dispatch_to` 仍返回 `UnsupportedAnalysis`，解析只经独立的显式环境入口）。A09–A11、A13、A14、A16、A17 按 [P2 验收映射](changes/archive/2026-09-19-p2-jvm-ir/verification.md#p2-acceptance-map) 在 P2 范围内通过：A16/A17 的证据仍是预算维度代理加 Cargo 分层与源码 token 守卫（没有逐 pass 构造计数器），且在 P2 验收时 Region/Java AST 尚无生产构造路径——不得据此声称已覆盖，也不得声称 Java 恢复或 verifier 通过。P3 已开始，现状见下节；P4/P5 未实施。只有真实测试记录满足对应任务和 change 的出口门槛后才归档。


当前恢复验收：**P3 已交付并归档（12/12，归档提交 `250fe1f`）**。复核的 P3-R1（旧值重读）、R2（fallback effect 漏失）、R3（分支声明作用域）、R4（确定性比较 elapsed 假红）与后续发现的 R5（`boolean` 参数按描述符定型）、R6（slot 复用按 spec 拆分）、R7（每条已解码指令必须被块或 `unreachable` 交代）**均已关闭**。A12 已由**公开入口**验收：同一次运行呈现直接字段表达式，且 `Engine::query` 的两条原始边逐字段不变（`tests/p3_accessor_edges.rs`）；按需 callee 读取与跨方法 origin 由 3.2 交付。A16/A17 已按恢复层真实存在重做：A17 由源码守卫 + 运行期隔离回归（`tests/p3_isolation.rs`）承担，不再沿用“尚无构造路径”的 P2 时点说明。证据见 [P3 验证记录](changes/archive/2026-09-20-p3-java8-recovery/verification.md#review-2026-09-19-recovery)。
