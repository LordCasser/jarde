During evidence collection, a fresh build from the concurrently changing working tree was attempted with a private Cargo target and failed before CLI execution. Rust reported a non-exhaustive match in `crates/jarde-java/src/build.rs` around line 5392: `ShortCircuitConsumer::Local(u16, ValueId)` was not covered in the `build_short_circuit_statement` match. This was an in-progress local-values edit, not a Jarde runtime result. Per root's direction, the baseline uses the separately supplied and SHA-256-pinned stable gateway-complete executable instead; it is explicitly not called latest. The root will independently refresh the latest-source report after the concurrent edit completes.
