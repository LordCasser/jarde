# Nested conditional value

`NestedConditional.java` freezes two cases. `nested(int)` remains the minimal integer tree `a > 10 ? (a > 100 ? 3 : 2) : 1`. In `nestedEffects`, `arm` now receives an integer `failAt`: the outer and inner true leaves pass `-1`, and the arm-2 leaf passes `a`; the helper throws when `failAt == 12`. Arm 2 can be selected only when `a > 10` and `a <= 100`, so this preserves the former `a == 12` exception and its event order while moving that branch into the helper, outside the conditional-value tree. `Runner.java` is unchanged.

`javac --release 8 -g:none` emits classfile major version 52. The original class and the JADX 1.5.6 source, compiled with a package-adjusted runner, both pass `java -Xverify:all`; their complete outputs are byte-for-byte equal (`original-run.txt` and `jadx-run.txt`, with an empty `jadx-run.diff`). This covers the six `nested` boundaries and all nine effect/exception inputs. The former exception still reports `ArithmeticException;message=arm-2;events=OI2`, and outer/inner exceptions still suppress later tests and leaves. `javap.txt` is the complete `javap -v -c -p` listing. `hashes.sha256` pins the fixture sources and the original, JADX, and Jarde helper class outputs.

For `nestedEffects(ILjava/lang/StringBuilder;)I`, the outer test branches at BCI 6 and the inner test at BCI 15. The three leaves each load the arguments/constants, invoke `arm`, and transfer directly to the single join: the calls are at BCIs 23, 34, and 45, their unconditional transfers target BCI 48, and there is no leaf-local branch or intermediate join. The condition's `innerLimit` call is at BCI 12; it is part of the condition, not a fourth arm. The canonical CFG and SSA dump in `ssa-probe.txt` has normal edges `18 -> 48`, `29 -> 48`, and `40 -> 48`; the join's `Stack(0)` Phi has exactly three `Instruction` inputs defined by those three arm calls. The local Phis at the join are trivial and resolve to method-entry values. Together with the bytecode, this establishes three Straight leaves at one final value join, without an intermediate merge-and-continue shape.

The frozen Jarde baseline was built from exact commit `e9bb60bb41aeeab241fc78bef348f7564b74fcd3`, exported with `git archive` to an isolated source directory and built in `/tmp/jarde-ncv-e9bb-target`; no shared uncommitted `build.rs` was used. `jarde-method.java`, `jarde-report.json`, and `jarde-report.txt` preserve the `nestedEffects` run with all evidence requested; `jarde-class-source.json` preserves the whole-class report beside the readable text body in `jarde-class-report.txt`. It remains `quality=fallback`, `syntax_status=not_java`, and `compile_status=not_attempted`: the conditional leaves are emitted as comments rather than recovered calls/values, and the final stack value is unresolved. The report's source-map and region-detail evidence categories are complete; its 17 source-map segments retain direct origins including BCIs 12, 23, 34, 45, and 48. This is baseline evidence, not a claim that the target recovery already works.

Jarde's class-source output does include the `arm(..., int, int)` helper with the new `failAt == 12` condition. `jarde-helper-source.java` isolates that exact emitted helper in a minimal wrapper; it compiles with `javac --release 8 -g:none` and produces major-version-52 bytecode. The full class-source output is retained in `jarde/NestedConditional.java`; javac reports missing returns only in `nested` and `nestedEffects`, so no Jarde runtime run is claimed. Tool versions, baseline commit/build command, CLI hash, compile logs, and exit statuses are recorded alongside the source/class hashes.
