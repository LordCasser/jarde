// jarde: presentation of `Patrol` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class Patrol extends java.lang.Object {
    public Patrol() {
        // @method <init>()V
        // @declaration a constructor of `Patrol`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    static int staticRef(java.lang.String s) {
        // @method staticRef(Ljava/lang/String;)I
        // @declaration a static method of `Patrol`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        return s.length();
    }

    int boundRef() {
        // @method boundRef()I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        return 42;
    }

    static int anyRef(Patrol p, java.lang.String s) {
        // @method anyRef(LPatrol;Ljava/lang/String;)I
        // @declaration a static method of `Patrol`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        return s.hashCode() + p.boundRef();
    }

    // jarde: generic Signature projection refused for `listCtor()Ljava/util/function/Supplier;`: unsupported (generic_call_binding_unproved): a same-class Methodref names this method or an adjacent overload
    java.util.function.Supplier listCtor() {
        // @method listCtor()Ljava/util/function/Supplier;
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        return java.util.ArrayList::new;
    }

    // jarde: generic Signature projection refused for `arrayCtor()Ljava/util/function/Function;`: unsupported (generic_call_binding_unproved): a same-class Methodref names this method or an adjacent overload
    java.util.function.Function arrayCtor() {
        // jarde: not recovered: the recovery run for `arrayCtor()Ljava/util/function/Function;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method arrayCtor()Ljava/util/function/Function;
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 5
        // the implementation `Patrol.lambda$arrayCtor$0(I)[I` takes `int` at parameter 0 and the site binds `java.lang.Integer` there, which is not the same shape
    }

    // jarde: generic Signature projection refused for `dispatch(Ljava/util/function/Supplier;Ljava/util/function/Function;)I`: unsupported (generic_call_binding_unproved): a same-class Methodref names this method or an adjacent overload
    int dispatch(java.util.function.Supplier s, java.util.function.Function f) {
        // @method dispatch(Ljava/util/function/Supplier;Ljava/util/function/Function;)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        return ((java.lang.Integer) s.get()).intValue() + ((java.lang.Integer) f.apply((java.lang.Object) "x")).intValue();
    }

    int supply() {
        // @method supply()I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        return 7;
    }

    int exerciseMethodRefs(Patrol other) {
        // jarde: not recovered: the recovery run for `exerciseMethodRefs(LPatrol;)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method exerciseMethodRefs(LPatrol;)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 1 12
        // the erased SAM `()Ljava/lang/Object;`, instantiated type `()Ljava/lang/Integer;` and implementation `Patrol.supply()I` require a parameter or return conversion outside the proven identity, Object check, or Object upcast cases
        // @bytecode 17
        // the instruction at BCI 17 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 18 21 16
        // the value at BCI 18 comes from an Duplicate at BCI 17, which produces no expression this subset writes
        // @bytecode 73 12 33 49 46 41 38 71 68 63 56 60 0 1 15 16 27 37 55
        // the value at BCI 73 was produced by a saved declaration this run could not commit
    }

    int len(java.lang.String s) {
        // @method len(Ljava/lang/String;)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        return s.length();
    }

    void preciseRethrow(java.lang.String mode) throws java.text.ParseException, java.io.IOException {
        // jarde: not recovered: the recovery run for `preciseRethrow(Ljava/lang/String;)V` produced no statement (explanation only); the artifact's own comment lines are below
        // @method preciseRethrow(Ljava/lang/String;)V
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 1 3 6
        // BCI 42: the exceptional path repeats code the normal path also runs — the `finally` copy javac emits for a `finally` clause; this candidate lacks the complete straight-body, copy, range, and ownership proof needed to merge them into one `finally`
        // @bytecode 9 12 13 15 16 19 20 21 23 26 29 32 33 35 38 39 42 43 44 47 48 49
        // 5 live block(s) are reachable only through edges the normal-flow view leaves out: [9, 20, 42, 29, 39]
    }

    static void log(java.lang.Exception e) {
        // @method log(Ljava/lang/Exception;)V
        // @declaration a static method of `Patrol`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        return;
    }

    int labeledBlock(int arg0) {
        // @method labeledBlock(I)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        int r;
        r = 0;
        if (arg0 < 0) {
        } else {
            r = arg0 * 2;
            if (arg0 == 0) {
                r = 1;
            } else {
                r = r + 3;
            }
        }
        return r;
    }

    int multiResource(java.nio.file.Path a, java.nio.file.Path b) throws java.io.IOException {
        // jarde: not recovered: the recovery run for `multiResource(Ljava/nio/file/Path;Ljava/nio/file/Path;)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method multiResource(Ljava/nio/file/Path;Ljava/nio/file/Path;)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 1 4 5 6 9 11 12 15 17 20 21 23 25
        // BCI 33: the protecting row does not end where this shape requires: the range a handler covers is not the one the guarded statement would run
        // @bytecode 28 30 33 34 37 38 41 43 44 46 48 51 53 56 59 61 63 65 68 70 71 73 74 77 78 81 84 86 88 90 93 95
        // 12 live block(s) are reachable only through edges the normal-flow view leaves out: [28, 33, 44, 71, 37, 41, 51, 68, 77, 93, 59, 84]
    }

    int charOps(char c, int arg1) {
        // @method charOps(CI)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        int r;
        int up = (char) (c - 32);
        r = up + arg1;
        switch (up) {
            case 65:
                r = r + 1;
                break;
            case 66:
                r = r + 2;
                break;
            default:
                r = r + 3;
                break;
        }
        return r;
    }

    int forever(int arg0) {
        // jarde: not recovered: the recovery run for `forever(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method forever(I)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 1 2 5 6 7 8 9 12 13 14 17 20 21 22 25 28 29 31 34 37 38 39 40 41 44 47 50 51 54 57 58 59 60
        // canonical block at BCI 2 on jsr path [] has more than one owner in the completed Region tree; the whole method is quoted
    }

    int unreachableAfter(int arg0) {
        // jarde: not recovered: the recovery run for `unreachableAfter(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method unreachableAfter(I)I
        // @declaration an instance method of `Patrol`, member flags 0x0000
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 1 2 5 6 7 8 9 12 13 14 17 20 21 22 25 28 29 31 34 37 38 39 40 41 44 47 50 51 54 57 58 59 60 61
        // canonical block at BCI 2 on jsr path [] has more than one owner in the completed Region tree; the whole method is quoted
    }

    private static int[] lambda$arrayCtor$0(int x$0) {
        // @method lambda$arrayCtor$0(I)[I
        // @declaration a static method of `Patrol`, member flags 0x100a
        // recovered from bytecode; presentation is not claimed to compile
        return new int[x$0];
    }
}
