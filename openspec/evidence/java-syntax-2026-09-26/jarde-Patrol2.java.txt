// jarde: presentation of `Patrol2` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class Patrol2 extends java.lang.Object {
    static int calls;

    static final boolean $assertionsDisabled;

    public Patrol2() {
        // @method <init>()V
        // @declaration a constructor of `Patrol2`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    static int check(int arg0) {
        // @method check(I)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        if (!Patrol2.$assertionsDisabled) {
            if (arg0 <= 0) {
                // @bytecode 10
                // the instruction at BCI 10 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
                // @bytecode 13
                // the instruction at BCI 13 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
                // @bytecode 36 33 26
                // the value at BCI 36 comes from an Duplicate at BCI 13, which produces no expression this subset writes
            }
        }
        return arg0 * 2;
    }

    static synchronized int bump(int arg0) {
        // @method bump(I)I
        // @declaration a static method of `Patrol2`, member flags 0x0028
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 + 1;
    }

    static int dow(int arg0) {
        // jarde: not recovered: the recovery run for `dow(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method dow(I)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 1 2 5 6 7 8 11 14 17 18 19 22 23
        // canonical block at BCI 2 on jsr path [] has more than one owner in the completed Region tree; the whole method is quoted
    }

    static int strSwitch(java.lang.String s) {
        // @method strSwitch(Ljava/lang/String;)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        switch (s) {
            case "a":
                return 1;
            case "b":
                return 2;
            default:
                return -1;
        }
    }

    static int cond(int arg0) {
        // @method cond(I)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        int r;
        r = arg0 > 0 ? side(1) : side(2);
        if (arg0 > 10) {
            if (arg0 > 100) {
            }
        }
        // @bytecode 37
        // the value at BCI 37 is the entry state of stack depth 0, which no instruction produced
        // @bytecode 38 39 40 41
        // the statement at BCI 41 reads `t`, and no statement of this body declared that local: the write that would have declared it was refused, so its name cannot be read here (P3 2b.2)
    }

    static int side(int x) {
        // @method side(I)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        Patrol2.calls = Patrol2.calls + 1;
        return x;
    }

    static int bits(int a, int b, int c) {
        // @method bits(III)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        return a & b | c ^ 255 | a >>> 3 | a << 1;
    }

    static boolean fp(double a, double b) {
        // @method fp(DD)Z
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        return (!(a > b) ? a <= b ? 1 : 0 : 1) % 2 != 0;
    }

    static int lng(long a) {
        // @method lng(J)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        long m = a * 4294967296L;
        if (m == -1L) {
            return 1;
        } else {
            if (m > 4611686018427387903L) {
                return 2;
            } else {
                return (int) (m >>> 32);
            }
        }
    }

    static int multiCatch(int arg0) {
        // @method multiCatch(I)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        try {
            if (arg0 == 0) {
                throw new java.lang.NumberFormatException("n");
            } else {
                if (arg0 == 1) {
                    throw new java.nio.file.FileSystemException("f");
                } else {
                    return 0;
                }
            }
        } catch (java.lang.NumberFormatException | java.nio.file.FileSystemException e) {
        }
        // @bytecode 44
        // 1 live block(s) are reachable only through edges the normal-flow view leaves out: [44]
    }

    // jarde: generic Signature projection refused for `foreach([ILjava/util/List;)I`: unsupported (ordinary_generic_source_unproved): same-run Program/SSA cannot prove the body under parameterized types
    static int foreach(int[] arr, java.util.List list) {
        // @method foreach([ILjava/util/List;)I
        // @declaration a static method of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        int n;
        int[] local3;
        n = 0;
        local3 = arr;
        for (int x : local3) {
            if (x < 0) {
            } else {
                n = n + x;
            }
        }
        local3 = list.iterator();
        while (local3.hasNext()) {
            java.lang.String s = (java.lang.String) local3.next();
            if (s.isEmpty()) {
                break;
            } else {
                n = n + 1;
            }
        }
        return n;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `Patrol2`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        $assertionsDisabled = (!Patrol2.class.desiredAssertionStatus() ? 1 : 0) % 2 != 0;
    }
}
