// jarde: presentation of `NonInvokeQualifierProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public final class NonInvokeQualifierProbe extends java.lang.Object {
    public NonInvokeQualifierProbe() {
        // @method <init>()V
        // @declaration a constructor of `NonInvokeQualifierProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int call(Child arg0) {
        // @method call(LChild;)I
        // @declaration a static method of `NonInvokeQualifierProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0.ping();
    }
}
