// jarde: presentation of `StaticStraight` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class StaticStraight extends java.lang.Object {
    static int value;

    public StaticStraight() {
        // @method <init>()V
        // @declaration a constructor of `StaticStraight`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int get() {
        // @method get()I
        // @declaration a static method of `StaticStraight`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return StaticStraight.value;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `StaticStraight`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        StaticStraight.value = 7;
    }
}
