// jarde: presentation of `StaticConditional` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class StaticConditional extends java.lang.Object {
    static int value;

    public StaticConditional() {
        // @method <init>()V
        // @declaration a constructor of `StaticConditional`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int get() {
        // @method get()I
        // @declaration a static method of `StaticConditional`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return StaticConditional.value;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `StaticConditional`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        if (java.lang.System.getProperty("jarde.static.missing") == null) {
            StaticConditional.value = 1;
        } else {
            StaticConditional.value = 2;
        }
    }
}
