// jarde: presentation of `ThrowSwitch` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class ThrowSwitch extends java.lang.Object {
    public ThrowSwitch() {
        // @method <init>()V
        // @declaration a constructor of `ThrowSwitch`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static void select(int arg0, java.lang.RuntimeException arg1, java.lang.RuntimeException arg2) {
        // @method select(ILjava/lang/RuntimeException;Ljava/lang/RuntimeException;)V
        // @declaration a static method of `ThrowSwitch`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        switch (arg0) {
            case 0:
                throw arg1;
            case 1:
                throw arg2;
            default:
                throw null;
        }
    }
}
