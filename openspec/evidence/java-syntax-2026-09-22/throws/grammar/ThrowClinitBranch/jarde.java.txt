// jarde: presentation of `ThrowClinitBranch` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class ThrowClinitBranch extends java.lang.Object {
    public ThrowClinitBranch() {
        // @method <init>()V
        // @declaration a constructor of `ThrowClinitBranch`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `ThrowClinitBranch`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        if (java.lang.System.nanoTime() == 0L) {
            throw new java.lang.IllegalArgumentException("a");
        } else {
            throw new java.lang.IllegalStateException("b");
        }
    }
}
