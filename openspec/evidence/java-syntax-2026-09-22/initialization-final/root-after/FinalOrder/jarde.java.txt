// jarde: presentation of `FinalOrder` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class FinalOrder extends java.lang.Object {
    public static final int A;

    public static final int B;

    public FinalOrder() {
        // @method <init>()V
        // @declaration a constructor of `FinalOrder`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int result() {
        // @method result()I
        // @declaration a static method of `FinalOrder`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return FinalOrder.A * 10 + FinalOrder.B;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `FinalOrder`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        A = FinalSupport.next();
        B = FinalSupport.next();
    }
}
