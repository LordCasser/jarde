// jarde: presentation of `FinalInterface` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public interface FinalInterface {
    public static final java.lang.Object VALUE;

    public static int result() {
        // @method result()I
        // @declaration an interface's static method of `FinalInterface`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return FinalSupport.check(FinalInterface.VALUE);
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `FinalInterface`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        FinalInterface.VALUE = FinalSupport.object();
    }
}
