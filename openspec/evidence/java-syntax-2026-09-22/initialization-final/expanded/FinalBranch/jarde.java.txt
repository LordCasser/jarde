// jarde: presentation of `FinalBranch` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class FinalBranch extends java.lang.Object {
    public static final int VALUE;

    public FinalBranch() {
        // @method <init>()V
        // @declaration a constructor of `FinalBranch`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int result() {
        // @method result()I
        // @declaration a static method of `FinalBranch`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return FinalBranch.VALUE;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `FinalBranch`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        if (FinalSupport.flag()) {
            FinalBranch.VALUE = 7;
        } else {
            FinalBranch.VALUE = 9;
        }
    }
}
