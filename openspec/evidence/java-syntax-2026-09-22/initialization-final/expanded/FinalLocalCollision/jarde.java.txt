// jarde: presentation of `FinalLocalCollision` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class FinalLocalCollision extends java.lang.Object {
    public static final int local0;

    public static final int local0_2;

    public FinalLocalCollision() {
        // @method <init>()V
        // @declaration a constructor of `FinalLocalCollision`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int result() {
        // @method result()I
        // @declaration a static method of `FinalLocalCollision`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return FinalLocalCollision.local0 * 10 + FinalLocalCollision.local0_2;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `FinalLocalCollision`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        int local0 = FinalSupport.next();
        FinalLocalCollision.local0 = local0 + local0;
        FinalLocalCollision.local0_2 = FinalSupport.next();
    }
}
