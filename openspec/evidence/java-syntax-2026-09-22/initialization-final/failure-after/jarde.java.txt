// jarde: presentation of `FinalFailure` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class FinalFailure extends java.lang.Object {
    public static final int first;

    public static final int second;

    public FinalFailure() {
        // @method <init>()V
        // @declaration a constructor of `FinalFailure`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int value() {
        // @method value()I
        // @declaration a static method of `FinalFailure`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return FinalFailure.first * 10 + FinalFailure.second;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `FinalFailure`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        first = FinalFailureSupport.next("A");
        second = FinalFailureSupport.next("B");
    }
}
