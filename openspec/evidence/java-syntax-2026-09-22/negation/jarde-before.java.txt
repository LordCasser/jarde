// jarde: presentation of `NegAudit` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class NegAudit extends java.lang.Object {
    public NegAudit() {
        // @method <init>()V
        // @declaration a constructor of `NegAudit`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int mixed(int arg0) {
        // jarde: not recovered: the recovery run for `mixed(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method mixed(I)I
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        // @bytecode 6
        // the instruction at BCI 6 is not part of the provable subset
        // @bytecode 8
        // the value at BCI 8 comes from an Other at BCI 2, which produces no expression this subset writes
    }

    public static long longMin() {
        // @method longMin()J
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        long local0 = -9223372036854775808L;
        // @bytecode 5
        // the instruction at BCI 5 is not part of the provable subset
        // @bytecode 6
        // the value at BCI 6 comes from an Other at BCI 5, which produces no expression this subset writes
    }

    public static java.lang.String concat(int arg0) {
        // jarde: not recovered: the recovery run for `concat(I)Ljava/lang/String;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method concat(I)Ljava/lang/String;
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 13
        // the instruction at BCI 13 is not part of the provable subset
        // @bytecode 20 17
        // the value at BCI 20 comes from an Other at BCI 13, which produces no expression this subset writes
    }

    public static java.lang.Integer boxed(int arg0) {
        // jarde: not recovered: the recovery run for `boxed(I)Ljava/lang/Integer;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method boxed(I)Ljava/lang/Integer;
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 5 2
        // the value at BCI 5 comes from an Other at BCI 1, which produces no expression this subset writes
    }

    public static int branch(int arg0) {
        // jarde: not recovered: the recovery run for `branch(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method branch(I)I
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 0 5 7 2
        // the value at BCI 2 comes from an Other at BCI 1, which produces no expression this subset writes
    }

    public static int loop(int arg0) {
        // jarde: not recovered: the recovery run for `loop(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method loop(I)I
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0
        // the loop@1 rule did not claim the block at BCI 0: it requires a test block whose every instruction is part of a value expression, and the instruction at BCI 1 is not part of one, so presenting the structure would have moved that effect out of the shape it decides
        // @bytecode 0 5 11
        // 3 live block(s) are reachable only through edges the normal-flow view leaves out: [0, 5, 11]
    }

    public static double floating(double arg0, double arg2) {
        // jarde: not recovered: the recovery run for `floating(DD)D` produced no statement (explanation only); the artifact's own comment lines are below
        // @method floating(DD)D
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 3
        // the instruction at BCI 3 is not part of the provable subset
        // @bytecode 5
        // the instruction at BCI 5 is not part of the provable subset
        // @bytecode 7
        // the value at BCI 7 comes from an Other at BCI 3, which produces no expression this subset writes
    }

    public static int stored(int arg0) {
        // @method stored(I)I
        // @declaration a static method of `NegAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 2
        // the value at BCI 2 comes from an Other at BCI 1, which produces no expression this subset writes
        arg0 = arg0 + 1;
        // @bytecode 6 7 8 9
        // the statement at BCI 9 reads `local1`, and no statement of this body declared that local: the write that would have declared it was refused, so its name cannot be read here (P3 2b.2)
    }
}
