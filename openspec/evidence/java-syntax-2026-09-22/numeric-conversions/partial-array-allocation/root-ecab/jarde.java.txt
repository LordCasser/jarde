// jarde: presentation of `PartialArrays` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class PartialArrays extends java.lang.Object {
    public static int[][] held;

    public PartialArrays() {
        // @method <init>()V
        // @declaration a constructor of `PartialArrays`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int[][] primitive(int arg0) {
        // jarde: not recovered: the recovery run for `primitive(I)[[I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method primitive(I)[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 4
        // the value at BCI 4 comes from an Other at BCI 1, which produces no expression this subset writes
    }

    public static java.lang.String[][] reference(int arg0) {
        // jarde: not recovered: the recovery run for `reference(I)[[Ljava/lang/String;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method reference(I)[[Ljava/lang/String;
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 4
        // the value at BCI 4 comes from an Other at BCI 1, which produces no expression this subset writes
    }

    public static int[][][] prefix(int arg0, int arg1) {
        // jarde: not recovered: the recovery run for `prefix(II)[[[I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method prefix(II)[[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        // @bytecode 6
        // the value at BCI 6 comes from an Other at BCI 2, which produces no expression this subset writes
    }

    public static java.lang.Object[][][] referencePrefix(int arg0, int arg1) {
        // jarde: not recovered: the recovery run for `referencePrefix(II)[[[Ljava/lang/Object;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method referencePrefix(II)[[[Ljava/lang/Object;
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        // @bytecode 6
        // the value at BCI 6 comes from an Other at BCI 2, which produces no expression this subset writes
    }

    public static int[][] local(int arg0) {
        // jarde: not recovered: the recovery run for `local(I)[[I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method local(I)[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 4
        // the value at BCI 4 comes from an Other at BCI 1, which produces no expression this subset writes
        // @bytecode 5 6
        // the statement at BCI 6 reads `local1`, and no statement of this body declared that local: the write that would have declared it was refused, so its name cannot be read here (P3 2b.2)
    }

    public static java.lang.String overload(int arg0) {
        // jarde: not recovered: the recovery run for `overload(I)Ljava/lang/String;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method overload(I)Ljava/lang/String;
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 4
        // the dependency chain from BCI 4 to final consumer 7 is not bounded
        // @bytecode 7 4
        // the value at BCI 7 was produced by a saved declaration this run could not commit
    }

    public static void field(int arg0) {
        // @method field(I)V
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 4
        // the value at BCI 4 comes from an Other at BCI 1, which produces no expression this subset writes
        return;
    }

    public static int length(int arg0) {
        // jarde: not recovered: the recovery run for `length(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method length(I)I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 4
        // the dependency chain from BCI 4 to final consumer 5 is not bounded
        // @bytecode 5 4
        // the value at BCI 5 was produced by a saved declaration this run could not commit
    }

    public static int[][][] effects(int arg0, int arg1) {
        // @method effects(II)[[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        PartialArrayEffects.dim(1, arg0);
        PartialArrayEffects.dim(2, arg1);
        // @bytecode 10
        // the instruction at BCI 10 is not part of the provable subset
        // @bytecode 14
        // the value at BCI 14 comes from an Other at BCI 10, which produces no expression this subset writes
    }

    public static int[][] complete(int arg0, int arg1) {
        // @method complete(II)[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new int[arg0][arg1];
    }
}
