// jarde: presentation of `PartialArrays` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class PartialArrays extends java.lang.Object {
    public static int[][] held;

    public PartialArrays() {
        // @method <init>()V
        // @declaration a constructor of `PartialArrays`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int[][] primitive(int arg0) {
        // @method primitive(I)[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new int[arg0][];
    }

    public static java.lang.String[][] reference(int arg0) {
        // @method reference(I)[[Ljava/lang/String;
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new java.lang.String[arg0][];
    }

    public static int[][][] prefix(int arg0, int arg1) {
        // @method prefix(II)[[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new int[arg0][arg1][];
    }

    public static java.lang.Object[][][] referencePrefix(int arg0, int arg1) {
        // @method referencePrefix(II)[[[Ljava/lang/Object;
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new java.lang.Object[arg0][arg1][];
    }

    public static int[][] local(int arg0) {
        // @method local(I)[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int[][] local1 = new int[arg0][];
        return local1;
    }

    public static void field(int arg0) {
        // @method field(I)V
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        PartialArrays.held = new int[arg0][];
        return;
    }

    public static int length(int arg0) {
        // @method length(I)I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new int[arg0][].length;
    }

    public static int[][][] effects(int arg0, int arg1) {
        // @method effects(II)[[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new int[PartialArrayEffects.dim(1, arg0)][PartialArrayEffects.dim(2, arg1)][];
    }

    public static int[][] complete(int arg0, int arg1) {
        // @method complete(II)[[I
        // @declaration a static method of `PartialArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new int[arg0][arg1];
    }
}
