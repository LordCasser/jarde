// jarde: presentation of `DiscardedConversion` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class DiscardedConversion extends java.lang.Object {
    public DiscardedConversion() {
        // @method <init>()V
        // @declaration a constructor of `DiscardedConversion`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static void drop(int arg0) {
        // @method drop(I)V
        // @declaration a static method of `DiscardedConversion`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the instruction at BCI 1 is not part of the provable subset
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        return;
    }
}
