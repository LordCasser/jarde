// jarde: presentation of `NullCatchOnly` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public final class NullCatchOnly extends java.lang.Object implements java.lang.AutoCloseable {
    private static int catches;

    public NullCatchOnly() {
        // @method <init>()V
        // @declaration a constructor of `NullCatchOnly`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int catches() {
        // @method catches()I
        // @declaration a static method of `NullCatchOnly`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return NullCatchOnly.catches;
    }

    public static void ordinary() {
        // @method ordinary()V
        // @declaration a static method of `NullCatchOnly`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        Object local0 = null;
        try {
            local0.toString();
        } catch (java.lang.RuntimeException local1) {
            NullCatchOnly.catches = NullCatchOnly.catches + 1;
        }
        return;
    }

    public void close() {
        // @method close()V
        // @declaration an instance method of `NullCatchOnly`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        return;
    }
}
