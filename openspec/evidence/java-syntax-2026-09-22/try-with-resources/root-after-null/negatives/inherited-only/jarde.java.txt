// jarde: presentation of `NullResourceInheritedOnly` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public final class NullResourceInheritedOnly extends NullResourceBase {
    private static int bodyCalls;

    public NullResourceInheritedOnly() {
        // @method <init>()V
        // @declaration a constructor of `NullResourceInheritedOnly`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public void close() {
        // @method close()V
        // @declaration an instance method of `NullResourceInheritedOnly`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        return;
    }

    public static void useNullResource() {
        // @method useNullResource()V
        // @declaration a static method of `NullResourceInheritedOnly`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 14 21 26 33 39 15
        // the null resource at BCI 1 cannot be typed as the current class because its class header does not directly declare `java/lang/AutoCloseable`
        return;
    }

    public static int bodyCalls() {
        // @method bodyCalls()I
        // @declaration a static method of `NullResourceInheritedOnly`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return NullResourceInheritedOnly.bodyCalls;
    }
}
