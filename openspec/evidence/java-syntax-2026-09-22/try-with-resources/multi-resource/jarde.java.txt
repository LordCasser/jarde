// jarde: presentation of `TwrMultiAudit` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public final class TwrMultiAudit extends java.lang.Object implements java.lang.AutoCloseable {
    private static int closes;

    private static int trace;

    private final int id;

    private final java.lang.Exception closeFailure;

    private TwrMultiAudit(int arg1, java.lang.Exception arg2) {
        // @method <init>(ILjava/lang/Exception;)V
        // @declaration a constructor of `TwrMultiAudit`, member flags 0x0002
        // recovered from bytecode; presentation is not claimed to compile
        super();
        this.id = arg1;
        this.closeFailure = arg2;
        return;
    }

    private static TwrMultiAudit open(int arg0, java.lang.Exception arg1) {
        // @method open(ILjava/lang/Exception;)LTwrMultiAudit;
        // @declaration a static method of `TwrMultiAudit`, member flags 0x000a
        // recovered from bytecode; presentation is not claimed to compile
        return new TwrMultiAudit(arg0, arg1);
    }

    private static void failBody(java.lang.Exception arg0) throws java.lang.Exception {
        // @method failBody(Ljava/lang/Exception;)V
        // @declaration a static method of `TwrMultiAudit`, member flags 0x000a
        // recovered from bytecode; presentation is not claimed to compile
        throw arg0;
    }

    private static void event(int arg0) {
        // @method event(I)V
        // @declaration a static method of `TwrMultiAudit`, member flags 0x000a
        // recovered from bytecode; presentation is not claimed to compile
        TwrMultiAudit.trace = TwrMultiAudit.trace * 10 + arg0;
        return;
    }

    public static void reset() {
        // @method reset()V
        // @declaration a static method of `TwrMultiAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        TwrMultiAudit.closes = 0;
        TwrMultiAudit.trace = 0;
        return;
    }

    public static int closes() {
        // @method closes()I
        // @declaration a static method of `TwrMultiAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return TwrMultiAudit.closes;
    }

    public static int trace() {
        // @method trace()I
        // @declaration a static method of `TwrMultiAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return TwrMultiAudit.trace;
    }

    public static void bodyAndThreeFailingCloses(java.lang.Exception arg0, java.lang.Exception arg1, java.lang.Exception arg2, java.lang.Exception arg3) throws java.lang.Exception {
        // @method bodyAndThreeFailingCloses(Ljava/lang/Exception;Ljava/lang/Exception;Ljava/lang/Exception;Ljava/lang/Exception;)V
        // @declaration a static method of `TwrMultiAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        try (TwrMultiAudit local4 = open(1, arg1); TwrMultiAudit local5 = open(2, arg2); TwrMultiAudit local6 = open(3, arg3)) {
            event(9);
            failBody(arg0);
        }
        return;
    }

    public void close() throws java.lang.Exception {
        // @method close()V
        // @declaration an instance method of `TwrMultiAudit`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        TwrMultiAudit.closes = TwrMultiAudit.closes + 1;
        event(this.id);
        if (this.closeFailure != null) {
            throw this.closeFailure;
        } else {
            return;
        }
    }
}
