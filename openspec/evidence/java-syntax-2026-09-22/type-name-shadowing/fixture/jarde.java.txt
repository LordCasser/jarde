// jarde: presentation of `TypeQualifierProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class TypeQualifierProbe extends java.lang.Object {
    public TypeQualifierProbe() {
        // @method <init>()V
        // @declaration a constructor of `TypeQualifierProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int ownPick(int arg0) {
        // @method ownPick(I)I
        // @declaration a static method of `TypeQualifierProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 + 50;
    }

    public static int ownCall() {
        // @method ownCall()I
        // @declaration a static method of `TypeQualifierProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return ownPick(1);
    }

    public static int invoke(ShadowOther arg0) {
        // @method invoke(LShadowOther;)I
        // @declaration a static method of `TypeQualifierProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0.pick(1) + arg0_2.pick(1);
    }

    public static int read(ShadowOther arg0) {
        // @method read(LShadowOther;)I
        // @declaration a static method of `TypeQualifierProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0.value + arg0_2.value;
    }

    public static void write(ShadowOther arg0, int arg1) {
        // @method write(LShadowOther;I)V
        // @declaration a static method of `TypeQualifierProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        arg0.value = arg1;
        arg0_2.value = arg1 + 1;
        return;
    }
}
