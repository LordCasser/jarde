// jarde: presentation of `MethodQualifierProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class MethodQualifierProbe extends java.lang.Object {
    public MethodQualifierProbe() {
        // @method <init>()V
        // @declaration a constructor of `MethodQualifierProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static java.util.function.Function ref(MethodQualifierOther arg0) {
        // @method ref(LMethodQualifierOther;)Ljava/util/function/Function;
        // @declaration a static method of `MethodQualifierProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0::pick;
    }
}
