// jarde: presentation of `FieldPrefixProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class FieldPrefixProbe extends java.lang.Object {
    public static java.lang.Object java;

    public FieldPrefixProbe() {
        // @method <init>()V
        // @declaration a constructor of `FieldPrefixProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int abs(int arg0) {
        // @method abs(I)I
        // @declaration a static method of `FieldPrefixProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return java.lang.Math.abs(arg0);
    }
}
