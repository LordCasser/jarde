// jarde: presentation of `ArrayInitializerProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class ArrayInitializerProbe extends java.lang.Object {
    private static final java.lang.StringBuilder trace;

    public ArrayInitializerProbe() {
        // @method <init>()V
        // @declaration a constructor of `ArrayInitializerProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    private static void append(char arg0) {
        // @method append(C)V
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x000a
        // recovered from bytecode; presentation is not claimed to compile
        ArrayInitializerProbe.trace.append(arg0);
        return;
    }

    private static int intElement(int arg0, int arg1) {
        // @method intElement(II)I
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x000a
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 4
        // the instruction at BCI 4 is not part of the provable subset
        // @bytecode 5
        // the value at BCI 5 comes from an Other at BCI 4, which produces no expression this subset writes
        return arg1 * 10 + 10 / (arg1 - arg0);
    }

    private static java.lang.String stringElement(int arg0, int arg1) {
        // @method stringElement(II)Ljava/lang/String;
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x000a
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 4
        // the instruction at BCI 4 is not part of the provable subset
        // @bytecode 5
        // the value at BCI 5 comes from an Other at BCI 4, which produces no expression this subset writes
        return "s" + arg1 + 10 / (arg1 - arg0);
    }

    public static int[] literalInts() {
        // jarde: not recovered: the recovery run for `literalInts()[I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method literalInts()[I
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the array instruction at BCI 1 produces a value nothing in this body reads, so the instruction the bytecode runs has no place in the text
        // @bytecode 3 1
        // the instruction at BCI 3 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 6 1
        // the value at BCI 6 comes from an Duplicate at BCI 3, which produces no expression this subset writes
        // @bytecode 7 1
        // the instruction at BCI 7 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 10 1
        // the value at BCI 10 comes from an Duplicate at BCI 7, which produces no expression this subset writes
        // @bytecode 11 1
        // the instruction at BCI 11 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 14 1
        // the value at BCI 14 comes from an Duplicate at BCI 11, which produces no expression this subset writes
        // @bytecode 15 1
        // the instruction at BCI 15 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 19 1
        // the value at BCI 19 comes from an Duplicate at BCI 15, which produces no expression this subset writes
        // @bytecode 20 1
        // the value at BCI 20 comes from an Duplicate at BCI 15, which produces no expression this subset writes
    }

    public static int[] emptyInts() {
        // @method emptyInts()[I
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new int[0];
    }

    public static java.lang.String[] literalStrings() {
        // jarde: not recovered: the recovery run for `literalStrings()[Ljava/lang/String;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method literalStrings()[Ljava/lang/String;
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the array instruction at BCI 1 produces a value nothing in this body reads, so the instruction the bytecode runs has no place in the text
        // @bytecode 4 1
        // the instruction at BCI 4 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 8 1
        // the value at BCI 8 comes from an Duplicate at BCI 4, which produces no expression this subset writes
        // @bytecode 9 1
        // the instruction at BCI 9 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 12 1
        // the value at BCI 12 comes from an Duplicate at BCI 9, which produces no expression this subset writes
        // @bytecode 13 1
        // the instruction at BCI 13 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 17 1
        // the value at BCI 17 comes from an Duplicate at BCI 13, which produces no expression this subset writes
        // @bytecode 18 1
        // the value at BCI 18 comes from an Duplicate at BCI 13, which produces no expression this subset writes
    }

    public static java.lang.String[] emptyStrings() {
        // @method emptyStrings()[Ljava/lang/String;
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new java.lang.String[0];
    }

    public static int[] effectfulInts(int arg0) {
        // jarde: not recovered: the recovery run for `effectfulInts(I)[I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method effectfulInts(I)[I
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the array instruction at BCI 1 produces a value nothing in this body reads, so the instruction the bytecode runs has no place in the text
        // @bytecode 3 1
        // the instruction at BCI 3 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 10 1 7
        // the value at BCI 10 comes from an Duplicate at BCI 3, which produces no expression this subset writes
        // @bytecode 11 1
        // the instruction at BCI 11 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 18 1 15
        // the value at BCI 18 comes from an Duplicate at BCI 11, which produces no expression this subset writes
        // @bytecode 19 1
        // the instruction at BCI 19 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 26 1 23
        // the value at BCI 26 comes from an Duplicate at BCI 19, which produces no expression this subset writes
        // @bytecode 27 1
        // the value at BCI 27 comes from an Duplicate at BCI 19, which produces no expression this subset writes
    }

    public static java.lang.String[] effectfulStrings(int arg0) {
        // jarde: not recovered: the recovery run for `effectfulStrings(I)[Ljava/lang/String;` produced no statement (explanation only); the artifact's own comment lines are below
        // @method effectfulStrings(I)[Ljava/lang/String;
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 1
        // the array instruction at BCI 1 produces a value nothing in this body reads, so the instruction the bytecode runs has no place in the text
        // @bytecode 4 1
        // the instruction at BCI 4 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 11 1 8
        // the value at BCI 11 comes from an Duplicate at BCI 4, which produces no expression this subset writes
        // @bytecode 12 1
        // the instruction at BCI 12 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 19 1 16
        // the value at BCI 19 comes from an Duplicate at BCI 12, which produces no expression this subset writes
        // @bytecode 20 1
        // the instruction at BCI 20 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 27 1 24
        // the value at BCI 27 comes from an Duplicate at BCI 20, which produces no expression this subset writes
        // @bytecode 28 1
        // the value at BCI 28 comes from an Duplicate at BCI 20, which produces no expression this subset writes
    }

    public static java.lang.String trace() {
        // @method trace()Ljava/lang/String;
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return ArrayInitializerProbe.trace.toString();
    }

    public static void reset() {
        // @method reset()V
        // @declaration a static method of `ArrayInitializerProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        ArrayInitializerProbe.trace.setLength(0);
        return;
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `ArrayInitializerProbe`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        trace = new java.lang.StringBuilder();
    }
}
