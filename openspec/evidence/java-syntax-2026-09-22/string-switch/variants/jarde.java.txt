// jarde: presentation of `StringSwitchVariants` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class StringSwitchVariants extends java.lang.Object {
    static int calls;

    public StringSwitchVariants() {
        // @method <init>()V
        // @declaration a constructor of `StringSwitchVariants`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    static java.lang.String read(java.lang.String arg0) {
        // @method read(Ljava/lang/String;)Ljava/lang/String;
        // @declaration a static method of `StringSwitchVariants`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        StringSwitchVariants.calls = StringSwitchVariants.calls + 1;
        if ("!".equals((java.lang.Object) arg0)) {
            throw new java.lang.IllegalStateException("selector");
        } else {
            return arg0;
        }
    }

    public static int choose(java.lang.String arg0) {
        // @method choose(Ljava/lang/String;)I
        // @declaration a static method of `StringSwitchVariants`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        java.lang.String local1 = arg0;
        int local2 = -1;
        switch (local1.hashCode()) {
            case 0:
                if (local1.equals((java.lang.Object) "")) {
                    local2 = 4;
                }
                break;
            case 120:
                if (local1.equals((java.lang.Object) "x")) {
                    local2 = 2;
                }
                break;
            case 121:
                if (local1.equals((java.lang.Object) "y")) {
                    local2 = 3;
                }
                break;
            case 2112:
                if (local1.equals((java.lang.Object) "BB")) {
                    local2 = 1;
                } else {
                    if (local1.equals((java.lang.Object) "Aa")) {
                        local2 = 0;
                    }
                }
                break;
            case 38634:
                if (local1.equals((java.lang.Object) "雪")) {
                    local2 = 5;
                }
                break;
        }
        // @bytecode 141 180 183 192
        // two switch arms of the block at BCI 141 claim the same block, so one case falls through into another case's code
        // @bytecode 192
        // block at BCI 192 can be re-entered and belongs to no loop this subset proves
        // @bytecode 199 201 204
        // 3 live block(s) are reachable only through edges the normal-flow view leaves out: [199, 201, 204]
    }

    public static int once(java.lang.String arg0) {
        // @method once(Ljava/lang/String;)I
        // @declaration a static method of `StringSwitchVariants`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return choose((java.lang.String) read(arg0));
    }
}
