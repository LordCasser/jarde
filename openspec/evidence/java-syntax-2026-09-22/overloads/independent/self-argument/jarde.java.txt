// jarde: presentation of `SelfArgument` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class SelfArgument extends java.lang.Object {
    public SelfArgument() {
        // @method <init>()V
        // @declaration a constructor of `SelfArgument`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int pick(java.lang.Object arg0) {
        // @method pick(Ljava/lang/Object;)I
        // @declaration a static method of `SelfArgument`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return 1;
    }

    public static int pick(SelfArgument arg0) {
        // @method pick(LSelfArgument;)I
        // @declaration a static method of `SelfArgument`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return 2;
    }

    public int run() {
        // jarde: not recovered: the recovery run for `run()I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method run()I
        // @declaration an instance method of `SelfArgument`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 4 1
        // the parameter 0 of the invocation at BCI 1 is declared `java.lang.Object` has no reference type fact for the argument
    }
}
