// jarde: presentation of `CompoundProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public final class CompoundProbe extends java.lang.Object {
    public static int calls;

    public static int selects;

    public static int[] data;

    public static CompoundBox box;

    public static boolean nullBox;

    public static boolean badIndex;

    public CompoundProbe() {
        // @method <init>()V
        // @declaration a constructor of `CompoundProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static void reset() {
        // @method reset()V
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        CompoundProbe.calls = 0;
        CompoundProbe.selects = 0;
        CompoundProbe.data = new int[1];
        CompoundProbe.data[0] = 10;
        CompoundProbe.box = new CompoundBox(7);
        CompoundProbe.nullBox = false;
        CompoundProbe.badIndex = false;
        return;
    }

    public static int rhs(int arg0) {
        // @method rhs(I)I
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        CompoundProbe.calls = CompoundProbe.calls + 1;
        return arg0;
    }

    public static CompoundBox receiver() {
        // @method receiver()LCompoundBox;
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        CompoundProbe.selects = CompoundProbe.selects + 1;
        if (CompoundProbe.nullBox) {
            return null;
        } else {
            return CompoundProbe.box;
        }
    }

    public static int index() {
        // @method index()I
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        CompoundProbe.selects = CompoundProbe.selects + 1;
        if (CompoundProbe.badIndex) {
            return 1;
        } else {
            return 0;
        }
    }

    public static int local(int arg0) {
        // @method local(I)I
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        arg0 = arg0 + rhs(3);
        return arg0;
    }

    public static int field() {
        // @method field()I
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        receiver();
        // @bytecode 3
        // the instruction at BCI 3 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 12 4 8
        // the value at BCI 12 comes from an Duplicate at BCI 3, which produces no expression this subset writes
        return CompoundProbe.box.value;
    }

    public static int array() {
        // @method array()I
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        index();
        // @bytecode 6
        // the instruction at BCI 6 is not part of the provable subset
        // @bytecode 0 7
        // the dependency chain from BCI 7 to final consumer 13 is not bounded
        // @bytecode 9
        // the dependency chain from BCI 9 to final consumer 13 is not bounded
        // @bytecode 13 0 7 9
        // the value at BCI 13 comes from an Other at BCI 6, which produces no expression this subset writes
        return CompoundProbe.data[0];
    }

    public static int postField() {
        // @method postField()I
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        receiver();
        // @bytecode 3
        // the instruction at BCI 3 belongs to no shape this run verified: an allocation, a copy or a cast is presented only where a rule proved what it builds
        // @bytecode 7
        // the instruction at BCI 7 is not part of the provable subset
        // @bytecode 10 4
        // the value at BCI 10 comes from an Other at BCI 7, which produces no expression this subset writes
        // @bytecode 13 4
        // the value at BCI 13 comes from an Other at BCI 7, which produces no expression this subset writes
    }

    public static int postArray() {
        // @method postArray()I
        // @declaration a static method of `CompoundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        index();
        // @bytecode 6
        // the instruction at BCI 6 is not part of the provable subset
        // @bytecode 0 7
        // the dependency chain from BCI 7 to final consumer 8 is not bounded
        // @bytecode 8
        // the instruction at BCI 8 is not part of the provable subset
        // @bytecode 11 0 7
        // the value at BCI 11 comes from an Other at BCI 8, which produces no expression this subset writes
        // @bytecode 12 0 7
        // the value at BCI 12 comes from an Other at BCI 8, which produces no expression this subset writes
    }
}
