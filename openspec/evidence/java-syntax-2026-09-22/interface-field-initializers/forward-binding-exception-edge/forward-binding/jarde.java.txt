// jarde: presentation of `ForwardProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public interface ForwardProbe {
    public static final int LATE;

    public static final int EARLY;

    public static java.lang.String observe() {
        // @method observe()Ljava/lang/String;
        // @declaration an interface's static method of `ForwardProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return new java.lang.StringBuilder().append(BoundaryEffects.trace).append("|").append(ForwardProbe.EARLY).append("|").append(ForwardProbe.LATE).toString();
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `ForwardProbe`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        ForwardProbe.EARLY = ForwardProbe.LATE;
        ForwardProbe.LATE = BoundaryEffects.value();
    }
}
