// jarde: presentation of `DeferredScope` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class DeferredScope extends java.lang.Object {
    public DeferredScope() {
        // @method <init>()V
        // @declaration a constructor of `DeferredScope`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int direct() {
        // jarde: not recovered: the recovery run for `direct()I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method direct()I
        // @declaration a static method of `DeferredScope`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0
        // BCI 0: the resource's own initialisation is not one statement of this block whose value lands in a slot: writing it in the header would move or drop an effect
        // @bytecode 7
        // 1 live block(s) are reachable only through edges the normal-flow view leaves out: [7]
    }

    public static void keepPool() {
        // @method keepPool()V
        // @declaration a static method of `DeferredScope`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        ScopeSupport.mark();
        return;
    }
}
