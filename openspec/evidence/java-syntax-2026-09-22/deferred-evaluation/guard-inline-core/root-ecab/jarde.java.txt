// jarde: presentation of `GuardInlineCore` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class GuardInlineCore extends java.lang.Object {
    public GuardInlineCore() {
        // @method <init>()V
        // @declaration a constructor of `GuardInlineCore`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static void locked() {
        // @method locked()V
        // @declaration a static method of `GuardInlineCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        synchronized (GuardEffects.lock()) {
            GuardEffects.mark();
        }
        return;
    }

    public static int returned() {
        // @method returned()I
        // @declaration a static method of `GuardInlineCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        synchronized (GuardEffects.shared) {
            int saved0 = GuardEffects.value();
            return saved0;
        }
    }
}
