// jarde: presentation of `SwitchDeferredCore` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class SwitchDeferredCore extends java.lang.Object {
    public SwitchDeferredCore() {
        // @method <init>()V
        // @declaration a constructor of `SwitchDeferredCore`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int run(int arg0) {
        // jarde: not recovered: the recovery run for `run(I)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method run(I)I
        // @declaration a static method of `SwitchDeferredCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0 20 29 1
        // the value at BCI 26 was produced by a saved declaration this run could not commit
        // @bytecode 35
        // the value at BCI 35 is the entry state of stack depth 0, which no instruction produced
    }
}
