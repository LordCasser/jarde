// jarde: presentation of `ConstructionOrder` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class ConstructionOrder extends java.lang.Object {
    public ConstructionOrder() {
        // @method <init>()V
        // @declaration a constructor of `ConstructionOrder`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static OrderValue direct() {
        // @method direct()LOrderValue;
        // @declaration a static method of `ConstructionOrder`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        OrderValue saved0 = new OrderValue();
        OrderEffects.mark();
        return saved0;
    }

    public static void keepPool() {
        // @method keepPool()V
        // @declaration a static method of `ConstructionOrder`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        OrderEffects.mark();
        return;
    }
}
