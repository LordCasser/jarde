// jarde: presentation of `DeferredChecks` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class DeferredChecks extends java.lang.Object {
    public DeferredChecks() {
        // @method <init>()V
        // @declaration a constructor of `DeferredChecks`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int divI(int arg0, int arg1) {
        // @method divI(II)I
        // @declaration a static method of `DeferredChecks`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int saved0 = arg0 / arg1;
        CheckSupport.mark();
        return saved0;
    }

    public static int remI(int arg0, int arg1) {
        // @method remI(II)I
        // @declaration a static method of `DeferredChecks`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int saved0 = arg0 % arg1;
        CheckSupport.mark();
        return saved0;
    }

    public static long divL(long arg0, long arg2) {
        // @method divL(JJ)J
        // @declaration a static method of `DeferredChecks`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        long saved0 = arg0 / arg2;
        CheckSupport.mark();
        return saved0;
    }

    public static long remL(long arg0, long arg2) {
        // @method remL(JJ)J
        // @declaration a static method of `DeferredChecks`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        long saved0 = arg0 % arg2;
        CheckSupport.mark();
        return saved0;
    }

    public static int field(CheckSupport arg0) {
        // @method field(LCheckSupport;)I
        // @declaration a static method of `DeferredChecks`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int saved0 = arg0.value;
        CheckSupport.mark();
        return saved0;
    }

    public static int length(int[] arg0) {
        // @method length([I)I
        // @declaration a static method of `DeferredChecks`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int saved0 = arg0.length;
        CheckSupport.mark();
        return saved0;
    }

    public static void keepPool() {
        // @method keepPool()V
        // @declaration a static method of `DeferredChecks`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        CheckSupport.mark();
        return;
    }
}
