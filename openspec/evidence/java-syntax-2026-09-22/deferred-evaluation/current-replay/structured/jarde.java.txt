// jarde: presentation of `DeferredStructured` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class DeferredStructured extends java.lang.Object {
    public DeferredStructured() {
        // @method <init>()V
        // @declaration a constructor of `DeferredStructured`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int branch(boolean arg0) {
        // @method branch(Z)I
        // @declaration a static method of `DeferredStructured`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        if (arg0) {
            int saved0 = StructuredSupport.value();
            StructuredSupport.mark();
            return saved0;
        } else {
            return 3;
        }
    }

    public static int prefix() {
        // @method prefix()I
        // @declaration a static method of `DeferredStructured`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        boolean saved0 = StructuredSupport.flag();
        StructuredSupport.mark();
        if (saved0) {
            return 7;
        } else {
            return 9;
        }
    }

    public static void keepPool() {
        // @method keepPool()V
        // @declaration a static method of `DeferredStructured`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        StructuredSupport.mark();
        return;
    }
}
