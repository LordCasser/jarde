// jarde: presentation of `DeferredArrays` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class DeferredArrays extends java.lang.Object {
    public DeferredArrays() {
        // @method <init>()V
        // @declaration a constructor of `DeferredArrays`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int[] ints(int arg0) {
        // @method ints(I)[I
        // @declaration a static method of `DeferredArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int[] saved0 = new int[arg0];
        ArraySupport.mark();
        return saved0;
    }

    public static java.lang.String[] refs(int arg0) {
        // @method refs(I)[Ljava/lang/String;
        // @declaration a static method of `DeferredArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        java.lang.String[] saved0 = new java.lang.String[arg0];
        ArraySupport.mark();
        return saved0;
    }

    public static int[][] multi(int arg0, int arg1) {
        // @method multi(II)[[I
        // @declaration a static method of `DeferredArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int[][] saved0 = new int[arg0][arg1];
        ArraySupport.mark();
        return saved0;
    }

    public static void keepPool() {
        // @method keepPool()V
        // @declaration a static method of `DeferredArrays`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        ArraySupport.mark();
        return;
    }
}
