// jarde: presentation of `DeferredComposite` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class DeferredComposite extends java.lang.Object {
    public DeferredComposite() {
        // @method <init>()V
        // @declaration a constructor of `DeferredComposite`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int nested() {
        // @method nested()I
        // @declaration a static method of `DeferredComposite`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        DeferredSupport.mark();
        int saved0 = DeferredSupport.take(DeferredSupport.value());
        DeferredSupport.mark();
        return saved0;
    }

    public static void keepPool() {
        // @method keepPool()V
        // @declaration a static method of `DeferredComposite`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        DeferredSupport.mark();
        return;
    }
}
