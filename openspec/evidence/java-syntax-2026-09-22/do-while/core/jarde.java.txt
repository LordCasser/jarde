// jarde: presentation of `DoWhileCore` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class DoWhileCore extends java.lang.Object {
    private static int trace;

    public DoWhileCore() {
        // @method <init>()V
        // @declaration a constructor of `DoWhileCore`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static void reset() {
        // @method reset()V
        // @declaration a static method of `DoWhileCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        DoWhileCore.trace = 0;
        return;
    }

    public static int trace() {
        // @method trace()I
        // @declaration a static method of `DoWhileCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return DoWhileCore.trace;
    }

    public static int basic(int arg0) {
        // @method basic(I)I
        // @declaration a static method of `DoWhileCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int local1;
        local1 = 0;
        do {
            local1 = local1 + 1;
            DoWhileCore.trace = DoWhileCore.trace * 10 + local1;
        } while (local1 < arg0);
        return DoWhileCore.trace;
    }

    public static int withContinue(int arg0) {
        // @method withContinue(I)I
        // @declaration a static method of `DoWhileCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int local1 = 0;
        // @bytecode 2
        // the loop whose header is the block at BCI 2 has a test, an exit or a latch this subset does not prove
        // @bytecode 2 10 13 24 29
        // 5 live block(s) are reachable only through edges the normal-flow view leaves out: [2, 10, 13, 24, 29]
    }

    public static int withBreak(int arg0) {
        // @method withBreak(I)I
        // @declaration a static method of `DoWhileCore`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int local1 = 0;
        // @bytecode 2
        // the loop@1 rule did not claim the block at BCI 2: it requires a test block whose every instruction is part of a value expression, and the instruction at BCI 2 is not part of one, so presenting the structure would have moved that effect out of the shape it decides
        // @bytecode 2 10 13 29
        // 4 live block(s) are reachable only through edges the normal-flow view leaves out: [2, 10, 13, 29]
    }
}
