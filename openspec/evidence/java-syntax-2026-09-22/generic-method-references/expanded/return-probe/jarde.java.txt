// jarde: presentation of `ReturnProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class ReturnProbe extends java.lang.Object {
    public ReturnProbe() {
        // @method <init>()V
        // @declaration a constructor of `ReturnProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static java.util.function.Supplier genericString() {
        // @method genericString()Ljava/util/function/Supplier;
        // @declaration a static method of `ReturnProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return ReturnHelper::value;
    }
}
