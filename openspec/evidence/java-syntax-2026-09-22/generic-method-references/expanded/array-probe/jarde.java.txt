// jarde: presentation of `ArrayProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class ArrayProbe extends java.lang.Object {
    public ArrayProbe() {
        // @method <init>()V
        // @declaration a constructor of `ArrayProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int pick(java.lang.Object[] arg0) {
        // @method pick([Ljava/lang/Object;)I
        // @declaration a static method of `ArrayProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return 30;
    }

    public static int pick(java.lang.String[] arg0) {
        // @method pick([Ljava/lang/String;)I
        // @declaration a static method of `ArrayProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return 31;
    }

    public static java.util.function.ToIntFunction strings() {
        // @method strings()Ljava/util/function/ToIntFunction;
        // @declaration a static method of `ArrayProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return ArrayProbe::pick;
    }

    public static java.util.function.ToIntFunction objects() {
        // @method objects()Ljava/util/function/ToIntFunction;
        // @declaration a static method of `ArrayProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return ArrayProbe::pick;
    }
}
