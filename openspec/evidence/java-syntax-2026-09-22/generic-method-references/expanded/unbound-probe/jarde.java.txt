// jarde: presentation of `UnboundProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class UnboundProbe extends java.lang.Object {
    public UnboundProbe() {
        // @method <init>()V
        // @declaration a constructor of `UnboundProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static java.util.function.ToIntBiFunction strings() {
        // @method strings()Ljava/util/function/ToIntBiFunction;
        // @declaration a static method of `UnboundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return (UnboundHelper p0, java.lang.String p1) -> p0.pick(p1);
    }

    public static java.util.function.ToIntBiFunction arrays() {
        // @method arrays()Ljava/util/function/ToIntBiFunction;
        // @declaration a static method of `UnboundProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return (UnboundHelper p0, java.lang.String[] p1) -> p0.pick(p1);
    }
}
