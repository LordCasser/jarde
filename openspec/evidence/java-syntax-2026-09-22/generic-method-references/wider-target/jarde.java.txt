// jarde: presentation of `WiderTarget` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class WiderTarget extends java.lang.Object {
    public static int calls;

    public WiderTarget() {
        // @method <init>()V
        // @declaration a constructor of `WiderTarget`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int accept(java.lang.Object arg0) {
        // @method accept(Ljava/lang/Object;)I
        // @declaration a static method of `WiderTarget`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        WiderTarget.calls = WiderTarget.calls + 1;
        return 7;
    }

    public static java.util.function.ToIntFunction strings() {
        // @method strings()Ljava/util/function/ToIntFunction;
        // @declaration a static method of `WiderTarget`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return WiderTarget::accept;
    }
}
