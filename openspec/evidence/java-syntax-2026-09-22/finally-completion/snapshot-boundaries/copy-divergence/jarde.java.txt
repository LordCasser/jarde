// jarde: presentation of `CleanupBoundaries` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class CleanupBoundaries extends java.lang.Object {
    public static int value;

    public static int trace;

    public static boolean failAtOne;

    public static final java.lang.RuntimeException TRY_FAILURE;

    public static final java.lang.RuntimeException CLEANUP_FAILURE;

    public CleanupBoundaries() {
        // @method <init>()V
        // @declaration a constructor of `CleanupBoundaries`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    private static int mark(int arg0) {
        // @method mark(I)I
        // @declaration a static method of `CleanupBoundaries`, member flags 0x000a
        // recovered from bytecode; presentation is not claimed to compile
        CleanupBoundaries.trace = CleanupBoundaries.trace * 10 + arg0;
        if (arg0 == 1) {
            if (CleanupBoundaries.failAtOne) {
                throw CleanupBoundaries.TRY_FAILURE;
            }
        }
        return arg0;
    }

    public static int snapshotReturn() {
        // jarde: not recovered: the recovery run for `snapshotReturn()I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method snapshotReturn()I
        // @declaration a static method of `CleanupBoundaries`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0
        // BCI 26: the exceptional path repeats code the normal path also runs — the `finally` copy javac emits for a `finally` clause; merging the copies into one `finally` restates the source only if they are provably equal, which this build does not prove
        // @bytecode 26
        // 1 live block(s) are reachable only through edges the normal-flow view leaves out: [26]
    }

    public static int cleanupThrowsOverReturn() {
        // jarde: not recovered: the recovery run for `cleanupThrowsOverReturn()I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method cleanupThrowsOverReturn()I
        // @declaration a static method of `CleanupBoundaries`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0
        // block at BCI 0 leaves through exception handler 0: a handler's shape is not part of the recoverable subset
        // @bytecode 14
        // 1 live block(s) are reachable only through edges the normal-flow view leaves out: [14]
    }

    public static int cleanupThrowsOverTryThrow() {
        // jarde: not recovered: the recovery run for `cleanupThrowsOverTryThrow()I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method cleanupThrowsOverTryThrow()I
        // @declaration a static method of `CleanupBoundaries`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 0
        // block at BCI 0 leaves through exception handler 0: a handler's shape is not part of the recoverable subset
        // @bytecode 9
        // 1 live block(s) are reachable only through edges the normal-flow view leaves out: [9]
    }

    static {
        // @method <clinit>()V
        // @declaration a static initializer of `CleanupBoundaries`, member flags 0x0008
        // recovered from bytecode; presentation is not claimed to compile
        TRY_FAILURE = new java.lang.IllegalArgumentException("try");
        CLEANUP_FAILURE = new java.lang.IllegalStateException("cleanup");
    }
}
