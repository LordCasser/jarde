# Finally snapshot boundaries

Source-only Java 8 evidence for saved return values, cleanup exceptions overriding pending returns/exceptions, and controlled JVM-valid differences in finally-copy behavior and exception coverage. Read `analysis.md`; replay with `python3 run_audit.py`. JADX output is comparative evidence, not an oracle.
