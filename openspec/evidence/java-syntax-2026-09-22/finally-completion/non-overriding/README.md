# Non-overriding finally fixture

Minimal standalone `try/finally` fixture for normal return and exceptional completion. See `analysis.md` for results and the catch-all boundary. Run `python3 run_audit.py` to reproduce source/JADX/Jarde compilation and execution checks.
