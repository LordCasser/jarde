// jarde: presentation of `BitwiseAudit` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class BitwiseAudit extends java.lang.Object {
    public BitwiseAudit() {
        // @method <init>()V
        // @declaration a constructor of `BitwiseAudit`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static boolean nested(boolean arg0, boolean arg1, boolean arg2) {
        // jarde: not recovered: the recovery run for `nested(ZZZ)Z` produced no statement (explanation only); the artifact's own comment lines are below
        // @method nested(ZZZ)Z
        // @declaration a static method of `BitwiseAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 4
        // the instruction at BCI 4 is not part of the provable subset
        // @bytecode 5
        // the instruction at BCI 5 is not part of the provable subset
        // @bytecode 6
        // the instruction at BCI 6 is not part of the provable subset
        // @bytecode 7
        // the value at BCI 7 comes from an Other at BCI 6, which produces no expression this subset writes
    }

    public static boolean copied(boolean arg0, boolean arg1, boolean arg2) {
        // jarde: not recovered: the recovery run for `copied(ZZZ)Z` produced no statement (explanation only); the artifact's own comment lines are below
        // @method copied(ZZZ)Z
        // @declaration a static method of `BitwiseAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        // @bytecode 3
        // the value at BCI 3 comes from an Other at BCI 2, which produces no expression this subset writes
        // @bytecode 4 5
        // the statement at BCI 5 reads `local3`, and no statement of this body declared that local: the write that would have declared it was refused, so its name cannot be read here (P3 2b.2)
        // @bytecode 10
        // the instruction at BCI 10 is not part of the provable subset
        // @bytecode 11
        // the value at BCI 11 comes from an Other at BCI 10, which produces no expression this subset writes
        // @bytecode 15
        // the value at BCI 15 is returned from a method whose own descriptor returns `Z`, and this layer has no evidence that the value is a boolean (a `0`/`1` literal, a `boolean` parameter's load, the result of a call whose callee descriptor returns `Z`, a claimed field read whose descriptor is `Z`, or a local this body declared `boolean`): the `int` spelling this layer would write is text the member's own signature rejects
    }

    public static boolean hoisted(boolean arg0, boolean arg1, boolean arg2) {
        // @method hoisted(ZZZ)Z
        // @declaration a static method of `BitwiseAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int local3;
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        // @bytecode 3
        // the value at BCI 3 comes from an Other at BCI 2, which produces no expression this subset writes
        if (arg2) {
            // @bytecode 10
            // the instruction at BCI 10 is not part of the provable subset
            // @bytecode 11
            // the value at BCI 11 comes from an Other at BCI 10, which produces no expression this subset writes
        }
        // @bytecode 13
        // the value at BCI 13 is returned from a method whose own descriptor returns `Z`, and this layer has no evidence that the value is a boolean (a `0`/`1` literal, a `boolean` parameter's load, the result of a call whose callee descriptor returns `Z`, a claimed field read whose descriptor is `Z`, or a local this body declared `boolean`): the `int` spelling this layer would write is text the member's own signature rejects
    }

    public static int passed(boolean arg0, boolean arg1, boolean arg2) {
        // jarde: not recovered: the recovery run for `passed(ZZZ)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method passed(ZZZ)I
        // @declaration a static method of `BitwiseAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        // @bytecode 4
        // the instruction at BCI 4 is not part of the provable subset
        // @bytecode 8 5
        // the value at BCI 8 comes from an Other at BCI 4, which produces no expression this subset writes
    }

    public static boolean array(boolean[] arg0) {
        // jarde: not recovered: the recovery run for `array([Z)Z` produced no statement (explanation only); the artifact's own comment lines are below
        // @method array([Z)Z
        // @declaration a static method of `BitwiseAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 2
        // the array instruction at BCI 2 produces a value nothing in this body reads, so the instruction the bytecode runs has no place in the text
        // @bytecode 5
        // the array instruction at BCI 5 produces a value nothing in this body reads, so the instruction the bytecode runs has no place in the text
        // @bytecode 6
        // the instruction at BCI 6 is not part of the provable subset
        // @bytecode 7 2 5
        // the value at BCI 7 comes from an Other at BCI 6, which produces no expression this subset writes
    }

    public static int promoted(byte arg0, char arg1) {
        // jarde: not recovered: the recovery run for `promoted(BC)I` produced no statement (explanation only); the artifact's own comment lines are below
        // @method promoted(BC)I
        // @declaration a static method of `BitwiseAudit`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        // @bytecode 2
        // the instruction at BCI 2 is not part of the provable subset
        // @bytecode 3
        // the value at BCI 3 comes from an Other at BCI 2, which produces no expression this subset writes
    }
}
