// jarde: presentation of `BitwiseProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class BitwiseProbe extends java.lang.Object {
    public BitwiseProbe() {
        // @method <init>()V
        // @declaration a constructor of `BitwiseProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static int andInt(int arg0, int arg1) {
        // @method andInt(II)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 & arg1;
    }

    public static int orInt(int arg0, int arg1) {
        // @method orInt(II)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 | arg1;
    }

    public static int xorInt(int arg0, int arg1) {
        // @method xorInt(II)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 ^ arg1;
    }

    public static long andLong(long arg0, long arg2) {
        // @method andLong(JJ)J
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 & arg2;
    }

    public static long orLong(long arg0, long arg2) {
        // @method orLong(JJ)J
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 | arg2;
    }

    public static long xorLong(long arg0, long arg2) {
        // @method xorLong(JJ)J
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 ^ arg2;
    }

    public static int nested(int arg0, int arg1) {
        // @method nested(II)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return (arg0 | arg1) ^ arg0 & arg1 + 1;
    }

    public static int complement(int arg0) {
        // @method complement(I)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 ^ -1;
    }

    public static long complementLong(long arg0) {
        // @method complementLong(J)J
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 ^ -1L;
    }

    public static boolean andBoolean(boolean arg0, boolean arg1) {
        // @method andBoolean(ZZ)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 & arg1;
    }

    public static boolean orBoolean(boolean arg0, boolean arg1) {
        // @method orBoolean(ZZ)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 | arg1;
    }

    public static boolean xorBoolean(boolean arg0, boolean arg1) {
        // @method xorBoolean(ZZ)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 ^ arg1;
    }

    public static boolean constant(boolean arg0) {
        // @method constant(Z)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 ^ true;
    }

    public static boolean ordered(boolean arg0, boolean arg1) {
        // @method ordered(ZZ)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return BitwiseEffects.left(arg0) & BitwiseEffects.right(arg1);
    }

    public static int branch(boolean arg0, boolean arg1) {
        // @method branch(ZZ)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        if (arg0 | arg1) {
            return 7;
        } else {
            return 9;
        }
    }

    public static boolean nested(boolean arg0, boolean arg1, boolean arg2) {
        // @method nested(ZZZ)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 & (arg1 ^ (arg2 | true));
    }

    public static boolean copied(boolean arg0, boolean arg1, boolean arg2) {
        // @method copied(ZZZ)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        boolean local3 = arg0 | arg1;
        boolean local4 = local3;
        boolean local5 = local4 & arg2;
        return local5;
    }

    public static boolean hoisted(boolean arg0, boolean arg1, boolean arg2) {
        // @method hoisted(ZZZ)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        boolean local3;
        local3 = arg0 & arg1;
        if (arg2) {
            local3 = arg0 | arg1;
        }
        return local3;
    }

    public static int passed(boolean arg0, boolean arg1, boolean arg2) {
        // @method passed(ZZZ)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return BitwiseEffects.accept((arg0 ^ arg1) & arg2);
    }

    public static boolean array(boolean[] arg0) {
        // @method array([Z)Z
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0[0] ^ arg0[1];
    }

    public static int promoted(byte arg0, char arg1) {
        // @method promoted(BC)I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        return arg0 | arg1;
    }

    public static int integerLiteralControl() {
        // @method integerLiteralControl()I
        // @declaration a static method of `BitwiseProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        int local0 = 1;
        int local1 = 0;
        return local0 & local1 ^ local0;
    }
}
