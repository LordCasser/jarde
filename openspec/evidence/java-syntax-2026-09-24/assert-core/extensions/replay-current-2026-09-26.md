# Assertion boundary replay (2026-09-26)

Replay command, run from the repository root:

```sh
python3 openspec/evidence/java-syntax-2026-09-24/assert-core/extensions/replay-current.py \
  --cli /path/to/current/jarde-cli \
  --out /tmp/assert-replay-2026-09-26
```

The script requires an empty output directory, never removes it, and records the full transcripts and source files. The frozen run is in [`replay-output-current-2026-09-26/`](replay-output-current-2026-09-26/). It used CLI built from repository revision `9e62a11b334d440d5e4089e73f329de7274f7bc8` into private Cargo target `/tmp/jarde-assert-task12-target`; CLI SHA-256 is `2d6e745caa3440bfefdc112aaa2a4411af8ac25264a85034f3fc2fdfe15f11d1`. Tool versions: javac/java 23.0.1 and JADX 1.5.6. The nested `assert-core-current-replay/` reruns the frozen core and wrong-owner class through the same CLI; `existing-extension-replay/` repeats the already documented positive and constructor/field controls.

| Physical input | Important bytecode facts | Original / JADX | Current Jarde |
| --- | --- | --- | --- |
| `AssertVariants` positive, SHA-256 `cf37e61be49733e3273ee46567129eb72eb52ba2051a879f212b2eb569fe419b` | `$assertionsDisabled:Z`, flags `0x1018`; `<clinit>` write BCI 13, followed by independent `effects++` at BCI 16–21. No-message constructor `()V` at BCI 17; second no-message constructor at BCI 17 and message `(Object)V` at BCI 41. | Java 8 compilation succeeds. `-Xverify:all -ea`: `init=1;plain-true=12;plain-false=AssertionError:null;multiple-true=12222;multiple-second-false=AssertionError:message;effects=12222223`. `-da`: `init=1;plain-true=1;plain-false=none;multiple-true=1;multiple-second-false=none;effects=1`. | Java 8 compilation succeeds; both outputs match. The class source still spells out field/guards/throws rather than source `assert`. |
| `AssertCore-non01-arms`, SHA-256 `b52d39dd6ae7943d70b510c4925f23faadcc2fcc64c5cb7a2c309ae450de4e2c` | Existing verifier-valid patch changes `<clinit>` BCI 8/12 constants to `2`/`3`; `putstatic $assertionsDisabled:Z` at BCI 13; field flags `0x1018`. Patch and provenance are in [`tests/fixtures/p3-assert-core/patch_non01_arms.py`](../../../../../tests/fixtures/p3-assert-core/patch_non01_arms.py). | Original class verifies. `-ea`: `0|0;0|0`; `-da`: `1|0;bad|2|1`. JADX source fails Java 8 compilation because integer arms cannot be assigned to `boolean`. | Java 8 compilation succeeds, emits explicit `(... ? 2 : 3) % 2 != 0`, and reproduces both outputs. This shape is already covered by the conditional-values evidence; it is cited and replayed here rather than duplicated. |
| `AssertExtraFieldAccess`, SHA-256 `a7ea4f420050e3e803ba91336ad5d670f6c85d4747f9d506843976ef9da6cdbc` | `$assertionsDisabled:Z`, flags `0x1018`; outside-assert `statusProbe()` reads it at BCI 0; `check` increments `extraWrites` at BCI 7–12 before `AssertionError.<init>:()V` at BCI 23. | Both sources compile and match: enabled `extra=AssertionError:null:writes=1;probe=false;ctor=AssertionError:42;`; disabled `probe=true;`. | Jarde complete class compiles and matches both outputs; explicit field read/write and guard are retained. |
| `AssertDuplicateStatusWrite`, SHA-256 `17e988592792a34bd92ebf3bf9539cb6827f6ab5bd6a8d4db53a7ef9a91c0ef5` | `$assertionsDisabled:Z`, flags patched `0x0008` → `0x1018`; `<clinit>` contains a second `putstatic` to that same field at BCI 7 (first is BCI 3). The sole two-byte edit changes only the field flags. | Source compiles before the flags patch; patched class passes `-Xverify:all` and runs (`AssertionError` under both modes because the second write forces false). JADX source retains both writes, but Java 8 compilation fails because a `final` field may be assigned twice. | Jarde source retains both writes and Java 8 compilation likewise rejects the second assignment. See the saved class, source, full javap, and compiler transcripts. This is the same-field duplicate-write control; `extraWrites:I` in the previous row is a separate observable side effect. |
| `AssertDifferentConstructor`, SHA-256 `d94bded16b62e9fa8af1143da531aed3b897bef27dcb9928d1cf75bda16195c5` | `$assertionsDisabled:Z`, flags `0x1018`; `AssertionError.<init>:(I)V` at BCI 16. | Both sources compile and match the same runner outputs above. | Both Jarde classes compile and match; the `(I)V` overload remains explicit. |
| `AssertHandlerBoundary`, SHA-256 `7d84084d8f2002ddce5337c7a5e50ed0e12878db551f10450a888c911e13f162` | `$assertionsDisabled:Z`, flags `0x1018`; handler covers BCI 13–22 and targets BCI 23 for `AssertionError`. The condition call and its possible `AssertionError("condition")` are outside that handler. | Java 8 compilation and `-Xverify:all` succeed. Both original and JADX outputs: enabled `generated=1;condition=escaped:condition`; disabled `generated=0;condition=none`. | The `check(Z)V` record says “not recovered” and “explanation only,” lists bytecode BCIs `0 3 6 7 10 13 16 17 19 22 23 24 27 28 29 32`, and gives the refusal reason that the canonical block at BCI 32 has multiple owners. The class text happens to compile, but that does not make the omitted method a recovered behavior; running it produces different output. Treat this as a refusal record, not a successful equivalence result. |
| Core wrong-owner control, SHA-256 `af3c1190da44c938138da327a6232220acd3b8e8161808e2d9d517f11a4e71dc` | Original `AssertCore` field `Z`, flags `0x1018`; initializer calls `StringBuilder.class.desiredAssertionStatus()` rather than the current owner. | Original, JADX, and Jarde Java 8 compilation succeeds; verifier execution produces `0|0;0|0` for selective `-ea:AssertCore -da:java.lang.StringBuilder`. | Current CLI preserves `StringBuilder.class` in the full class source; it does not spell the guard as `assert`. Full sources, javap, and execution transcripts are under `assert-core-current-replay/`. |

Class hashes, full `javap -p -c -v`, all three source forms, Java 8 compiler transcripts, and `-Xverify:all` runner output are preserved in the replay directory. The extension controls patch only field access-flag bytes; no code or stack-map bytes are changed. The duplicate-write fixture patches `0x0008` to `0x1018`, while javac-generated synthetic-field controls patch `0x0018` to `0x1018`. The exception-handler class is produced by javac and receives the latter field-flag patch, so both new boundary classfiles are verifier-valid.

The evidence closes the fixture construction and behavior-recording portion of task 1.2. The handler-boundary method and duplicate-final-write initializer are explicit refusals: Jarde emits a method-level explanation-only marker for the former and Java 8 compilation rejects the latter's two writes to a final field. Neither is counted as successful recovered behavior.
