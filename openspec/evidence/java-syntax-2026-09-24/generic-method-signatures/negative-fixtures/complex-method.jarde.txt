// jarde: presentation of `ComplexMethodProbe` from the class file's own declaration and one recovery run per member.
// jarde: not a compilable project: no imports and no resources are claimed (the `package` line is the class file's own name, not a claim about a directory); every place this text is not a full recovery carries a marker of this prefix.
public class ComplexMethodProbe extends java.lang.Object {
    public ComplexMethodProbe() {
        // @method <init>()V
        // @declaration a constructor of `ComplexMethodProbe`, member flags 0x0001
        // recovered from bytecode; presentation is not claimed to compile
        super();
        return;
    }

    public static java.util.List[] choose(java.util.List[] arg0) throws java.io.IOException {
        // @method choose([Ljava/util/List;)[Ljava/util/List;
        // @declaration a static method of `ComplexMethodProbe`, member flags 0x0009
        // recovered from bytecode; presentation is not claimed to compile
        if (arg0.length == 0) {
            throw new java.io.IOException();
        } else {
            return arg0;
        }
    }
}
