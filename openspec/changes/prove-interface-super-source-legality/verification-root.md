# Root 独立验收：接口 `super` 的 Java 8 源码合法性

2026-09-25 使用最终独立 CLI `/tmp/jarde-interface-super-agent-cli`（SHA-256 `ae305c7e6ac445d7f7b7dd9b8fbd1be08c29d815f3e86f09f2be4473020ff190`）运行本目录 `accept-jarde.py`。旧基线 `/tmp/jarde-generic-accepted-cli`（SHA-256 `ca04265a4f412d59c29d6bd4a26b7d9cb961f72ae13e77684831c0b9e57b5145`）曾在直接冗余样例写出 javac 拒绝的 `RedundantParent.super.value()`；修后两种调试表输入均通过整个矩阵。三个先期证据组见 [直接接口冗余](../../evidence/java-syntax-2026-09-25/redundant-interface-super/analysis.md)、[抽象覆盖](../../evidence/java-syntax-2026-09-25/abstract-interface-super/analysis.md)与[父类链冗余](../../evidence/java-syntax-2026-09-25/superclass-redundant-interface-super/analysis.md)。JADX 1.5.6 在正常双接口样例把显式接口调用降成裸 `super` 后由 11/22/33 变为 7/7/14，不能作为 Jarde 的发射规则。

`python3 openspec/changes/prove-interface-super-source-legality/accept-jarde.py --jarde-cli /tmp/jarde-interface-super-agent-cli` 对 `-g` 与 `-g:none` 分别核对四种合法调用：无关双接口显式选择输出 11/22/33，唯一直接接口自声明 default 输出 4，继承 Parent default 输出 3，接口 default 调用自己的直接父接口输出 4。Jarde 的完整 class-source 可按 Java 8 原样重编并以 `-Xverify:all` 运行；method-only 保留相同精确限定调用且无缺口。所有 all/essential 正文相同。对 class-source 要求方法 BCI 范围的调用按既有合同明确以 `jre_evidence_range_invalid` 停止，未把证据选择变成源文差异。

同一脚本构造并执行直接接口冗余、父类直接/跨代实现 owner、目标接口抽象、**中间父接口抽象覆盖**及缺少提供 default 的祖先定义控制；补充同名重载的合法字节码保守拒绝，以及带 `tick()` 副作用实参的冗余调用。前述 patched class 用 `-Xverify:all` 验证原 JVM 可运行或在调用处抛预期 `AbstractMethodError`，但 Java 8 源码限定名不能由此推出。修后 class-source 与 method-only 都不发布未经证明的 `I.super`；相应报告 `quality=fallback`、`content=explanation_only`，正文的 `@bytecode` 保留调用 BCI，副作用样例还保留 `tick()` 的生产者 BCI。两种入口和 all/essential 决定一致。当前首片对同名不同描述符一律拒绝，即使 `Overloaded.super.value(1)` 在 Java 8 本来合法；这是明确的源级重载绑定证明边界，不冒充已恢复。

Root 重跑 `cargo test -p jarde --lib --test class_source --test p3_special_dispatch --test p3_special_refusal --locked`：31/31、47/47、5/5、4/4；显式运行 ignored 的 JDK 重编执行测试 1/1；`cargo test -p jarde-java --lib --locked -q` 180/180。`cargo fmt --all -- --check`、`git diff --check` 与 `openspec validate prove-interface-super-source-legality --strict` 通过。代码复核确认证明只由真实 `invokespecial InterfaceMethodref` 触发，按请求环境选定接口及完整父类链，`java/lang/Object` 仅作固定 JVM 根，交给恢复层的是精确 owner/name/descriptor；当前接口的 default 方法不被错误要求有类父链。拒绝仍走现有来源回退；预算/取消的定向 Rust 测试覆盖深度停止与预先取消。新增两个不相关父 default 的证明函数单测：三份 Java 8 class 的 SHA 与编译命令记录在 fixture README，测试在读取选定成员事实后仅剔除子接口为满足 Java 语法而写的冲突解决 override，`unique_source_default` 随即拒绝。这是函数级控制，不声称变异 class 可通过 Java 源码或 JVM 验证。

全量 reader 的其余 176 项通过，唯一 `repository_class_fixtures_validate_without_false_target_rejections` 因共享 fixture 人数从旧钉值 `(164,1152,98,381,8)` 漂到 `(310,1645,144,880,8)` 失败；本次三个冲突控制 class 正好使验收前实测的 `(307,1642,144,880,8)` 增加三类、三个 Code。严格 Clippy 因共享树既有 17 项 jarde-java 与 11 项 jarde 告警未过，新增接口证明函数无可归因告警。两项已拆记于 [架构债务](../../evidence/java-syntax-2026-09-22/architecture-debt.md)，不为使当前 change 表面全绿而改动无关逻辑。

代理的私有 Cargo target 已清理；Root 在全部回归与重编对照后执行 `cargo clean`，从本项目工作区移除 9830 个文件、释放 3.6 GiB，工作区落盘约 250 MiB、系统可用空间约 78 GiB。清理后再次运行 `git diff --check` 与本 change 的 strict validation 均通过。
