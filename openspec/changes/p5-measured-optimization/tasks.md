## 1. Measurement baseline

- [x] 1.1 固定版本/编译器/打包/身份/字节码/恢复/退化/对抗语料 fingerprint，并验证 A01–A18 覆盖清单（2026-09-20 完成，提交 `6d08124`。**落点判断**：fingerprint 属测试语料清单 → 写进仓库（`tests/fixtures/corpus-fingerprint.json` + `tests/p5_corpus_fingerprint.rs` + fixtures README 一节）；**A01–A18 的判定属验收权威陈述 → 未写 `openspec/**`，完整清单已由父级落进本 change 的 `verification.md`**。**fingerprint**：93 文件（fixtures 77 + fuzz/corpus 16）、**八维**（任务原文的七维 + `编译器` 单列）、每文件 `path/bytes/blake3`，与 `git ls-files` 范围内集合**逐条相等**；**按范围收录而非名单**（新 fixture 落地即覆盖），README 与生成脚本按扩展名排除（**记录不是输入**）。**先探明避免重造**：既有 fixtures README 的 SHA-256 表**从未被任何测试校验**（仓库无 sha2 实现，**表是散文**），golden 的 45 条 blake3 回放**已构成一部分**（本片**只索引不重复**），fuzz 种子此前只有 10 条**尺寸**（本片为 16 条加摘要）。**维度状态如实**：版本/编译器/字节码/恢复/退化 **pinned**；打包/身份/对抗 **partial**（WAR/Boot/ZIP64 只在 builder、proptest 逐次生成）；`known_gaps` **五条**如实列出**未凑**。**父级独立复核**：校验 5 passed / 1 ignored（0.03 s、无 JDK/网络）；JSON = 93 files / 8 dims / 18 rows / 5 gaps；**翻转一个语料字节 → FAILED 并打印两个摘要，还原后复绿**。**A01–A18 逐行清单**（本片核心产出，已落进 verification.md）：**16 行现已通过**（A02 除本机未跑的 JDK 25 oracle）、**A15 未通过**（P5 自身未开始；仓库**不存在任何 cache/并行实现**）、**A18 部分通过**（P1 半已通过，缓存/并行半属 2.x）；A04/A12 的「现代恢复」增量**未发生**（与 P4 3.4 一致）。**证据**：1082 passed / 0 failed / 4 ignored（+5 passed、+1 ignored）；fmt/clippy 1.98.1 干净；**MSRV 1.88.0 check 通过**；openspec strict 16；**被修正的既有断言 0 条**；锁文件两条 exit 0。**未完成**：1.2/1.3/2.x/3.x 各节；`known_gaps` 五条未闭合）
- [x] 1.2 建立冷/热、局部/全范围、direct/cache/parallel 的重复 benchmark，记录耗时、读取字节、物化范围、内存代理和取消状态（2026-09-20 完成，提交 `e7f509f`。**先探明**：仓库**无任何 benchmark 设施**（`benches/` 0 个、`criterion`/`[[bench]]`/`harness=false` 全 0 命中）→ 本片新建 `tests/p5_benchmark.rs`（测试内 harness，7 常开 + 1 `#[ignore]`，**未触及 `Cargo.toml`**）。**cache/parallel 选「甲」**：**只建 direct、如实记为 absent**，**不**跑两条同代码路径——否则会把「结果相同」读成「缓存已验证」（design Risk 与 A15 备注警告的正是这种**测量形状的主张**）；预留位是**代码**（`compare()` 对任意两次运行通用），context 写明「absent，P5 2.2/2.x 拥有」。**指标五项**：耗时用 harness 高分辨率时钟（报告时钟是整毫秒、本语料下恒 0，承载不了分布）；**内存代理复用既有 `UsageSnapshot` 计费维度**（不引入 allocator 计数器或新依赖），并写明**三条局限**（不是 RSS／是累计计费无高水位／可分配而不 charge）。**基线实测**（`REPEATS=200`，同机单线程）：四行的 min/median/p90/max 与两次测量的**固定量逐字相同**；**可比性声明**随数字给出——两次测量中位数漂移 +5%~+8%、**进程内首次可达中位数 13×**，故**低于约 10% 的差异在本机不可区分**，**不给加速倍数/P95**。**父级独立证伪**：正确变异（跳过归一化）→ **2 红**（含「同进程两次同输入运行摘要不同」）；**第一次变异无效**（对双方施加同一变换 = no-op）→ 全绿，**如实记录为变异写错、不计入证据**。**证据**：1089 passed / 0 failed / 5 ignored（+7/+1）；fmt/clippy 1.98.1 干净；openspec strict 16；锁文件两条 exit 0 且 **未新增依赖**；**被修正的断言 0 条**）
- [x] 1.3 定义结果 fingerprint、稳定排序、coverage/diagnostic 对照报告，验证 A15/A16（2026-09-20 完成，提交 `e7f509f`。**结果 fingerprint**：对完整运行的序列化文档**递归删除 `elapsed_millis`（唯一归一化）**后取 blake3；`Published` **保留原始文档**使归一化可被质问。**确定性证据**：8 次进程内重复全等 + **三个独立进程 marker 逐字相同**（**这一条才把「确定」与「本进程 HashMap 顺序稳定」分开**，seed 按进程变化）；两次完整测量之间所有行逐字相等。**稳定排序基线**：同进程与跨进程的 item identity 序列逐字相同，且对照报告把顺序要求定为**相等**（决策 4：并行只改调度不改发布顺序）——**今天无并行，这是基线证据而非并行正确性的证明**。**A15 仍未通过**：**重复那一半 verified**（8 次 + 3 进程一致），**缓存那一半不可验**——仓库无任何 cache/index，**没有第二条路径可关/开缓存**，spec 的 `Cold and warm comparison` 第 2 条件无法满足；负责片 **2.x + 3.1**。冷/热今天的**语义**差被断言为 **0**。**A16 不重做**：只在 benchmark 层面记录物化范围——局部行 headers=1/bodies=1（该类声明 **3** 个有体成员）、read_bytes=303；同一语料全范围行 read_bytes=909/class_bytes=909。**与 1.1 的接法**：读 JSON manifest、**逐条校验 subject 的 blake3+字节数**（跑的就是校验过的字节）、记 `blake3(corpus-fingerprint.json)` 为**语料版本**；**改语料 → benchmark 红**。**四项对 3.1 可比性有直接影响的事实**：① 局部行的**发布报告少报请求计费**；② **X1 全范围行的 headers/bodies 恒 0**（走自己的计费路径），与 P2/recovery **不同轨**；③ `result_items` 是**计费量非页大小**；④ 本机中位数复现性 ~10%、首次可达 13×。**证据**：1089 passed / 0 failed / 5 ignored；fmt/clippy 1.98.1 干净；openspec strict 16；**被修正的断言 0 条**）

## 2. Data reuse candidates

- [x] 2.1 基于测量选择是否需要多查询合并或细粒度并行，并验证 single-flight、取消和稳定发布满足 A14/A15（2026-09-20 完成，提交 `170ea20`。**决策：保持 disabled**——依据全部来自 1.2/1.3 实测：小语料、direct 中位 131–147 µs、**本机重复带宽 ~10%**（两次测量漂移 +5%~+8%、前后半 ≤12%）、**进程内首次可达 13× 中位**，低于该带宽的调度收益**不可区分**；且**无第二路径可比**（现由源码守卫机械核对，非一次性 grep）。**触发条件**写进记录且**未编造阈值**：出现「一个 snapshot 两个请求」的行、或某行资源每单位份额居首且差异越过同机带宽；ceiling/upgrade path 同记录。**A14 如实判定**：验到「入口前取消 → cancelled、零 items、诊断非空」+ 新补「取消是**每请求**的，后续同字节请求重新发布基线 fingerprint」——**这是 single-flight 今天唯一能验的形状**。**single-flight：无对象可验**（无共享请求，不假装验过）。**A15 维持「未通过」**。**父级独立证伪**：让矩阵谎称有一个未跑过的第二配置 → **恰好 1 红**，失败消息即设计拒绝语；实现者另三组（收益声明改「已证明」/carrier token 改名/植入 `CacheEntry` 类型）各自红。**证据**：1095 passed / 0 failed / 5 ignored；fmt/clippy 1.98.1 干净；**既有断言 0 改动**）
- [x] 2.2 基于测量选择 cache/index 范围与 key 维度；在未证明收益前保持 disabled，并验证 A01 candidate 不直接成为事实（2026-09-20 完成，提交 `170ea20`。**决策：cache/index 保持 disabled**；**key 以「记录」形状定义**（无 key 类型、无落盘、无索引布局）。**理由三条是仓库事实**：key 无消费者；**IR 与 recovery 两维今天根本没有 version 身份**（全 grep 无 schema/version 标识），实现 key 等于**先凭空造出它要 hash 的版本**；key 只能靠 cached-vs-direct 对照验证，今天钉住的是猜测且**会被读成「已验证」**。**key 维度记录**：`KEY_DIMENSIONS` **10 条**（决策 2 的 9 条 + `dependency-snapshot`），每条带 layers/source/carrier；`Present` **锚定源码 token**、`Absent` 必须写 why 与谁关闭；测试钉住「九条各恰好一次」「**Present 的 token 必须在引擎源码中存在**」。**A01 已覆盖不重做**：`unused_constant_pool_entries_are_candidates_but_never_calls` 等；**cache 语境下补一条** `the_reference_path_publishes_no_pool_candidate_as_an_item`（参考路径 derivations 只有 `structural_consumer`，`constant_pool_candidate` **0**）——**这就是任何 cache/index 路径必须复现的形状**。**如实记录**：实现者自查发现收益字段原为可变 `Vec` 而候选表是 `static`，**「候选带实测收益」这一状态永远构造不出来**（守卫写在它看不见的形状外），已改为静态切片后重跑。**守卫盲区**（与 A17 同口径）：只抓模块名/以 `Cache` 开头的类型声明/线程派生，**不抓**局部 memo 与别名缓存——补充而非替代。**证据**：1095 passed / 0 failed / 5 ignored；**既有断言 0 改动**）
- [x] 2.3 实现 cache 损坏、依赖补齐、profile/output/registry 变化的失效和直接路径回退，覆盖 facts-cache spec（2026-09-20 完成，提交 `2f6754a`。**§0 裁决的执行**：`facts-cache` 是本 change 的 **ADDED** 能力、任务原文要求**实现**它，而 `performance-gates` 允许「保留未启用状态」、Migration Plan 写的是「**disabled-by-default 实验开关**」（开关意味着被开的东西存在），且 **3.1 需要第二条路径**——故**实现完整语义 + 默认关闭**（不落盘/不加依赖/无并发）；**2.2 的「不默认启用」不变**，被取代的只是「连 key 类型都不实现」的推理。**层与落点**：CP/Header 层，`crates/jarde-reader/src/facts_cache.rs`；挂在**既有**读取入口（`class_facts`/`inspect_header`）上，**未改任何签名**，故所有调用方自动获得；**引擎零构造点**（父级核对 jvm/query 零命中）。**key = 内容摘要 + registry 版本 + parse policy**。**失效三条各可单独翻转**：依赖补齐（**不存 negative verdict**，补 provider 后旧环境仍答 negative、新环境重算）、policy（**strict 不得被 forensic 项回答**，否则 fail-open）、分层（原始层 key 不含 profile，8→11 仍命中且 `reads` 证据逐字节相同）。**可选与透明**：损坏/未知格式 → 丢弃并在**同一预算**内走直接路径（父级实跑复现 `archive_entries=13`/`read_bytes=627` 两侧相同——**回退不重置预算**）；**cache 状态不进结果文档**（否则冷/热 diagnostics 不等）；命中**仍为发布付 `ResultItems`**。**冷/热对照与 A15 判定更新**：结果四平面（status/order/coverage/diagnostics）**逐字段相等**，差异**只在 `usage`**，**两个暖运行 fingerprint 相等**；资源确定性更少（−185 class/−29 attribute，matrix −555/−87）。**A15 由「未通过」改为「部分通过」**——缓存半首次有真实第二条路径；**未成立的一半是「整档 fingerprint 相等」这一读法**（它要求 cache 什么都不省或伪造 usage，而规格列举的必须相等平面里没有资源）。**父级独立证伪**：让 policy 掉出 key → **4 红**（含 fail-open 那一例）。**实现者三组**：其中「回退刷新预算」第一次**没有**打红（尚无可观测的 charge）→ **据此加强测试后再跑才红**（如实记录为「测试被证据推翻后补强」）。**7 处既有断言修正**（无放宽）：其中「引擎没有 cache」一条今日为假，改为**更强**的三条事实（单模块声明/引擎零构造点/`Budget::new` 不带）；`CANDIDATES` 由 `Unmeasured` 改 `Measured` 并**新增测试重算 delta 与记录逐项相等**（记录可被测量证伪）。**证据**：1109 passed / 0 failed / 5 ignored；fmt/clippy 1.98.1 干净；openspec strict 16；锁文件两条 exit 0 且**未新增依赖**。**未完成**：X1/resolution/IR 三层只有契约；容量以 entry 数计；中位数未阈值化；**3.1/3.2 的差分与压力语料待做**）

## 3. Release gates

- [x] 3.1 对每项启用候选执行 optimized/direct evidence、coverage、representation、diagnostic 和资源差分，验证 A13/A14/A18（2026-09-20 完成，提交 `6ca0cc2`。**改动面只有测试**（父级核：`crates/`/`src/`/`openspec/`/`Cargo.toml` **零改动**）。**门禁**：`plane_comparison` 把规格**八面**逐条映射（evidence/representation/coverage/diagnostics/**identity**/**peaks**/cancellation/**termination**），**名单外的面名直接 panic**；`blocking_difference` 任一面 `Compared(false)` 即拒并**点名哪一面**；`Candidate` 新增 `default_state`，**今天三项全部 `Disabled`**；门禁测试**逐候选**跑差分且**判定不依赖 state**。**「无对照对象」的如实清单**：逐行（query/resolution 行的 `representation` 无对象，由恢复行承担，且**测试断言有对象的行必须 Compared、无对象必须 NoObject**）+ 整体五项（index/parallel/merged、P4 modern 面、真 RSS）。**被修正断言 1 处**：`equivalent()` 不再要求**含 charge 的整档 fingerprint**（`Cold and warm results` 的必须相等平面里**没有资源**）——**未放宽**：唯一既有调用者仍单独断言 fingerprint，整档仍被比较仍被打印，新增三面是把比较面**扩到规格原文**；`tests/` 删除行数 = 1。**证据**：1113 passed / 0 failed / 5 ignored；fmt/clippy 1.98.1 干净；openspec strict 16；未新增依赖）
- [x] 3.2 在 ZIP bomb、condy 图、不可约 CFG、缺失依赖、取消压力和被选目标阶段的共享不变量语料上运行回归，任何语义差异阻断默认启用（2026-09-20 完成，提交 `6ca0cc2`。**五类语料 × 十行 × 三跑 × 八面，off/on 全 equal**：ZIP bomb（自建**谎报 uncompressed=1、流实为 8 KiB** → `partial` + `budget_exceeded{entry_bytes}` + scanned/skipped 非空）、condy 共享图（含 `bootstrap_edge`/`bootstrap_argument`）、不可约 CFG（**提交的** `Guarded.class` → 含 `jre_region_irreducible`）、**A13 对照行**（**同一类**的可呈现成员 → `representation=java`）、缺失依赖三态、取消压力三条（解压 / IR 预算 / IR 取消）。**存储确实在回路里**（非取消行断言 `consultations>0 && hits>0`）。**A13/A14/A18 逐条**：A13 —— 呈现与拒答**同类并存**、四平面分离、缺失依赖**不变否定**；A14 —— 四条停止行两侧都给**已扫描范围与终止原因**、`exit != complete`；**A18 —— 本片补跨快照隔离**（2.3 未覆盖）：不同内容的两快照各自等于自己的 direct 运行、同内容不同 origin 各发布自己的身份、**取消的请求不留东西给另一个快照**；**父级独立证伪**：把**内容从 key 拿掉** → **恰好该用例红**（消息即 `the store is not keyed by the content it holds`）。**方法学记录（本片最有价值的一对）**：取消被报成 Complete 分两个方向——**③a 引擎侧对称错** → 取消压力用例红而**差分判相等**（**差分抓不到对称错误**，抓到它的是每行自己的 `exit`/终止原因断言，**二者缺一不可**）；**③b 只丢 cache-on 侧** → 用例与**门禁同时红**。**另一处机制性限制**：**取消不可能被 cache「吞掉」**——每次咨询前都已 poll 过读取路径。**未闭合**：`NO_SECOND_PATH_TODAY` 五条、`KEY_DIMENSIONS` 的 IR/recovery 仍 `Carrier::Absent`、容量按 entry 数计；**未阈值化**（十行墙钟未断言）——门槛与测得范围的发布属 **3.3**）
- [ ] 3.3 发布实测范围、重复策略、未决阈值和启用开关；不添加无证据性能数字或未评估依赖
- [ ] 3.4 执行 cargo fmt、clippy、test、benchmark smoke 与 OpenSpec strict validation，记录本阶段仍未改变结果契约
