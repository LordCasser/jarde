## 1. Modern registry and facts

- [x] 1.1 定义 release-bound feature registry、preview/minor/attribute/flag 规则与 capability 输出，覆盖 53–71 和未知 future cases（2026-09-20 完成，提交 `8620249`。**新增** `crates/jarde-reader/src/release_registry.rs`，既有 `classify_version` 改为**消费**它；既有四类枚举/字段**全部保留**，既有版本矩阵等用例**一字未改即通过**（父级核：删除行中 `assert` 计数为 **0**）。**每个 release 一条记录**：记 CP tag / attribute / flag / opcode 四类**引入**的约束（各带 `since` 与 JVMS 来源），累积生效；**45–71 逐条无空洞**（表自身有不变式测试）。**来源诚实是本片的一半**：每条记录带**非空** `unregistered`（列**刻意不主张**的东西，测试强制非空），`NotRegistered`/`Unregistered` 是「**无主张**」而非「非法」。**四个平面分开**：parse=`structural_read`、dialect=`VersionCapability`、verification=`NotPerformed`、**output-level 新增 `HeaderInspection::output_level`**（本片仅 `NotEvaluated`，与 verification 对称）。**父级独立探针**证实未登记 release **绝不借用**上限 release 的能力（72/100 的 `Record`/`invokedynamic` 查询均返回 `UnregisteredRelease`），且探针非空洞（71/59 正常作答；`Record@59 → VersionNotApplicable`）。**证据**：1006 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；CI run 35467145921 四 job success；三组证伪见 verification。**边界**：不读 module/record/sealed/nestmate 事实、不把合法性接到读路径（负向用例钉住）、不改 `dialect_validation_scope`、不产 fixture——前两项属 1.2/1.3）
- [x] 1.2 实现 module/nestmate/condy/record/sealed/modern concat facts 与 origin/evidence，覆盖 A05 及 output-level conflict（2026-09-20 完成，提交 `3df96d1`。新增 `crates/jarde-reader/src/modern.rs`：record components（含组件上的嵌套 attribute shell）、sealed 名单、condy 图、modern concat 站点，**各带 CP 索引/attribute 范围/bootstrap 索引**。**modern concat 归 reader**（只读 CP + `BootstrapMethods` + 句柄 `MethodRef`，**不求值 recipe、不解析句柄、不调用工厂**），站点 BCI 归字节码读取、**join key 是 CP 索引**；与 P3 的 `concat@1`（呈现）显式区分。**A05**：行走是**显式栈非递归**（父级读码确认），visited 展开一次、环记为事实不跟随、四维预算（复用既有维度）命中即停走**并发布已走前缀**；**不执行 bootstrap 的铁证**——fixture 的句柄指向**本仓库不存在的类**，图仍完整且断言「走到句柄即止、从不跟随它命名的成员」。**output-level 冲突**：`OutputLevelStatus::Conflict` 列出每个 feature 的 origin 与决定它的 release 规则，**事实原样保留、不做等价降级**；header 平面仍只产 `NotEvaluated`（负向用例钉住）。**registry 接线**在事实读取路径：`Legal` 才读内容，版本/位置不适用**不读内容**且报诊断（`AttributeBytes` 低于合法读为证）；`inspect_header` 一字未动。**fixture**：`tests/fixtures/p4-modern/` 11 个 class（52/60/61），SHA 入 README；**父级独立重编译 → 11/11 逐字节相同**。**产不出如实记录**：javac 23.0.1 在 8/16/17 上不产任何 `CONSTANT_Dynamic`，故 condy fixture 测试内手工组装。**证据**：1023 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；CI run 35468561216 四 job success；三组证伪见 verification。**未完成**：flag/opcode 接线与 golden diagnostics 属 **1.3**；`OutputLevelStatus` 的其它 level 属 **2.x**）
- [x] 1.3 建立现代合法/非法 fixture 和 golden diagnostics，验证结构读取不冒充 dialect/verification 成功（2026-09-20 完成，提交 `6ed448a`。**golden** 照 `p2_golden` 逐字段同形：固定回放表与 JSON 自带表**互比**、每条重建具名生成器并断言 `blake3`+长度、钉整个 header 平面与事实侧投影 + **strict 结论**、文件静态；`illegal` 标志被断言必须与所在文件一致。**非法 fixture 逐字节可重放**（`version_patched` 补版本字段 + `jsr_class`/`invokedynamic_class`/`nest_members_in_a_method`/`constant_value_in_a_method`/`acc_module_class` 五个具名生成器，摘要入 golden）。**接线在事实路径**：`inspect_header` 一字未动；成员属性判位置、opcode 经 `method_body_opcodes` 取真实游标、flag 只陈述 release 主张、tag 不新造 code。**两处上报发现**：① 把 opcode 禁止加到 `inspect_method_bytecode` 会改动 P2 已冻结的 golden（`illegal-version-jsr-in-52`），故改接事实路径——**字节码路径仍不对 release opcode 发言**；② **逐位解析 flag 不成立**：登记的是「哪些 release 引入了哪些 flag」而非结构词表，第一版在真实 `RecordSample.class` 上产 **8 条假诊断**（如把 `ACC_FINAL` 报成只属 `MethodParameters`），故**不作任何 flag 位置主张**、flag 改按名驱动，由 `a_flag_is_answered_by_name_and_never_inferred_from_a_bit` 钉住。**父级独立复核发现 2**：对 11 个真实类跑事实路径 → **flag 诊断 0、诊断总数 0**。**不冒充成功**：对 9 条非法条目逐项断言 `structural_read=Complete` + `verification=NotPerformed` + `output_level=NotEvaluated` + dialect 与记录一致；并断言「本 build **校验**的 release 照样可带违规」「只**探测**的 release 照样被完整读出」。**证据**：1029 passed / 0 failed / 3 ignored；`p4_golden` 6 passed；fmt/clippy 1.98.1 干净；openspec strict 14；**既有断言零改动**；三组证伪见 verification（副本 323/323 校验无差异）。**未完成**：flag 位置主张与完整词表属 1.1/2.x 决定；参数 flag 与成员属性内容归各自 pass；多档 `output_level` 属 2.1/3.3）

## 2. Runtime depth

- [x] 2.1 实现 RuntimeMatrix、模块/loader policy 和 MR/layout 选择函数，验证 A06/A07 的多 profile 物理保留（2026-09-20 完成，提交 `c2676d4`。**归属 `jarde-reader`**（输入全是 reader 自有事实；放上层要么重扫要么另写 MR 选择，二者皆禁），`Engine::runtime_matrix` 一行委派。**共享扫描**：`multi_release::select` 拆为 `physical_evidence()` + `select_over_physical()`（`select` = 两者，既有 p1 MR/golden/query 用例零改动通过）；父级读码确认唯一枚举**在每 profile 循环之外**，循环内读零 archive 字节。**证据是测得的恒等式**：`matrix == 3×扫描 + Σ每profile == 三个 standalone`；实测平坦 jar 省两次枚举（34 vs 42 条目录记录），WAR 树另省 386 字节；每 profile 的 220 字节是 compliance probe 自身重读、**如实分开呈现**。**不塌缩**：三个 view 各自的答案与**全部物理条目**并列（`unselected().len()==2`）；**父级独立证伪**——把 release 换成常量使矩阵塌缩 → 「三 profile 选不同条目」用例**恰好 1 红**，证明该断言承重。**压缩只在可证时**：签名相等**且**区间之交覆盖所有成员 release 才合并（签名不含规则文本）；`Ambiguous`/`NoSelection`/`Unknown` **无区间**故永不合并；反例（两个 `Custom{id}` 答案相同但无区间）有专测。**module/loader 边界如实**：`ClassPath` 可行动、`ParentFirst`/`ChildFirst` 给显式次序；`ModulePath`/`Hybrid`/`Custom`/`Unknown` 一律 `Unsupported` 且**放弃顺序主张**（与 jvm 的拒绝一致）。**layout**：改前 `LayoutMode` 全仓库无消费者，矩阵首次把它投影为层 + 在路径容器。**A06/A07**：Java 8/11/17 三答案并存 + 4 条物理条目全列；Manifest 未激活与缺 public 前驱各有诊断且**条目仍列出**；A07 的同名类不同 ordinal/origin + `Ambiguous`/`Ordered`/`Undetermined` 三种序，且**换 delegation 声明真的改变答案**。**证据**：1040 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；**既有断言零改动**；锁文件两条 exit 0；CI 见 verification。**未完成**：CLI 出口未扩展；具名模块顺序、兄弟 loader 并存、`WEB-INF/classes/` 下的 MR 目录均**如实未做**；解析态（缺失依赖/default conflict/open-world）属 **2.2**）
- [x] 2.2 扩展 X2 declaration/dispatch candidate 查询，覆盖缺失依赖、default conflict 和 open-world 状态（2026-09-20 完成，提交 `2472887`。**本片修掉了 A11 的一个真实违反**：层级行走读不到父类时仍发布 `Missing`——而 `Missing` 意为「覆盖全部且什么都没找到」，由**未覆盖全部**的搜索给出；**7 条既有断言**把同一主张编码下来（这正是它能存活的原因）。**扩展而非平行模型**：加 `ResolutionState::UnresolvedDependency` 与 `ResolutionReport.unresolved_dependencies`（带 name/loader/reason/**declared_by**/gap）；`WalkGaps` 由名字数组改为**记录表**，`Search.unread: u64` 与两处 `bool` 一并升级；名字**第一次以结构化形式**发布（此前只在诊断文字里）。**A11 核心 + 对照**：缺 `p/Absent` → `UnresolvedDependency` 且 `assert_ne!(Missing)`、整结构体钉住；**对照**（父类在场）→ `Missing` 且依赖列表为空——证明修法**不是**「一律不说 Missing」。**default conflict** 沿用既有 `IncompatibleClassChange` + `resolution_default_conflict`（判据 `maximally_specific`，JVMS 5.4.3.3），并**两条不冲突控制**（子接口覆写父接口、类自带覆写）防止过度报告。**open-world**：候选逐条带事实、范围命名读不到的类；链外 loader 与「位置解析到范围外」各一条；「**不声称唯一目标**」由对 `DispatchReport`/`DispatchCandidate` 的**穷尽解构**在编译期钉住。**出口**：不加 relationship，经既有 `Engine::resolve_symbol`；新增键与新增 state 取值在 JSON 层是**加字段**、Rust 层破坏性（穷尽 match 强制，已有用例抓出）；新平面是证据、**不计费**（`ResultItems` 未变）。**与 2.1 独立**：一个请求只命名一个 view，故「矩阵选 view → 每 view 一次请求」；反向依赖才是分层倒置。**既有断言 7 处修正，全部加强**（父级核：`tests/` diff **+100/−7**，5 条被删的断言全被更长的整结构体断言取代并**新增**「不得否定」断言；其余 1035 条一字未改）。**证据**：1048 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；分层 12 条 closure 全绿；锁文件两条 exit 0；三组证伪见 verification。**未完成**：`DeclarationRefReport` 的结构化缺依赖面属 **2.4** 决定；歧义/环是否拆成独立状态属**公开契约决定**；X3 属 **2.3**）
- [x] 2.3 实现有界 X3 reflection/ServiceLoader patterns，验证常量目标与动态输入分别返回 inferred/Unknown（2026-09-20 完成，提交 `886f814`。**登记表**照 registry 风格（五要素 + `not_claimed` 测试强制非空 + `source`），覆盖 `Class.forName` 两个 overload、`getMethod/getDeclaredMethod/getField/getDeclaredField`、`MethodHandles.Lookup.find*` 五种、`ServiceLoader.load` 两个 overload；**四条如实登记为 Unsupported**（名字由 API 固定的 `<init>`、枚举型 overload）；**未登记 = 不主张**（不产站点记录）。**落点 `jarde-jvm/src/reflection.rs`**：传播必须用本方法 SSA、loader 假设需 jvm 私有环境、名字解析走 2.2 closure；**不加 `QueryRelation`**（避免 jvm→query 倒置），复用 2.2 的记录类型与 2.5 的 range 机制。**有界**：复用既有 `analyze_method_ir`（**无第二次解码**）、常数源限 `ldc` 与本类 `ConstantValue`、别名链 ≤8、**不跨方法**、预算走既有维度；`getstatic` 源带「本快照不持有的写者未被排除」的文本假设，loader 假设逐条带 `statement`。**inferred 五要素**由 `#[serde(deny_unknown_fields)]` + **测试穷尽解构**在**编译期**保证（加字段即编译不过），JSON 拼写 = 规格原词 `pattern_inferred_target`。**Unknown 不猜测**：动态来源按 SSA 定义逐类命名（参数/merge/caught/其它 opcode/无定义/超界）；**父级独立证伪**——把 `Entry`/`Phi` 当常量返回 → **恰好 1 红**（`a_dynamic_input_is_unknown_and_never_a_guess`）。**边界**：常量名但类不在快照 → inferred + `NotInSnapshot` + 缺依赖（**推断问的是「调用点要什么」，与「快照有什么」分开发布**）；非法二进制名与 ServiceLoader 的接口缺失 → `NotDemanded`（**把畸形串当缺失的类是错的**）。**不执行**：`dynamic_analysis == NotRequested`；目标类**从不被 decode 或 resolve**；去掉目标类后站点结论逐字段相同。**证据**：1065 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；**既有断言零改动**；锁文件两条 exit 0；三组证伪见 verification。**未完成**：`Ambiguous`/`Caught`/`ChainBeyondBound`/环境被拒/listing 截断等分支**无 fixture**（如实记录）；`ServiceLoader.stream` 等未登记；descriptor 化与 CLI 出口属 **3.1/3.3**）

## 3. Plugin and acceptance

- [x] 3.1 定义 versioned query plugin descriptor、schema、rule/evidence/coverage 和资源预算，验证未注册配置为 Unsupported（2026-09-20 完成，提交 `9b37ea8`。新增 `crates/jarde-query/src/plugin.rs`：`PluginRule` 声明 **id/rule version/输入类别/配置路径/输出 schema/evidence/coverage/预算类别**，注册表风格照 2.3 与 registry（`not_claimed`/`source`/`isolation` **非空由单测钉住**）。**未注册 = Unsupported** 由专测区分**三种拒绝**（id 未注册/版本未注册/已登记未执行），三者 coverage 均 `not_requested()`；**对照**：只含注释的 entry 才是「跑过 + 0 item + `CompleteWithinSchema`」——**声明为空 ≠ 拒绝**。**父级独立证伪**：让未注册配置回退到第一条已登记规则 → **恰好 1 红**。**不改变 P1/P2 事实**：plugin 前后 X1 报告与 listing **serde JSON 逐字段相同**，而 **plugin 确实返回 3 条**（两者缺一不可）；与通用 XRef 的分隔由规则身份、schema 字段集、选择规则与类型四重表达。**隔离立场**写在 `PLUGIN_TRUST_DOMAIN` 并单测钉住含 `not a sandbox`/`separate process`/`Wasm`——**未实现也未暗示 trait 即沙箱**。**证据**：1074 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；**既有断言零改动**；三组证伪见 verification。**未完成**：`ArtifactTree` 嵌套 scope 答 `NotRequested`（有专测）；`Skipped{Error}`/`Cancelled` 无 fixture；`input` 类别目前只有一个取值；CLI 出口属 3.3）
- [x] 3.2 实现至少一个只读 framework/resource plugin fixture，验证不执行 bootstrap、launcher、JNI 或网络（2026-09-20 完成，提交 `9b37ea8`。选 **`META-INF/services/<service-interface>`**：它正是 2.3 明确留下的声明面（其 `not_claimed` 写着「no `META-INF/services` resource is opened」），两条规则**互补不重叠**；语法由 `ServiceLoader` javadoc 规定；通用结构面已对同批字节有 target-driven 视角，**分隔可测**。**格式所有者复用**：`xref/resource.rs` 的 matcher/解析器提升可见性后**共用同一份实现**（无第二份格式实现），descriptor 的 prefix 与共享常量比对，不一致即 **fail-closed 不读任何 entry**。**不执行（证据非声明）**：先证明归档里**真有**可解码类与 manifest 真写着 `Launcher-Agent-Class`/外部 `Class-Path` URL；plugin 只发布配置里拼写的名字（**含归档里不存在的类**），其请求 usage 的 class/IR/bytecode 维度**全 0**、`entry_bytes=124`，报告 `Complete`（若真启动 launcher，缺失类会是失败），item 不含 agent/launcher 名或 URL。**证据**：`p4_plugins` 5 passed；全量 1074 passed / 0 failed / 3 ignored；**既有断言零改动**；实现者证伪 ②a/②b（污染通用 scanner / 给 X1 item 打 plugin 戳）均使「X1 逐字段相同」用例红。**未完成**：CLI 出口、现代支持矩阵与 A04–A07/A12 对抗测试属 3.3；`Skipped{Error}`/`Cancelled` 分支无 fixture）
- [x] 3.3 更新现代支持矩阵、Java 8 output conflict 和 source map，运行 A04–A07/A12 及 adversarial tests（2026-09-20 完成，提交 `bc2d205`。**A04–A07/A12 逐行落证据**：A05/A06/A07 **逐条对上**；**A04 的增量只在事实平面**（condy/concat 的任意 bootstrap 被证据化；lambda 恢复仍是 P3，**若读成要求恢复语义则 P4 无增量**）；**A12 如实标注「P4 无增量」**——**父级独立核实**：`jarde-java` 对 `ModernFacts`/`modern_facts`/`ModernOrigin`/`OutputLevelStatus` **零引用**，恢复层输出与 P3 相同。**现代矩阵**：`docs/support-matrix.md` 8 处逐字替换全部匹配（含**新增五平面小节**），`README.md` 新增 P4 段并**修正一处 P3 时代遗留的矛盾句**。**output conflict 判断有依据**：谓词是「**Java 8 没有等价物**」而非 `since > level`——concat 的 tag `since=51` 比 Java 8 还老，档位盲用会**误判为可表示**；故**不加档位**，改为把 level 变**穷尽 match**（**新增变体会编译失败**，必须给出该档谓词）并钉住「module/nestmate/condy 不产输出级别结论」；**触发条件**（出现第二个目标档）写进矩阵。**source map 无 P4 增量**，触发条件同样写明。**对抗测试实跑**：ZIP bomb（P0 已有**真实 bomb 形状**：谎报 size + 实际展开超限且**不返回字节**）4 passed、condy 7、缺失依赖 4、不可约 CFG 1、取消压力 40、非法现代形状 6——**无回归**。**证据**：1075 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；**测试断言零改动**（`tests/` **+65/−0**）；两组证伪见 verification。**未完成**：3.4 的门禁数字与「结构支持 vs 源码恢复」结论；CLI 出口与前序遗留缺口仍在矩阵中列明；新锚点未在渲染器验证）
- [x] 3.4 执行 cargo fmt、clippy、test 与 OpenSpec strict validation，记录结构支持和源码恢复之间的独立状态（2026-09-20 完成，提交见下；本片只写 `verification.md` 与 `tasks.md`，**未触碰** `crates/**`/`src/**`/`tests/**`/`docs/**`/`Cargo.*`。**门禁全跑**：全量 **1075 passed / 0 failed / 3 ignored**（**57** binary，`--no-fail-fast`，与基线逐字相同）；`p3_execution_comparison -- --ignored` **2 passed**（17.66s）；fmt exit 0；clippy 1.98.1 `-D warnings` exit 0；**MSRV `+1.88.0` Finished**；锁文件 `cargo metadata --manifest-path fuzz/Cargo.toml --locked` exit 0；supply-chain **两套**（根与 fuzz）各 `advisories ok, bans ok, licenses ok, sources ok`；`cd fuzz && cargo test --locked` **21 passed**；fuzz 冒烟 26s×3（`/tmp` scratch 语料）`query` **218,962** / `artifact_tree` **370,066** / `method_analysis` **292,724** execs，三个 exit 0、`slowest_unit_time_sec` 0、**零 crash/timeout**、`fuzz/artifacts/**` **0 文件**、tracked 语料未写；**openspec strict 14 passed**；两个 example exit 0；分层三包的 `jarde-java` 计数 **0/0/0**；`git diff --check` 干净。**3 ignored 逐条点名**：① `jvm_bytecode_oracle::jdk25_instruction_boundaries_match_public_bytecode_inspection`（需 JDK 25 Class-File API oracle，**本机环境缺口，未运行**）；②③ `p3_execution_comparison` 的两条（需 PATH 上有 JDK）——**这两条本机已用 `--ignored` 实跑通过**，故 not verified 的只有 ①。**「结构支持 vs 源码恢复」独立状态**（**本片核心**）：① **结构面已到 3.1–3.2 且未触及恢复层**——依据是本片重跑的零引用 grep：`grep -rn "ModernFacts\|modern_facts\|ModernOrigin\|OutputLevelStatus" crates/jarde-java/` **退出码 1、无输出**，该目录 **27 个 `.rs`**；补充 `grep -rn "modern\|Modern" crates/jarde-java/src/` **同样零命中**。方向性：`jarde-java` 依赖 reader/jvm，反向引用被 cargo 解析期拒绝。② **恢复面仍是 P3 状态**——`OutputLevel`（`classfile.rs:144`）**只有 `Java8`**；其**唯一**非测试消费者是 `modern.rs:484` 的穷尽 match 空臂 `OutputLevel::Java8 => {}`（3.3 所加，**无行为**）；`Origin`（`jarde-java/src/source_map.rs:79`）仍是 P3 3.2 的 `{bci, method, cp, provenance}`。③ **故 A04/A12 的「现代恢复」增量尚未发生**，**触发条件**写明：**恢复层开始消费 `ModernFacts`**，最小可观察形状为 (a) `OutputLevel` 出现第二个变体（此时 `modern.rs:484` **编译失败**，强制给出新档谓词）或 (b) `jarde-java/**` 对 `modern_facts` 的引用由零变非零。④ **三个新入口各自报告的平面**（读类型得出，**不套用恢复层词汇**）：`RuntimeMatrix` 报 physical/domain_graph/profiles/groups/`coverage`/execution/scan/usage + 一个 **header 平面自有类型**的 `verification`（硬编码 `NotPerformed`）；`PluginReport` 报 `PluginAnalysis::{Performed,NotPerformed}` + 每规则 `Coverage` + execution，**完全没有** verification 面；`ReflectionPatternReport` 报 `analysis: ResolutionAnalysis`（**2.2 词汇**）+ reads + coverage，**完全没有** verification 面。`compile_status`/`semantic_validation`/`quality`/`syntax_status`/`representation` 是 **jvm IR 平面**（`crates/jarde-jvm/src/ir.rs:279/283`，`RecoveredMethod`）的词汇，**三个新入口一个都不填充**。**口径**：「结构读得出」≠「源码恢复」。**证伪（副本 + `shasum -a 256`；禁用的 git 还原命令一条未用）**：**A**（`RuntimeMatrix.verification` → `Performed`）→ **全量 1075/0/3 全绿，零红**；**C1 对照**（header 平面同一字段 → `Performed`）→ **红**，`p4_feature_registry` 2 failed；**B**（向 P4 新文件 `runtime_matrix.rs` 注入 `#[cfg(any())] use jarde_java::RecoveryReport;`，**编译通过**）→ A17 守卫 **1 passed 绿**；**C2 对照**（同注入放进 `crates/jarde-query/src/query.rs`，置于文档注释后，`cargo build` exit 0）→ **红** `left: ["crates/jarde-query/src/query.rs: jarde_java::, RecoveryReport"] / right: []`。**发现 1（无守卫，如实上报，本片不代为新增）**：**`RuntimeMatrix.verification` 无任何断言守卫**——改成 `Performed` 后 1075 条全绿；**不是方法无效**（C1 证明 header 平面同一字段一改就红），即 P4 引入**不对称**。因该字段类型**跨平面共享**（`classfile::VerificationStatus`），将来接线不会被任何测试拦下；**建议**在 `p4_runtime_matrix.rs` 加一条负向断言钉住 `NotPerformed`（无需新 fixture）。**发现 2（守卫覆盖缺口）**：**A17 恢复层源码守卫的受守卫集合只有 `crates/jarde-query/src/query.rs` + `crates/jarde-query/src/xref/**`**（`p2_contracts.rs::guarded_sources` 与 `A17_QUERY_MODULE`/`A17_XREF_DIRECTORY`），**P4 新增的五个模块全部在其外**（`jarde-reader/src/{release_registry,modern,runtime_matrix}.rs`、`jarde-jvm/src/reflection.rs`、`jarde-query/src/plugin.rs`）——B 注入编译通过且守卫照常通过，**即守卫盲区从 0 个模块扩到 5 个模块**；cargo 循环依赖拒绝仍兜底分层，但拦不住不构成依赖边的文本引用。**流程注记（如实）**：C2 首次尝试是**无效对照**（注入点落在 `//!` 之前触发 `E0753` 编译失败，exit 101 非守卫命中），已改为文档注释之后重跑；该次不计入证据。**副本校验**：四文件逐次还原后 `shasum -a 256` 全部 **OK**（`RESTORE_BYTE_EXACT_OK`）；仓库事后只有本片写入前既有的两处改动（`JVM_Rust_Engine_Final_Architecture.md`、`fuzz/README.md`，**非本片所为**）。**未完成汇总（P4 全片，逐条判定「能力缺口」或「有意边界」）**——① **CLI 出口**（三个新入口只有库/facade 出口，`crates/jarde-cli` 一字未动）：**能力缺口**。② **plugin 的 `ArtifactTree` 嵌套 scope** 答 `NotRequested{plugin_scope_not_registered}`（有专测，非静默等同 `SnapshotAll`）：**有意边界**。③ **X3 若干分支无 fixture**（`PatternTargetState::Ambiguous`、`DynamicOrigin::{Caught,ChainBeyondBound,NoDefinition}`、环境被拒 → `Analysis=NotPerformed`、listing 截断、`resolution_pattern_body_not_analysable`）：**能力缺口（覆盖缺口，非实现缺口）**。④ **`MethodParameters` 未读**（方法级属性需新计费口径；P3 遗留同一项）：**有意边界**。⑤ **类级事实不在载荷**（`InnerClasses`/`ACC_INTERFACE`；P3 遗留）：**有意边界**。⑥ **flag 具体位置不主张**：**有意边界且有实测依据**（1.3 发现逐位解析在真实 `RecordSample.class` 上产 8 条假诊断，故 registry 只记「哪个 release 引入哪些 flag」，位置主张由调用方按名查询）。⑦ **`OutputLevel` 第二档**：**有意边界**（3.3 已把 level 改成穷尽 match，新增变体**编译失败**，逼出新档谓词；触发条件＝出现第二个目标档）。⑧ **现代恢复未发生**（A04/A12 的「现代恢复」增量不在 P4）：**有意边界**（触发条件见上）。⑨ **新锚点未在渲染器验证**（`docs/support-matrix.md`/`README.md` 新增章节锚点按 GitHub 规则推导）：**有意边界（记录口径局限，非能力缺口）**。**另**：`DeclarationRefReport` 无结构化 `unresolved_dependencies` 平面（留给 **2.4** 契约决定）＝**有意边界**；歧义/环分支与「名字不存在」共用 `UnresolvedDependency`（2.2 留给复核的**公开契约选择**）＝**有意边界**；plugin `Skipped{Error}`/`Cancelled` **无 fixture**＝**能力缺口**；plugin descriptor `input` 类别只有一个取值＝**有意边界**；未实现进程/Wasm 隔离（`PLUGIN_TRUST_DOMAIN` 已写明属另立 change）＝**有意边界**；具名模块顺序、兄弟 loader 并存、`WEB-INF/classes/` 下的 MR 目录未做（2.1）＝**有意边界**；`ServiceLoader.stream` 等 overload 未登记＝**不主张**（有意边界）；`method_body_opcodes` 容错且每 body 计一次 `CodeBytes`（1.3，成本口径留 2.x 重看）＝**有意边界**；62–71 只登记「无自有条目」、`ACC_STRICT` 未登记、CP tag 引用节而非子节号、opcode 只登记四种（1.1 来源诚实）＝**有意边界**；P3 遗留的 canonical「handler 入口即根」待裁决与 **verifier 未实现**＝**有意边界**）
