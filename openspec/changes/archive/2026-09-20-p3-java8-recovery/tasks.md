P0/P1、P2（29/29）和分层（7/7）均已归档，P2/分层归档提交为 `7a5f994`。当前进入 P3：`1.1/1.2/1.3/2.1/2.2` 已有交付记录（代码到 `492e31e`，收尾记录 `51a5cac`）；本轮新增基础值流修正 1.3d 后为 5/12，其后 3.1/3.2/2.3/2.4/3.3/3.4 与 P3-R6/R7、发现 (i) 相继关闭，**现为 12/12**（证据见 [验证记录](verification.md)；归档由父级执行）。已有 Java 方法体、if/loop/switch、lambda 及部分拼接/bridge/accessor 呈现，仍受公开入口和语义边界约束；P4/P5 未实施。 复核反例见 [当前记录](verification.md#review-2026-09-19-recovery)。下列完成项中的“仍未完成/进行中”如属于 1.3a/b 的子片记录，只描述当时状态，不重开已完成交付。

## 1. Recovery contracts and first usable method

先完成 **1.3d 值物化与 effect/fallback 修正 → 3.1 方法声明与作用域 → 3.2 跨方法 origin/按需成员交接**，再继续 2.3/2.4 的复杂模式与异常恢复，最后完成 3.3/3.4 的语料、重编译/行为对照和发布门禁。 已完成的交接、建包、CLI 与选型不重复执行。**该路线已走完**：1.3d→3.1→3.2 与 2.3/2.4 均交付，3.3 的可重放编译/执行对照已入 CI，3.4 的门禁与文档同步已完成；仍未做的三处能力边界（`MethodParameters` 未读、类级事实不在载荷、canonical「handler 入口即根」的取舍）已在验证记录与本文件 3.4 子注写明，不随本 change 归档而消失。

- [x] 1.1 定义 RecoveryProfile、模式前置条件、rule version、representation、quality、compile_status、semantic_validation、verification 和 fallback 类型；明确由 `jarde-jvm` 拥有、恢复侧消费的只读 method/CFG/value/effect/origin 输入，不公开全部可变中端或反向依赖门面；明确请求内 IR 载荷的所有权/生命周期与阶段有效性（已通过 MethodIr 交出同次运行的真实表，report 与载荷分开），不重复运行 P2 或重建语义；用真实 P2 结果消费与未满足前置条件 fixture 验证边界和不误识别
  - 已落地（2026-09-19）：只读 IR 交接本身——`jarde-jvm` 自有的 `MethodIr`（按值持有 canonical/frames/ssa 三表，`canonical()/frames()/ssa()` 只给借用、通过访问器读）、共享同一次运行的 `engine::analyze_method_ir`（同时交出 report）、`method_ir` 的再导出面与源码守卫；决定与验证记录见 design 的“1.1 的只读 IR 交接”。仍未完成：本 bullet 的 profile/前置条件/rule version/表示与验证状态/fallback 类型，以及未满足前置条件 fixture 的边界验证。
  - **1.1 已全部落地（2026-09-19，收尾于 1.3c `2d156d8`）**：剩余四项类型已在 `jarde-java/src/pass.rs` 定义——`RecoveryProfile` **复用** `jarde_reader::view::RuntimeProfile`（规格句点名的就是它，另立枚举即同一事实的第二种拼写）；`RuleVersion`（`straight@1`/`if@1`/`loop@1`/`switch@1`）落进报告（`RegionRecord.rule` 记**谁产出或谁拒绝**、`RecoveryReport.rules`/`profile`）；`Precondition{IrTable, StatementFree, Metadata}` 类型化 + 编译期常量表 `PASSES`（无 trait、无动态注册）；失败 fallback 收敛为 `FallbackReason::UnmetPrecondition{pass, requirement, block_bci, at}`（`debug_assert!` 让声明与检查点漂移在本 build 测试里失败）。任务文本最后那条验证——**未满足前置条件的 fixture**——由 `a_loop_whose_header_writes_state_is_quoted_rather_than_hoisted` 覆盖：文本无 `while`、只有 `// @bytecode`，记录与诊断给出 `loop@1`+`StatementFree`+`BCI 5`，`Mixed`/`Fallback`/`execution=Complete`。**父级独立证伪**：把前置条件检查改为恒 `Ok(())` → 该用例红，且失败输出正是「header 每轮写被丢掉、结构却看起来对」的 `while (local1 != 0) {}`。**如实记录**：4 条规则的 `required_release` 全为 `None`，`admits` 的拒绝分支在 2.x 的 Java 8 专属规则前**无生产实例**，仅合同用例覆盖谓词（8/9 接受、7 拒绝）。
  - 已落地（2026-09-19，续）：上一条里的**产物词汇**（表示与验证状态类型）——按 P3 规格句实际写下的取值把平面扩到可表达：`Representation` 加 `Java`/`Mixed`、`Quality` 加 `Structured`、`SyntaxStatus` 加 `Checked`/`Unchecked`、`CompileStatus` 加 `Compiles`/`Failed`、`verification`（类型是 `jarde-reader` 的 `VerificationStatus`，故加在那里）加 `Performed`/`Failed`；`SemanticValidation` 三值已够用（`FixtureDifferential` 即 P2 为受控对照预留的值）。每条新增变体的文档写明它属哪个阶段、P2 为何不产出它；取值判定逐句取自 `recovery-validation` 的平面句与架构 §13.1 输出表（`Structured` 是 **quality**，不是 representation；`Java` 是 representation，不是 syntax_status）。P2 的产出、既有变体名、serde 形状与既有断言零改动。证据：`tests/p3_product_vocabulary.rs`（命名/往返 + 六平面穷尽性对照）与 design 的两组证伪。
  - 1.3 交付（不在本片）：`RecoveryProfile`、模式前置条件、rule version、失败 fallback。依据：本 change 决策 1「每个**模式 pass** 声明所需 IR/effect/metadata、输出节点、rule version 和失败 fallback」，而 `layer-jarde-crates` design 写明 `jarde-java`「随第一个真实 Region→AST→文本闭环」在 1.3 创建——声明随模式 pass 走。因此本片不建空壳、不预置跨层抽象。（**1.3c 已落地**：`jarde_java::pass` 给出 `RecoveryProfile`（**复用** `jarde_reader::view::RuntimeProfile`，见下）、`RuleVersion`、`Precondition{IrTable,StatementFree,Metadata}`、`Pass`/`PASSES` 编译期表与 `FallbackReason::UnmetPrecondition`；实例是循环 pass 的 `StatementFree` 门控。）
  - 1.1 剩余未满足项（如实记录）：「用真实 P2 结果消费」由 1.1a 的 `tests/p3_method_ir.rs` 覆盖（真实 fixture、真实入口、具体数字）；「未满足前置条件 fixture 的边界与不误识别」依赖前置条件类型本身，故随 1.3 的模式 pass 一并验证，本片未做这一半。（**1.3c 已补上这半条**：`a_loop_whose_header_writes_state_is_quoted_rather_than_hoisted` 断言未满足的 `loop@1` + `StatementFree` 产出的是被陈述的 fallback + 诊断（`jre_region_unmet_precondition`、文本只有 `// @bytecode`、无 `while`）而不是「碰巧看起来对的结构」，并由证伪 ②（让检查点未满足时仍返回 `Ok(())`）证明该断言有牙。1.1 的其余部分仍按原样保留。）
- [x] 1.2 评估可用 Rust Java AST/formatter/文档组合库并选择通过 origin、转义和输出预算准入的最小实现；在内部划清正常流图视图、异常事实、Region 与 Java 输出的职责，沿用 petgraph，避免新增通用 backend trait；验证各输出质量/表示可独立表达（2026-09-19 完成。**结论**：**没有**任何现存 Rust 库同时满足六条准入——决定性的是 ③origin 与 ⑤输出预算：所有候选的发射 API 都是「一次调用 → `String`」，而决策 3 要求映射与文本**同时产生、不得事后从文本反推**，故节点级位置只能在渲染时记录，发射器必须是我们自己的。**决定 Route A**：`jarde-java` 私有模块内自研**最小** Java 语句/表达式 AST + **记录偏移的 emitter**，**0 新依赖**，继续复用 petgraph 与既有 classfile/ZIP；代价是我们承担决策 2 那个可证明子集的词法与版式，**明确不覆盖** lambda/拼接/TWR/内部类/注解完整面（2.x）。**探针**（`/tmp`，std-only，未入库；父级复跑 **PROBE OK**，`probe.rs` sha256 `43db4815…698b7598`）：12 段/71 字节的段表、字段节点区间恰为 `other.count` 的 primary+derived origin、同一 BCI 的表达式段与语句段各保留不同区间、`limit=65` 时在 **bci 20 节点内部**停止并丢弃缓冲、9 条转义向量（含 `😀`→`\ud83d\ude00`、`int`→确定性别名）。**四者职责与数据流**（正常流图视图 / 异常事实 / Region / Java 输出）已写成逐层表：petgraph 只在①正常边过滤视图上用；②是事实输入不参与支配推导；③定结构不发文本；④定语法不做边发现。**不新增通用 backend trait**（一个生产者、一个消费者、一种语言）。**平面可独立表达**：`Mixed+Fallback+CompleteWithinSchema` 与 `Java+NotJava` 两组组合均可构造，类型间无构造耦合。**记录**：`design.md` 末尾新增 1.2 节（+125 行，纯增补）。**两处需父级裁决**：① 决策 5「优先复用成熟库」的前提在本生态**不成立**（准入 ③⑤ 无库满足）——本片按「能复用的复用、不能复用的不假装能复用」落地，未改契约；② 三份 P3 delta 写的 `coverage=CompleteWithinSchema` 与仓库唯一类型 `CoverageState::CompleteWithinSchema` **不一致**（前者在源码里不存在），待定夺）
- [x] 1.3 随实际实现创建 `jarde-java`，打通真实单方法入口的 P2 SSA → 普通 if/loop/switch 可证明子集 → Java AST → 文本/source map/诊断闭环，先实现 3.1/3.2 的最小稳定命名和 BCI 映射；模式未命中保留通用结构，不可约/交叉异常区域明确 bytecode fallback。以独立小图/已知 fixtures 核对分支极性、循环 header effect 次数、return 和异常优先级，并验证库/CLI 一致、jvm 不反向依赖 java、预算/取消不会变成空 body 或成功状态，覆盖 A09/A10/A13/A16（部分完成：1.3a/1.3b 已交付（`fc2d24b`、`a0954ed`），**1.3c（门面与 CLI 入口、库/CLI 一致、A09/A10/A13/A16 覆盖、1.1 遗留的恢复侧声明类型）进行中**，故本项未勾。**1.3a**：建 `crates/jarde-java`（0 新第三方包；分层门禁 12 配置全 PASS、`cargo tree -p jarde-jvm` 中 `jarde-java` 出现 0 次；`fuzz/Cargo.lock` 无需更新）；四者职责落点 = `normal_flow`(petgraph 唯一使用者)/`facts`/`region`/`ast`+`build`+`emit`；**段表**以生成文本字节区间为键、`Origin{bci,cp,Direct|Derived}`；直线 + if 闭环通，含**分支极性**、9 条转义向量、确定性命名、预算/取消/缺表**都不产空 body 不报成功**；真实语料 `add` → `return arg1 + arg2;`、`finallyPath` → Mixed/Fallback + 真实 BCI 引用。**1.3b**：**事实缝闭合**——`MethodIr` 按值携带同一次运行的 `MethodCodeFacts` + 常量池，`decode.rs` 是 opcode→Operation 的唯一映射，`RecoveryFacts` 缩到 `{method, debug_locals}`，**签名上已无参数**能交进极性/槽号/常量值（调用方给不出第二个意见）；循环/switch 实现且**循环 test 留在括号内**（`TestBlockEffect` 守卫，不搬走）；不可约/交叉异常整具身体 fallback；**独立 oracle**（自写朴素字节码机 + 文本模型 + 比较器，`include_str!` 自守卫）覆盖极性/循环 effect 次数/return/异常优先级，并**在首次运行就抓到生产 bug**（`if<cond>` 与 `if_icmp*` 合称一个变体致合法循环被拒）。**证伪**：1.3a 三组（段表错位/超限报成功/**半成品缓冲首轮全绿**→补断言）、1.3b 四组（循环 effect 上提/极性反转/**削弱 oracle 时生产全绿**/去掉不可约检查）。**证据**：全量 **867 passed / 0 failed / 1 ignored**；`jarde-java` 单元 34 + 集成 15；fmt 与 clippy 1.98.1 干净；CI run 35441081367 四 job success；**既有断言零改动**）
  - **1.3 已完成（2026-09-19，收尾 `2d156d8` + `a681e4c`）**：门面依赖 `jarde-java` 并**只导出**请求/报告半边与可命名词汇（内部模块路径引用计数 0）；CLI 新 operation（`recover_method`）**只跑一次分析**（`recover_method` 调 `analyze_method_ir` 一次，report 与载荷同源），**不另设 profile 字段**（门控读 `environment.runtime.profile`）；**A09/A10/A13/A16 逐条**给了用例与断言，并**标明哪半属后续阶段**（A09 的 finally 恢复属 2.4、A10/A13 的语料与 coverage 诊断属 3.1/3.2/3.3、A16 的逐 pass 计数器非本 build 所有——以「一次 header + 一次 body + 一条 read + 只调一次分析」为代理）。**父级独立证伪**「未满足前置条件」：把检查改为恒 `Ok(())` → 用例红，输出正是 header 写被丢掉而结构看似正确的 `while`。
  - 已落地（2026-09-19，**1.3 前半**）：`crates/jarde-java` 已创建（workspace 成员 + 根 `Cargo.toml`），依赖 `jarde-jvm`/`jarde-reader`/`petgraph`（与 jvm 同版本同 feature），**0 新第三方包**（`Cargo.lock` 只多 `jarde-java`；`fuzz/Cargo.lock` 无需更新，`cargo metadata --locked` 通过）。真实闭环已通：真实 class 字节（reader 的 class 构造器 + 已入库的 ECJ v45 fixture）→ `analyze_method_ir` → `jarde_java::recover` → **Java 文本 + 段表 + 六平面 + 诊断**。四者职责分别落在 `normal_flow`（①，只保留 `Normal`+`Return` 边的投影，不删边/不写回/不推异常）、`facts`（② 解码事实与 debug 名，`Operation` 词汇）、`region`（③ 直线与 `If{prefix, branch, then_arm, else_arm, join}`，证据不足**不产空 body**、逐条 `FallbackReason`）、`ast`+`build`+`emit`（④ 语句化规则 + 文本/段表同一批写入 + 逐写入口预算检查 + 转义）。段表载体为 `source_map::Segment{start,end,Origin{ bci, cp, provenance }}`，`cp` 本片恒 `None`（1.1 载荷不发布 CP），表按完成顺序记录、`of_bci` 可同时给出 `Direct`/`Derived` 两个节点。命名（关键字/非法→确定性别名、无 debug→`argN`/`localN`）在 `names`；转义（UTF-16 单元含代理对）+ 预算（先查后写、超限丢缓冲）在 `emit`。**验证**：workspace 846 passed / 0 failed / 1 ignored（基线 818 + 新增 28），fmt/clippy 干净，`openspec validate --all --strict` 12 passed，两个 CI example exit 0，分层门禁按 CI 口径 12 配置全绿，三组证伪（段表 +1 字节 / 超限报成功 / 超限交半成品）逐条变红。落点与事实缝见 design 的「1.3a 的实际落点与事实缝」。
  - **本片未做，留给 1.3b**：循环与 `switch`（含 header effect 次数核对）、不可约/交叉异常区域的 fallback 细化、独立小图 oracle（分支极性/循环 header/return/异常优先级）、A09/A10/A13/A16 覆盖、库/CLI 一致性（CLI 入口未接）；**事实缝**：1.1 载荷不发布符号与操作数词汇（CP 引用的 owner/name/descriptor、常量值、`ldc`/`*const*`、`*load*`/`*store*` 的槽、分支极性），本片经调用方 `RecoveryFacts`（按 BCI 键控）交入，从 class 字节与真实解码派生这些事实属驱动侧工作；本片测试用一张只覆盖所涉 opcode 的小表填充（未知 opcode → `Other`，不错分类）。

  - 已落地（2026-09-19，**1.3 中段 / 1.3b**）：① **事实缝闭合**——`MethodIr` 按值新增 `code: Option<Box<MethodCodeFacts>>` 与 `constant_pool: Vec<CpEntryFacts>`（同一次 `raw_facts` 读的字节与池，move 进载荷，无重解码/无重计费，`new()` 断言「有图必有解码」），`jarde-java` 新增 `decode::Operations::of(code, pool)` 作为 opcode → `Operation` 的**唯一**映射（含分支 `target`、switch keys、`iinc`、CP 符号），`RecoveryFacts` 缩减为「方法身份 + debug 名」并**删掉**整个 operations 面——极性/槽号/常量值只能来自同一次解码，这一点由 `RecoveryRequest` 的签名证明（没有参数能交进第二张表）。② **循环**（`while`/`for` 的 header-测与 `do … while` 的 latch-测，含自测块的一元形状）与 **switch**（`tableswitch`/`lookupswitch` → 每 target 一组、共享 target 合并、空臂 `case K: break;`），正常流视图新增 dominators/自然环/SCC 级不可约判据。③ **effect 次数与次序不变量**：条件写在循环/switch 语句内部、test 块必须纯（否则 `TestBlockEffect` fallback，不搬走）、环外分支 → `LoopLeavesEarly`、环内块全被走到（`covers()`）；`if`/`switch` 的 test 块效果按次序写在结构之前。④ **不可约 CFG 与交叉异常区域** → 整具身体 `Region::Fallback`（`jre_region_irreducible` / `jre_region_crossing_exception_regions`，消息按表序点名 primary），`representation=Mixed`、`quality=Fallback`、`execution=Complete`（不写 `Partial`、不报 `Structured`、不产空 body）。⑤ **独立小图 oracle**（`src/oracle.rs`，`#[cfg(test)]`）：自写朴素字节码机 + 文本模型 + 比较器（calls/return/test 次数三项全等），模型段由 `include_str!` 守卫证明不引用生产 helper，覆盖极性（两个 sense）、循环每轮 effect、return、异常优先级，四个自造形状，三条变异测试证明有牙。**验证**：workspace 867 passed / 0 failed / 1 ignored（1.3a 基线 847，+20 = `jarde-java` 单元 20→34 + 集成 9→15），fmt/clippy 干净，`openspec validate --all --strict` 12 passed，两个 CI example exit 0，`cargo tree` 分层门禁通过；四组证伪（含「放行 test 块 effect → 循环用例红」与「削弱 oracle → oracle 用例红」）逐条变红。落点见 design 的「1.3b 的实际落点」。oracle 在本片抓到一条生产 bug：`iflt` 家族与 `if_icmp*` 家族的 arity 混淆，已按操作数个数拆开。
  - 已落地（2026-09-19，**1.3c**）：**门面与 CLI 入口 + 库/CLI 一致 + A09/A10/A13/A16 + 模式声明类型**。① 分层链补完：root `Cargo.toml` 依赖 `jarde-java`（`jarde → jarde-java → jarde-jvm + jarde-reader`），门面**收窄**导出（恢复请求/报告/只读词汇；`region`/`ast`/`build`/`emit`/`names`/`decode`/`normal_flow` 与 `Segment`/`Origin`/`OriginSet` 一个都不在门面上），`jarde-reader/jarde-query/jarde-jvm` 仍不含 `jarde-java`。② `Engine::recover_method` 调 `analyze_method_ir` **一次**并把同一次运行的载荷交给 `recover`，返回 `RecoveredMethod{analysis,recovery}`；CLI 新增 `recover_method` operation（`environment`+`method`+`stages`，与 `analyze_method` 同形；**profile 不另设字段**——门控读 `environment.runtime.profile`），响应 `{"kind":"recover_method","analysis":…,"report":…}`，**只跑一次分析**；报告内停止仍是成功载荷（用例固定 `ssa` 缺表时的 stop + 同一次运行 analysis 侧 `execution=complete`）。③ 声明类型：`RecoveryProfile` **复用** `jarde_reader::view::RuntimeProfile`（规格句点名的就是它，新立枚举是同一事实的第二拼写）+ `Pass::admits`（4 条已注册规则全 `required_release: None`，拒绝分支待 2.x 的 Java 8 专属规则，本片用合同用例覆盖谓词而不伪造 pass）；`RuleVersion`（`straight@1`/`if@1`/`loop@1`/`switch@1`）进报告（`RegionRecord.rule`、`RecoveryReport.rules`）；`Precondition` 三族类型化 + 循环 pass 的 `StatementFree` 实例（`FallbackReason::TestBlockEffect` → `UnmetPrecondition{pass,requirement,block_bci,at}`，码 `jre_region_test_block_effect` → `jre_region_unmet_precondition`，`unmet()` 带 `debug_assert!(pass.requires(...))` 防声明/检查漂移；其余 reason 的迁移留 2.x/3.2）。④ A09/A10/A13/A16 逐条证据见 design 的 1.3c 表（A09 只覆盖「不误识别 + 正确降级」，恢复本身属 2.4；A13 的语料矩阵属 3.2；A16 无逐 pass 计数器，以一次 header/一次 body/一条 read 作代理）。**验证**：workspace **877 passed / 0 failed / 1 ignored**（1.3b 基线 867，+10）、`-p jarde-cli` **24**（基线 22，+2）、fmt/clippy（1.98.1）干净、`openspec validate --all --strict` 12 passed、两个 CI example exit 0、分层树检查通过；`Cargo.lock` 只多一行（`jarde-java` 的依赖边新增既有 `serde`，无新第三方包）。**证伪两组**：① CLI 响应改写 `quality` → 逐字段一致用例红；② 前置条件未满足时仍产出结构 → 未满足前置条件用例红。
  - **本片未做，留给 1.3c**：CLI 入口与库/CLI 一致性、A09/A10/A13/A16 覆盖、`RecoveryProfile`/模式前置条件/rule version/失败 fallback 类型、`ldc` 的 `float`/`double` 字面量、`athrow`/`try` 的呈现（2.4）、段表 `cp` 面（3.2）。（**1.3c 已交付**前三项，见上一条；`ldc` 的 `float`/`double`、`athrow`/`try`、段表 `cp` 面仍未做。）

- [x] 1.3d 修正表达式重建的值与 effect 所有权：保存读取时的 SSA 值，不能把跨 iinc/store 的旧 load 重新读成当前 local；只有确认真实消费处会渲染且保持求值顺序，才省略生产者语句，消费处 fallback 必须保留完整的生产者/effect 证据。以本轮 P3-R1/R2 的 `return x++` 与 `(String)make()` 为永久反例；保留 2.2 已关闭的简单 `return tick()` 单次调用回归，覆盖多读者、分支/循环、可能抛异常但结果未用及预算/取消。实现限于 build/相关前置条件与验证，不改 SSA 定义、不将语义缺陷降格成未执行编译（A10、A13）。（2026-09-19 完成，提交 `1e6521e`。**判据**：`slot_name_denotes_the_same_value(slot, denotes, at)`——在含**使用点** `at` 的 block 里取 `entry()` 的槽值，再按指令顺序覆盖 `bci < at` 的每次 `Local(slot)` 写入（最后写者胜），判等 `denotes`；`Load` 分支的 `denotes` = **该 load 自己 `reads()` 里的 Local 值**，`Entry`/`Phi` 分支 = 该 value id 自身；不成立即 `Err` 走 fallback，**绝不退回写槽名**，并由 `deferred_producers` 走查使拒绝同时陈述 load 的 BCI 与使用点（R2 的来源不丢原则）。**修复三类形态**（父级只诊断出 return 一类，另两类是实现者用执行对照发现的）：`return x++`（原 7 / 修前 8）、`int y = x++`（原 7 / 修前 8）、`if (x++ > 0)`（原 0 / 修前 1，**改变走哪条臂**）。**必须保持的反面**：真写后重读仍写 `return local0;`、跨块的 `loopAcross` 仍写 `return local2;`（执行 7/7 同值）。**父级独立证伪**：判据恒真 → 恰好 3 红（三类缺陷）；判据恒假 → 恰好 3 红（对照组）——真值在中间，不是一律拒绝。**fixture**：`tests/fixtures/p3-local-rewrite/` 真实 `javac 23.0.1 --release 8 -g:none` 输出，8 个成员；父级用提交源码重编译，产物**逐字节相同**（major 52、650 字节）。**R2 回归**由同一 class 的 `cast`/`make` 覆盖，防其回退。**证据**：全量 **953 passed / 0 failed / 1 ignored**；fmt/clippy 1.98.1 干净；openspec strict 12；CI 见 verification。**如实边界**：`Entry`/`Phi` 的 Local 分支无实测路径；本片只做到「不再产出错误的 Java」，**呈现**为 `x++` 属后续模式工作）

## 2. Java 8 patterns

- [x] 2.1 实现已验证的 LambdaMetafactory/method reference 和 capture 恢复，保留 bootstrap/use-site evidence 并覆盖 A04
  - **已落地（2026-09-19）**：判定链全部来自**同一次运行**的载荷——`MethodIr` 新增 `bootstrap_methods: Vec<BootstrapMethodFacts>`（按值、只读访问器、在同一次 header read 里读，无该属性的类零计费；用 `jarde-reader` 已公开的 `bootstrap_methods`，**reader 语义未改、无新依赖**）。新模块 `jarde-java/src/lambda.rs` 逐条判定：profile 准入 → 站点 bootstrap 索引在表内 → 条目句柄是 `java/lang/invoke/LambdaMetafactory.{metafactory,altMetafactory}` 的**静态**句柄 → 静态实参按种类/个数成立（`altMetafactory` 标志字必须为 0）→ 三份描述符可读且参数个数自洽 → 实现句柄是调用/构造器 → **arity 自洽**（`捕获数 + SAM 参数数 == 实现参数数 + receiver`，逐位形状相同）→ 捕获值可重读（`Precondition::Replayable`）→ 站点实例有读者。任一条不成立即**被陈述的拒绝**（`jre_lambda_*` 码 + 记录里点名 `lambda@1`），产物里既不出现 `->` 也不出现 `::`。
  - **规则与前置条件**：`pass.rs` 新增 `LAMBDA`（`lambda@1`，`required_release: Some(8)`）——本 build 第一条 Java 8 专属规则，1.3c 记录的「`admits` 拒绝分支无生产实例」由此关闭（Java 7 profile 用例走真实门控）；新增效果族 `Precondition::Replayable`；`Precondition::IrTable` 的文档改为分两种检查点（整趟必需 vs 规则认领时），因为不含 bootstrap 表的类是普通的类。
  - **呈现**：**lambda**（`(params) -> Impl(captures…, params…)`，实例/构造器句柄各写自己的调用形状）与 **method reference**（`receiver::name` / `Type::name` / `Type::new`，仅当捕获恰好等于句柄自己要的接收者）分开写；两者之别由「实现句柄是不是本站点自己的 body」决定——**标记只选写法，绝不决定是不是 lambda**。参数个数与顺序取自 SAM 的 `instantiatedMethodType`，参数名经 `names::free_name` 派生并避免与局部量/别的 lambda 参数重名（JLS 6.4）。
  - **evidence 读回**：报告新增 `RecoveryReport.lambdas: Vec<LambdaRecord>`（presented 与 refused 都在）：`use_site`/`site_cp`/`bootstrap_index`/`bootstrap`(含 `REF_kind`)/`bootstrap_arguments`/`sam_name`/`sam_descriptor`/`sam_method_type`/`instantiated_method_type`/`implementation`/`captures`(按站点顺序，各带来源 BCI)/`form`/`refusal{code,rule,requirement,message}`；`rules` 含 `lambda@1`，拒绝另有 `jre_lambda_*` 诊断。段表用既有 provenance：lambda 节点 primary = 站点 BCI（`cp = site_cp`），每个捕获值作为 **derived** 锚点挂上，捕获表达式自己仍锚在其产出 BCI——**一个表达式对应多个原始 BCI**。
  - **独立对照**：`oracle.rs` 扩成自写 class 读取（池 + `BootstrapMethods`，未知 tag 直接 panic）+ 自写机器（栈上带值来源，自己读出站点按什么顺序从哪些槽捕获）+ 自写文本解析与比较器；**槽名由 fixture 的 store 顺序与文本赋值顺序配对得出，不依赖生产命名规则**。边界如实：模型不执行 lambda 体，只检查创建、调用次数与形状。**验证**：workspace **894 passed / 0 failed / 1 ignored**（基线 877，+17 = 单元 37→43（lambda 4 + oracle 2）+ 集成 18→29（2.1 用例 11））；fmt/clippy（1.98.1）干净；`openspec validate --all --strict` 12 passed；两个 CI example exit 0；分层树 0 次；`cargo metadata --locked`/`cargo deny … --locked`（仓库根执行）通过（**未触及依赖边**）。**证伪三组**（`/tmp` 副本 + 独立 target，`sha256sum -c` 确认仓库未动，用完删除）：① 工厂检查改恒真 → **只有** A04 反例红，且产物真的写出了「像 lambda 的东西」；② 捕获顺序倒过来 → 集成顺序用例红 + oracle 正例与变异用例红；③ 削弱 oracle（只比个数）→ oracle 变异用例红而**生产全绿**。**既有断言修正** 8 条（`PASSES` 计数/规则表/`required_release` 逐条/`IrTable` 集合与 `pass("lambda")`、`Precondition::IrTable` 文档、`Type` 的四族说明、`value_type` 的描述符外框、oracle 的裸 `return`）逐条记在 design 的 2.1 节，**无放宽**。**本片未做**：适配类站点（子类型/装箱/加宽，一律拒绝）、`altMetafactory` 非零标志位、段表 `cp` 面与语料级矩阵（3.1/3.2/3.3）。
- [x] 2.2 实现 StringBuilder/StringBuffer concat、bridge 和 synthetic accessor 派生呈现，验证 A12 的双 origin 边（`492e31e`/`51a5cac`。历史验收保留；accessor 的实际呈现依赖低层 API 的 ClassMembers，门面未交成员表，不能称为公开 recover_method 已支持 accessor；公开交接和跨方法身份分别由 3.1/3.2 闭合）
- [x] 2.3 实现 inner/local/anonymous、enum/enum switch、default/static interface 和构造器/字段初始化模式（2026-09-20 勾选；实现与测试见 `068a4a8`，**父级独立复核已做**。**四块的实际边界**：`new@1` 认领 `new C ─ dup ─ [参数生产者] ─ invokespecial C.<init>` 且要求同块、接收者由本站点产出、区间无 effect，**但不声称嵌套**（载荷无 `InnerClasses`/`EnclosingMethod`，故不写 `Outer.this`、不写嵌套语法——文档化的边界，非疏漏）；`enumswitch@1` 认领**那次读**（静态 `[I` 字段 + 描述符 `()I` 的实例调用作下标），**不写 `case T.CONST:`**（常量名是类级事实、映射内容在合成类的静态初始化里，都不在本次运行；字段名 `$SwitchMap$…` 只是约定，与 `lambda$`/`access$` 同纪律）；`declaration@1` 靠 caller-stated 的类级 `ACC_INTERFACE` + 成员 flags（**3.1 之后成员的 flags 已从载荷来**，仅类的 flags 仍是调用方给的，见 `c5fead9` 的文档更正）；`field@1` 要求**帧给出的接收者静态类型恰等于池写的 owner**（否则 `receiver.f` 可能指到子类同名字段），`init@1` 要求接收者是 `UninitializedThis` 且按 JVMS 4.9.2。**父级复核结论**：四个判定链逐个读过，`enumswitch@1` 的形状判定（静态 `int[]` + 实例调用返回 `int`）尤其严；构造器顺序（`super()` → 实例字段 → 静态 → `return`）有具体断言；边界都用**结构性理由**写在 design 里（§2.3 的第 2/3/4/5/6 条），不是偏好。**与 spec 的一处冲突已由 R6 修复**（slot 复用，见 verification 的 P3-R6 段与本文件 3.1 子注）；本片自身的 slot-reuse 断言在处理 R6 时已按 spec 改写。**如实边界**：类级事实（嵌套/接口标志）不在载荷，门面级运行对 `declaration@1`/`accessor@1` 给出**被陈述的拒绝**；`new@1` 的 boolean 实参写成 `0/1`（发现 (i)，已记录待修）；`jsr`/`ret` 体仍整段 fallback）
- [x] 2.4 实现 try-with-resources、synchronized/finally 的异常/effect 保真恢复，覆盖 A09（2026-09-20 完成，提交 `d4c3901`。**TWR（`twr@1`）**：逐条证明资源初始化区间、正常路径 close（指令级 + CFG + **close 接收者与 `aload` 产出的同一 SSA 值**）、**close 顺序=声明逆序**、handler 覆盖与 catch 类型（`catch_type_index` 不放宽）、**`addSuppressed` 的接收者是 primary 且参数是 close 的异常**（两个 load 的顺序即证明）、`athrow` 重抛 primary；证明读过的 BCI 全进 derived origin。**synchronized（`monitor@1`）**：恰一个 `monitorenter`、恰两个 `monitorexit`（正常 + handler）、handler 的 exit 自覆盖；异常路径缺 exit / 第二入口 / 部分路径 exit 一律拒绝。**finally：如实拒绝**（`jre_guard_finally_copy`、`rule: None`）——javac 生成的是**复制**，并成一份只在两份可证等价且每条 exit 恰跑一份时才忠实，本 build 不证明它（**执行次数正是风险所在**）；`jsr`/`ret` 既有 Mixed/Fallback 行为未变。**P3-R5 关闭**：参数类型取自方法自己的 descriptor；`boolean` 参数写 `if (b)`；**父级独立验收**——自行包成 `public static int scope(boolean b)` 后 `javac --release 8` **exit 0**（此前同包装编译不过）、执行同值。**父级独立证伪**：close 顺序不逆序 → 2 红；suppressed 关系不检查 → **恰好 1 红且失败输出 `left: Java / right: Mixed`**（反向抑制被错误呈现）。**证据**：982 passed / 0 failed / 1 ignored；执行对照 TWR/monitor 7 个成员产物与原成员 **IDENTICAL**（含逆序 close 与 suppressed 链）；fmt/clippy 1.98.1 干净；openspec strict 12。**未完成**：可重放的编译/执行门禁属 3.3；新公开面文档属 3.4；builder 内部引用不进 diagnostics 的既有缺口已点名）

## 3. Names, maps and release gate

- [x] 3.1 先闭合方法声明事实与作用域：从同次 Header 读取保留 access_flags、descriptor、receiver/参数槽、debug 范围与物理身份，门面不再固定 parameters=0，也不为命名二次解码 Body；依据 Region/SSA 定义与使用决定声明的有效作用域，关闭 P3-R3 的 then 内声明却在 else/合流后使用反例，覆盖 slot 复用、category-2 参数和无 debug。再补关键字/别名/冲突与稳定命名；3.2 的按需 accessor 读取使用同一身份/预算契约，但不等同于只读取当前声明（A10、A16）。（2026-09-19 完成，提交 `f7f90b7`。**声明事实**：`MethodIr` 新增私有 `declaration: Option<Box<MethodDeclaration>>`（access_flags/name/descriptor/parameter_slots/identity），取自 `read_own_definition(…, DriverMethodBody)` 的**同一次** header 读取；门面 `recovery_facts` 不再固定 `parameters=0`，并从**同一个 Code 条目内部**读 LVT 供命名（**无第二次 Body 解码、无新增计费**——父级核对改前/改后 usage 逐字段相同）。**R3 已关闭**：判据 = 求包含该槽全部使用的**最内层 Region**，首次写入就在其中则保持 `int x = <value>;`，否则在 R 开头写无初值 `int x;` 并把所有写入改为赋值。**父级独立验收**：自行包成 `static int scope(int b)` 并用 `javac --release 8` 编译 **exit 0**、执行对照 `b=1→1/1`、`b=0→2/2`（与原方法同值）；修前报 `找不到符号: 变量 local1` 于 else 与 return 两处。**三类覆盖**：slot 复用（一个槽一个变量；LVT 两次命名同槽 → 如实无名字）、category-2（`long` 占两槽、参数绝不作局部声明）、无 debug（确定性序号名）。**父级独立证伪**：no-op 提升 → 4 红（含 R3）；一律提升 → 2 红（正面用例）——真值在中间。**证据**：960 passed / 0 failed / 1 ignored；fmt/clippy 1.98.1 干净；openspec strict 12；CI 见 verification；fixture 用提交源码重编译**逐字节相同**。**如实边界**：`MethodParameters` 未读（方法级属性，需新计费口径）；参数类型仍来自 frames，故 `boolean` 参数写成 `b != 0`——**复核本就排除此项**（其 R3 原文要求用 `int local0` 包装）；仓库无 `ACC_BRIDGE` 样例，门面→bridge 无端到端 fixture）
- [x] 3.2 扩展现有 source map 为带物理方法身份的 anchor（复用 reader 身份/OriginMember；BCI/CP 必须归属于明确的方法/定义），覆盖 caller/callee 相同 BCI、两个 accessor 相同字段 BCI、canonical clone 和 CP/attribute，保留 Direct/Derived。由 JVM 事实层按实际 accessor 候选提供有 reason、有预算、绑定同一物理定义/CP 的只读 callee 证据，接通门面/CLI 的成功呈现与拒绝；不能预装全类所有 Body 或把任意外部 ClassMembers 的同名声明视为同源。用“实际生成直接字段表达式 + X1 两条原始边逐项不变”的同次公开入口用例验收 A12；补 Mixed 区域映射/诊断，完整扫描下的 Fallback 不改成 Partial（A12、A13、A16）。（2026-09-19 完成，提交 `4488170`。**锚点**：`Origin` 扩为 `{bci, method: Option<Box<PhysicalMethodId>>, cp, provenance}`，`member()` 直接给出 reader 的 `OriginMember::MethodPoint`（**复用既有身份，未新立同义体系**），`Direct`/`Derived` 一字未改；覆盖 caller/callee **同号 BCI**、两个 callee 同字段 BCI 不同成员、**canonical clone**（引文坐标集合 == 锚点集合）、**CP/attribute**。**callee 证据**：新 `jarde-jvm/src/callee.rs` 产出 **jvm 自己的**只读类型，由**门面**适配成 `ClassMembers`（`ClassMembers` 在 java 侧，jvm 造不出来）；候选来自**被呈现体自己的 decode** 的 `invokestatic`，谓词与 `accessor@1` **共用同一函数**；先扣费 `MethodBodies` + attribute/code 字节，有 `ReadReason::CalleeMemberBody`；**绑定同一物理定义**（owner 与 `this_class` 不同即 `callee_not_this_class`）。**A12 同次公开入口**：产物 `return arg0.g + arg0.h;`（直接字段表达式，无 `access$200(`/`access$300(`）；`method_bodies == 3` 而 fixture 声明 **6** 个带体成员（**按需**，`access$400` 未读）；`analysis.reads.len() == 1` 不变；X1 两条边逐字段相等且两 item 互不相等。**拒绝半边**：外部类同名同描述符成员不被当同源。**Mixed**：引文锚点集合 == 引文 BCI 集合；`CompleteWithinSchema` 与 `Fallback` 并存（**扫描完整不被改写成 Partial**）。**父级独立证伪**：放松物理定义绑定 → **恰好 1 红**（同名外部成员被当同源），证明绑定承重。**证据**：968 passed / 0 failed / 1 ignored；fmt/clippy 1.98.1 干净；openspec strict 12；CI 见 verification。**未完成**：`CalleeBody` 线上摘要与若干新公开面的文档属 3.4；本片未做独立 review）
- [x] 3.3 建立多代 javac/ECJ、缺失 debug、混淆、缺失依赖和非 Java compiler 语料；先把 P3-R1/R2/R3 纳入可重放的实际 Java 8 编译/执行对照，再扩展矩阵。比较返回值、调用次数/顺序与异常，不能只解析文本形状或共享生产端的 opcode/命名假设；编译方法体时声明包装必须来自已验证的方法事实，记录不能独立成 compilation unit 的边界。（2026-09-20 完成，提交 `04cf83a`。**可重放对照**：`tests/p3_execution_comparison.rs` 两个 `#[ignore]` 用例——走**公开入口**取产物，**包装由事实派生**（类型来自参数类型事实并**与描述符交叉核对**、名字来自 debug 表、修饰符来自 access_flags，**无手写类型**），编译后在原成员旁执行，比**返回值 + 事件顺序 + 计数次数 + 异常（含 suppressed）**；实跑 `2 passed`。**不带 `--ignored` 的 `cargo test` 永不执行它**（无 JDK 的机器仍绿）。CI **add-only 一处**（唯一例外，父级逐行核过）。**语料**：同编译器五种合法 flag（`--release 8 -g:none`/`-g`/`-g:lines,source`/`-parameters`/`-source 8 -target 8`）+ ECJ 4.6.1 v52 产物 + 缺失依赖；**记录到的事实**：`--release 8 -g:none` 与 `-source 8 -target 8 -g:none` 产出**逐字节相同**（两个样本各自断言）。**如实记录不可得**：真混淆、非 Java 编译器、旧版 javac（本机只有 23.0.1，且不能运行 ECJ）。**边界**：6 类不能独立成 compilation unit 的原因逐条记录（引用型 Mixed、`<init>`/`<clinit>` 无包装、缺失类型、`new@1` 的实参侧 boolean 见发现 i）。**父级独立核对**：982 passed / 0 failed / **3 ignored**（ignored 由 1→3）；`--ignored` 直跑 `2 passed`；CI diff 为 add-only。**两处待裁决**：**(i)** `new@1` 把 boolean 实参写成 `0/1`（R5 的实参侧对偶）；**(ii) P3-R7** ECJ v52 `finallyPath` 的 handler 未被交代）
- [x] 3.4 同步五维 Java 8 支持矩阵、公开库/CLI 文档与归档链接；先修正 RecoveryReport 确定性比较中的 elapsed_millis 假红（本轮 P3-R4），递归仅剔除耗时，保留全部确定字段/顺序和预算断言，做反转顺序等证伪。验证恢复层已存在时的 A16/A17 与两个 workspace 锁/依赖门禁，执行 fmt、clippy、test、适用 oracle/MSRV/supply-chain/fuzz 与 OpenSpec strict，记录精确提交/CI；P3 所有出口通过再同步 delta/归档。（2026-09-20 完成。**A17 真实隔离回归**：(a) 源码守卫扩到恢复层——token 由 `crates/jarde-java/src/**` 的**公开类型声明自动派生**（82 个）再减去共享层已声明名（769 个）得 **75** 个禁用 token，**无手写名单**，并保留非空洞性检查与阳性对照（`src/facade.rs` 必须被命中且不在受守卫集）；**性质如实**：两处依赖边都被 cargo 以 `cyclic package dependency` **解析期拒绝**，故这是对**未来重排**的守卫，是结构性强制的**补充而非替代**（父级独立证伪：在 `query.rs` 注入 `jarde_java::`/`RecoveryReport`/`Region` → 两个守卫用例红）。(b) 运行期隔离回归 `tests/p3_isolation.rs`：同一 v45 fixture（真有两个 `jsr` 入口）——`Engine::query` 的 `ir_items`/`ir_edges`/`analysis_steps`/`normalization_clones` **全 0** 且 header/body 全 0，`Engine::recover_method` 四维**全 >0**（322/19/107/2）且各 1；文件头明写局限：Region/AST 仍无逐构造计数器，这是「两趟必经计费全缺席」而非点名它们的计数器。（父级独立证伪：让 query 路径 charge 一个 IR 维 → 该用例红。）**文档同步**（三份，逐字匹配替换成功）：`docs/support-matrix.md` 的质量行/`output-level` 行/P3 边界段；`README.md` 当前状态整段（2026-09-20、**P3 12/12**、六项缺陷关闭、可重放对照）；`openspec/acceptance.md` 的 A17 行改为已完成 + A16 行补 P3 三处锚点（另修正 P2 归档后过时的链接路径）。**最终门禁**：全量 **990 passed / 0 failed / 3 ignored**；`--ignored` 对照 2 passed；fmt/clippy 1.98.1 干净；**MSRV 1.88.0 check 通过**；两套 `cargo deny` 全 ok + `fuzz/Cargo.lock --locked` 通过；fuzz workspace 21 passed；**三个 fuzz target 各 25s 冒烟**（query 226,984 / artifact_tree 375,820 / method_analysis 316,241 execs，零 crash/timeout，tracked 语料未被写）；OpenSpec strict **12 passed**；两个 CI example exit 0；分层不含 `jarde-java`；`git diff --check` 干净。**未做**：`MethodParameters` 未读、类级事实不在载荷、canonical「handler 入口即根」的取舍待裁决（三项已在文档写明））
