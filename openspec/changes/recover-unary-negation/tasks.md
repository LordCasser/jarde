## 1. 固定失败证据

- [x] 1.1 新增独立自写 Java 8 fixture，覆盖四种取负、窄类型提升、嵌套与二元分组、返回/局部/调用参数、带计数或异常的调用和无法恢复的操作数；记录 javac 版本与命令。修前测试须证明四种直接取负仍是缺口。

## 2. 补齐一元值

- [x] 2.1 按 design 添加取负事实与 AST，复用值递归、来源和数值类型；相关直接消费列表只接纳这一操作。验证四 opcode 解码和四类型恢复测试通过，不新增 JVM/区域机制。
- [x] 2.2 发射一元负号，维护优先级、词法分组、预算和子表达式来源。验证 `-(-x)`、`-(x+y)`、乘除右操作数、负字面量，以及来源 BCI、拒绝生产者保留的测试通过。

## 3. 三方对照与验收

- [x] 3.1 保存同一 fixture 的源码、jadx 1.5.6 和 jarde 输出摘录与结论；对恢复文本重编译并执行整数边界、浮点正负零/无穷/NaN 分类、调用次数/异常对照。以可重放测试和 verification.md 记录结果。
- [ ] 3.2 主代理审查改动，复跑取负和相邻表达式/拒绝/预算回归；更新 fixture census/fingerprint，执行 fmt、受影响包 clippy 与 OpenSpec strict validation。仅根据实测勾选，并将原 2c.11 指向本交付。
