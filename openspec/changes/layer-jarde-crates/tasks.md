当前状态（2026-09-18）：**1.1–3.3 全部完成并勾选**（基线 `0f3134b` → 收口 `62d76bc`）。三次抽取（reader/query/jvm）与根门面已落地；集成与门禁经独立复核 **Approve**。复核期间发现并修掉的两处门禁缺口各自有证伪：可选 feature 绕过（旧配置 exit 0 → 新配置 exit 1 并指名 `jarde-query-normal-all-features`）、以及「红」曾隐式依赖 runner 的 `bash -e`（去掉显式 `set -euo pipefail` 后普通 bash 对早期违规仍 exit 0）。历史验收只证明拆分前后行为保持，**不证明语义正确**：新发现的 returnAddress 值证明缺口（R7）由 P2 3.4b 修正，不混入本 change。

## 1. Baseline and reader

- [x] 1.1 确认 P2 `0.3/0.3b/3.4` 及其依赖已验收，保存精确基线、测试结果和生产依赖树；按 design 盘点跨包私有访问及测试辅助消费者，清单中逐项注明所有者与最小公开面，未绿不搬迁（2026-09-18 **分析部分完成**：基线 `0f3134b`、628 passed / 0 failed / 1 ignored、生产依赖树与 19 个待拆模块已记入 verification 的 1.1；盘点 185 个 `pub(crate)` 项（140 个跨文件引用），按「接缝 / jvm 内部 / 无需公开」分类，产出 10 条接缝归属表、7 项**搬迁前必须处理**的编译失败项、门面必须迁入 jvm 的清单，见 1.2；三处 design 未定项当时定案（blake3 收敛决定已在本轮 §3.5 撤回，另外两项为 `CandidateFilter` 不整体公开、`read_entry_internal` 改名，见 design §3.5）。当时尚未做 7 项前置动作与搬迁；后续在 1.2/2.1/2.2 已执行，具体证据见 verification）
- [x] 1.2 抽出 `jarde-reader` 及检查入口，将共用 budget/error/model/view 与唯一解码留在该包，提供有界物化和只读 facts 接缝；验证 reader 独立 check/test、快照/身份/字符串/操作数/精确预算边界与现有 P0 oracle、P1 golden 不变，确认无向 query/jvm/facade 的依赖（2026-09-18 完成并**独立复核 Approve**：新包 `crates/jarde-reader/`（7 模块 + `inspect.rs` + `test_fixtures.rs`），根包只留 query/jvm/门面；`cargo test -p jarde-reader` = 126 独立通过，其 normal 依赖树无 petgraph/query/jvm/门面（编译器强制，复核者自测反例 `E0432`）；全仓 628 passed / 0 failed / 1 ignored 与基线一致；`p1_xref_golden` 5；可见性按盘点收敛、`operands` 仍私有；据复核修掉 `with_usage` 的第 4 份副本与门面 `jarde::with_usage` 的公开可达；fuzz lock 同步（只新增包、第三方零变动）。CI：搬迁本体 run 35346239122 红（fuzz lock）→ 修复后 35346754502、35347094952 全绿）

## 2. Query and JVM ownership

- [x] 2.1 抽出 `jarde-query` 的 query+xref 与最小 candidate scan 接口，同步 resolver 调用而不改变其语义；运行只依赖 query 的实际查询 consumer，独立 check/test、分页/coverage/cancel 回归及声明引用对照通过，生产和测试依赖均无 jvm/facade/petgraph（2026-09-18 完成并**独立复核 Approve**：新包 `crates/jarde-query/`（query + xref）；`cargo test -p jarde-query` = 3 独立通过，normal 与 all 边依赖树均无 petgraph/jvm/门面（编译器强制）；全仓 628 passed / 0 failed / 1 ignored；golden 5、`p2_contracts` 29；**`CandidateFilter` 最小面**：内部 `CandidateRule` 保持 crate 内，公开枚举只含两种候选形状，`Exact` 无公开拼法（`From` 的无通配 match 由编译器保证穷尽）；搬迁无语义改动（归一化 diff 逐字等价）；fuzz lock 只新增包、第三方零变动。**必须带进 2.2**：本片使 `jarde::{scan_candidates, CandidateScan, CandidateFilter, execute}` 从门面可达（搬迁前为 `pub(crate)`），2.2 须收窄白名单。CI run 35351589131 四 job 全绿）
- [x] 2.2 抽出 `jarde-jvm` 的运行环境、resolver、CFG/call-context/pass/IR 及方法分析 driver；门面只保留委托与选定再导出，用当前全部 P2 反例验证阶段/预算/身份/原 BCI 一致，不为搬迁公开 HeaderClosure、FactLedger 等可变内部结构（2026-09-18 完成并**独立复核 Approve**：新包 `crates/jarde-jvm/`（9 模块 + driver + 自己的 test_fixtures），根包 `src/` 只剩 `lib.rs`+`facade.rs`，`Engine` 各入口一行委托；**零新增 `pub`**，`HeaderClosure`/`FactLedger`/`AnalysisRun`/`IrPhase`/`PassDescriptor`/`CallContexts` 在 jvm 外均不可达（探针实证，`AnalysisRun` 为 E0603「名字在但无公开路径」）；`cargo test -p jarde-jvm` = 95；依赖树不含根包（复核者自测反例）；全仓 628 passed / 0 failed / 1 ignored（连跑 4 次）；golden 5、`p2_contracts` 29；两个 example 输出与基线逐字节相同；**门面收窄**：2.1 遗留的 `execute`/`scan_candidates`/`CandidateScan`/`CandidateFilter` 及 `query`/`xref` 模块路径已不可达；`classfile` 模块路径仍可达；**driver 逐字等价**（`run_method_analysis` 等 diff 0 行，旧 13 个顶层项全部存在）；偏离 A（`engine.rs`→`facade.rs`，因守卫 exactly-one 断言）与 C（三个自由函数路径随 glob 移除）已申报；同提交顺带修掉既有 flaky（`elapsed_millis` 归一化，复核判定未削弱断言）；CI run 35354728115 四 job 全绿。**2.2 时点的承重结论**：`classfile` 55 名单只是表面收窄，模块路径仍在——真正的收窄是删除 `query`/`xref` 模块路径（见 verification 2.2））

## 3. Integration and gates

- [x] 3.1 同步根包/CLI/examples、跨包单元测试辅助、fixtures 路径、fuzz path/lock 和 CI 包选择；第三方版本/features 不升级，验证现有 enumerate/inspect/query 的库/CLI 同请求逐字段一致（新增 method CLI 留 P2 5.1）（沿用 elapsed_millis 归一）、fuzz targets 可构建且固定 replay 通过，不复制生产 decoder 或新增空 java/common crate（2026-09-18 完成并**独立复核 Approve**：测试注释里的失效源码路径全部更新（含清单外同类 4 处）；fixtures 审计确认本体只有一份、reader/jvm 各持一模块、query/cli 不读；CI 包选择经复核者实测 `jvm` 边界正则不误命中 `jarde-jvm`（非空性反证：人造 `jvm` 行会命中）；库/CLI 逐字段一致 = `cargo test -p jarde-cli` 19 passed（`elapsed_millis` 归一在两侧测试内）；**门面进一步收窄**：移除 `jarde::classfile` 模块路径，复核者用 rustc `--extern` 探针确认名单内名字经该路径也报 `E0433`，故收窄的是模块路径本身；债务：`--all-features` 下 `jarde::MethodCodeFacts::from_parts` 仍可达（名单内类型上的 `test-support` 门控构造器，无 normal 依赖启用）。**本记录一处失真已更正**：不是「三个新包各自有 test_fixtures」，实际只有 reader/jvm 两份）
- [x] 3.2 更新 A17 的源码路径守卫并增加 Cargo 依赖闭包门禁；以临时加入 query→jvm 依赖、向 query 注入图分析调用的反例证明门禁会失败，用 `--all-features --edges normal` 及 `--all-features --edges all` 检查生产与 dev/build 闭包（reader/query 禁止上层及 petgraph，含未来 jarde-java），记录 Cargo 构图失败时的拒绝；同时保留 X0/X1 不启动分析、单方法不读无关 Body 的既有行为侧证据（完整构造计数仍归 P2 5.2）（A14/A16/A17）（2026-09-18 完成并**独立复核 Approve**：A17 守卫在 1.5 已布局无关，本片只同步注释路径；新增 CI 步骤 `Check layered crate dependency closure`，三层 × {normal,all} 边 × {默认,--all-features} = **12 项检查**，禁名含未来 `jarde-java`；**失败路径不可吞掉**三条（`cargo tree` 非零 / 闭包为空 / 禁名命中）经 fake cargo 注入验证；**两处由复核者指出并已硬化且各自证伪**：① 可选 feature 绕过（`optional` + 非默认 feature 在旧配置下不可见——旧门禁 exit 0、新门禁 exit 1 且指名 `jarde-query-normal-all-features`）；② 红是隐式依赖 runner 的 `bash -e`（去掉显式 `set -euo pipefail` 后普通 bash 对早期违规仍 exit 0）；查询→分析依赖使 workspace 不可解析时门禁报红而非吞掉工具错误；X0/X1 行为侧证据保留未削弱）
- [x] 3.3 完成 workspace fmt/clippy（-D warnings）/全量测试、P0 oracle/P1 golden/P2 回归、MSRV、两套 workspace supply-chain、既有 fuzz 门槛与 OpenSpec strict/diff 检查；记录实际命令、精确 commit/CI 和只读复核，更新架构/依赖/阶段源码路径后交接 P2 3.4b，再由其解锁 3.5，不将未执行门槛记为通过（2026-09-18 完成并复核 Approve：fmt/clippy(`-D warnings`)/全量 **628 passed / 0 failed / 1 ignored**/P1 golden 5/P2 契约 29/CLI 19/MSRV `cargo +1.88.0 check` exit 0/fuzz 语料回放 9 passed/fuzz 冒烟 198,336 与 394,614 exec 0 crash/OpenSpec strict 11 passed；**只能由 CI 覆盖故本机未跑**：P0 JDK 25 oracle（stable job 的 Temurin 25 步骤）与两套 supply-chain（需 advisory DB）；精确 commit 与 CI：`724bf1b`→35357240160、`3646a97`→35357911716、`78077e9`→35358644839、`eb1adb8`→35358993660、`62d76bc`→35359273784，均四 job success；架构/依赖/阶段源码路径已无失效引用（归档 change 的历史记录按口径保留）。**交接 P2 3.4b**）
