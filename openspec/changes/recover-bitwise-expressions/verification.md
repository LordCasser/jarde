# Verification

## Implemented slice: tasks 2.1–2.4 and 3.1

- `iand`/`land`, `ior`/`lor` and `ixor`/`lxor` now decode into one of three faithful operation facts. The existing `Binary` expression carries Java `&`, `^` and `|`; the binding table preserves their precedence and grouping. Type tests accept boolean/boolean or integral/integral operands, promote `byte`/`short`/`char` to `int`, preserve `long`, and reject mixed boolean/integer and non-integral pairs.
- Boolean evidence reads the two actual SSA operands. A `0`/`1` literal contributes no independent seed; existing descriptor evidence and already-decided locals remain the only seeds. Nested bitwise local dependencies enter the existing `decide_types` work queue and are rechecked as a complete expression when a dependency is proven. The positive fixture covers nested, copied, hoisted, passed, branch, array, and pure integer literal cases. The independent mixed-descriptor/old-local/dup-pop replay remains under `evidence/java-syntax-2026-09-22/bitwise/boundary-audit/root-replay/`; it uses the frozen CLI and JAR-verifies both inputs, and records that mixed `Z`/`I` remains a refusal.
- Rendering uses the ordered left/right SSA operands and carries the final consumer BCI into nested reads and refusal checks. The `ordered` runner confirms a false left operand still calls the right operand (`trace=12`); separate cases confirm the left exception prevents the right call and the right exception retains the left effect. `bitwise_source_uses_real_bcis_and_default_text_is_stable` checks non-empty segments for real operand, operator and consumer BCIs under the correct physical member, byte-for-byte equal default/full-evidence text, and no default source-map publication.
- The class-source stop test verifies a one-byte output bound, a partial source-map prefix under an `IrItems` bound measured from the same fixture's ordinary run, and cancellation. The JDK-only generated-class test verifies that a 32-operation value exceeds the existing value-depth limit and falls back, while a 40-local boolean dependency chain reaches a bounded fixpoint and keeps boolean declarations. No extra pass, AST layer, cache or general type solver was introduced.

## Commands and results

- `CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUST_TEST_THREADS=1 cargo test -p jarde-java --locked`: **112 passed, 0 failed** (plus the crate's integration and doc-test targets passed).
- `CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUST_TEST_THREADS=1 cargo test -p jarde --test p3_bitwise --locked`: positive fixture, source-map/default-text and budget/cancellation tests pass. The two JDK comparisons are explicitly ignored in the normal suite and were each run directly below.
- `CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUST_TEST_THREADS=1 cargo test -p jarde --test p3_bitwise deep_bitwise_values_fall_back_and_local_dependencies_reach_a_bounded_fixpoint --locked -- --ignored --nocapture`: **passed**; compiles its depth and local-chain input with `javac`.
- `CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUST_TEST_THREADS=1 cargo test -p jarde --test p3_bitwise recovered_bitwise_probe_matches_original_runtime --locked -- --ignored --nocapture`: the source recompiles to the exact frozen `BitwiseProbe.class`; original and complete `Engine::class_source` output both compile with `javac --release 8`, run under `java -Xverify:all`, and produce identical 268-line output. The original 267 lines are preserved and the added pure-integer `0`/`1` control is the 268th line.

The pre-implementation full-class JADX comparison remains the 267-line baseline in `evidence/java-syntax-2026-09-22/bitwise/final-fixture-before/`. The added integer-literal control is not attributed to that older JADX output.

## Root independent acceptance: task 3.2

Root reviewed the shared boolean proof, nested-local dependency work queue, per-node accounting and depth bound after the helper-signature refactor. It rebuilt and froze `jarde-cli` at `/tmp/jarde-cli-bitwise-root-after`, SHA-256 `88f2e7aa9b5b8172da02f5af5d4d2c029e72a52d9b3298b46b82774e0bd5fdfd`. `evidence/java-syntax-2026-09-22/bitwise/root-after/run_replay.py` recompiles the checked 1303-byte class byte-for-byte (SHA-256 `d9cc8c8809e156ad2c1ba064dbda74dd1c7a6045cb7773d84a3ad40dd2874d9b`) and compiles and runs complete original, JADX and Jarde classes under `java -Xverify:all`: all 268 lines match, and Jarde has zero bytecode quotes. `root-after/boundary/run_audit.py` independently recompiles and JVM-verifies the source and controlled mixed-descriptor/dup-pop variant; Jarde retains explicit quoted refusal for mixed `Z`/`I`, while the ordinary integer/control output remains executable. Both scripts assert the frozen CLI hash before and after.

After that refactor, root reran `jarde-java` tests (112 unit, 32 pattern, 50 recovery), `p3_bitwise` (3 ordinary and 2 JDK-only), `p3_boolean_contexts` (13), and `p3_numeric_comparison` (6 ordinary). The reader fixture census test passes at 123 classes, 896 Code bodies, 98 handler records, 340 branch/switch targets and 8 historical subroutines. The corpus fingerprint test passes for 314 files, including the frozen new fixtures. `cargo fmt --all -- --check`, `git diff --check`, and `openspec validate --all --strict` (56/56) pass. Strict `cargo clippy -p jarde-java --all-targets --locked -- -D warnings` reports only the pre-existing `region.rs:1736` `type_complexity`; with that one lint exempted, the same command passes. No new lint allowance was added to source.
