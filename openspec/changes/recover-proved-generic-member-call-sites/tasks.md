## 1. 冻结子片和反例

- [x] 1.1 冻结非泛型外层类、泛型成员类及独立调用方的 Java 8 `-g`/`-g:none` class、哈希和 `javap`；分别记录原/JADX 的源码形态、只重编调用方的结果、正常与 null 路径的 `-Xverify:all` 轨迹，并以证据脚本可重放为验收。
- [x] 1.2 构造错类签名、错构造器签名/擦除、缺目标、错成员关系和重载绑定的独立控制；每个能成为 verifier-valid class 的控制记录原始运行与拒绝，不能构造的控制用直接物理证明测试明确证据等级。

## 2. 受限泛型目标证明

- [x] 2.1 复用已选 target/outer 及 reader 签名解析，在已证明合成外层首参后核对目标类自己的 `V`、简单界和物理类头擦除；错变量、外层泛型、重复/缺失签名与预算/取消测试必须拒绝或停止。
- [x] 2.2 把准确 `<init>` 的源级签名参数与物理 descriptor 尾部逐项擦除对齐，并拒绝未证重载；既有无签名成员目标保持成功，错签名和重载控制保持拒绝。

## 3. 构造 AST 与报告

- [x] 3.1 在现有 `New` 表达式加入仅由泛型目标证明设置的 diamond 拼写，输出 `outer.new Inner<>(args)`；定向测试验证完整语义类型、限定接收者分组、普通参数顺序及所有 `New` 消费者穷尽编译。
- [x] 3.2 沿既有 `new@1` 站点、SSA 身份和 null/效果时序门交接泛型证明；报告仍保留完整物理实参 BCI，essential/all/range 的正文及判定一致，普通 `new` 与非泛型成员构造回归通过。

## 4. Root 独立验收

- [x] 4.1 用冻结原始外层及成员 class 作 classpath，只重编 Jarde 调用方 Java 8 源并与原 class 对照正常/null/前置与实参效果轨迹；记录 JADX 的实际表现、源码来源、错签名和缺失目标拒绝及整类声明的独立边界。
- [x] 4.2 跑适用定向 Rust、`cargo fmt`、Clippy 与 `openspec validate --strict`，把既有共享工作树失败单独记录；清理私有 Cargo target，写 `verification-root.md` 后才勾选已真实完成的任务。
