## 1. Modern registry and facts

- [x] 1.1 定义 release-bound feature registry、preview/minor/attribute/flag 规则与 capability 输出，覆盖 53–71 和未知 future cases（2026-09-20 完成，提交 `8620249`。**新增** `crates/jarde-reader/src/release_registry.rs`，既有 `classify_version` 改为**消费**它；既有四类枚举/字段**全部保留**，既有版本矩阵等用例**一字未改即通过**（父级核：删除行中 `assert` 计数为 **0**）。**每个 release 一条记录**：记 CP tag / attribute / flag / opcode 四类**引入**的约束（各带 `since` 与 JVMS 来源），累积生效；**45–71 逐条无空洞**（表自身有不变式测试）。**来源诚实是本片的一半**：每条记录带**非空** `unregistered`（列**刻意不主张**的东西，测试强制非空），`NotRegistered`/`Unregistered` 是「**无主张**」而非「非法」。**四个平面分开**：parse=`structural_read`、dialect=`VersionCapability`、verification=`NotPerformed`、**output-level 新增 `HeaderInspection::output_level`**（本片仅 `NotEvaluated`，与 verification 对称）。**父级独立探针**证实未登记 release **绝不借用**上限 release 的能力（72/100 的 `Record`/`invokedynamic` 查询均返回 `UnregisteredRelease`），且探针非空洞（71/59 正常作答；`Record@59 → VersionNotApplicable`）。**证据**：1006 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；CI run 35467145921 四 job success；三组证伪见 verification。**边界**：不读 module/record/sealed/nestmate 事实、不把合法性接到读路径（负向用例钉住）、不改 `dialect_validation_scope`、不产 fixture——前两项属 1.2/1.3）
- [x] 1.2 实现 module/nestmate/condy/record/sealed/modern concat facts 与 origin/evidence，覆盖 A05 及 output-level conflict（2026-09-20 完成，提交 `3df96d1`。新增 `crates/jarde-reader/src/modern.rs`：record components（含组件上的嵌套 attribute shell）、sealed 名单、condy 图、modern concat 站点，**各带 CP 索引/attribute 范围/bootstrap 索引**。**modern concat 归 reader**（只读 CP + `BootstrapMethods` + 句柄 `MethodRef`，**不求值 recipe、不解析句柄、不调用工厂**），站点 BCI 归字节码读取、**join key 是 CP 索引**；与 P3 的 `concat@1`（呈现）显式区分。**A05**：行走是**显式栈非递归**（父级读码确认），visited 展开一次、环记为事实不跟随、四维预算（复用既有维度）命中即停走**并发布已走前缀**；**不执行 bootstrap 的铁证**——fixture 的句柄指向**本仓库不存在的类**，图仍完整且断言「走到句柄即止、从不跟随它命名的成员」。**output-level 冲突**：`OutputLevelStatus::Conflict` 列出每个 feature 的 origin 与决定它的 release 规则，**事实原样保留、不做等价降级**；header 平面仍只产 `NotEvaluated`（负向用例钉住）。**registry 接线**在事实读取路径：`Legal` 才读内容，版本/位置不适用**不读内容**且报诊断（`AttributeBytes` 低于合法读为证）；`inspect_header` 一字未动。**fixture**：`tests/fixtures/p4-modern/` 11 个 class（52/60/61），SHA 入 README；**父级独立重编译 → 11/11 逐字节相同**。**产不出如实记录**：javac 23.0.1 在 8/16/17 上不产任何 `CONSTANT_Dynamic`，故 condy fixture 测试内手工组装。**证据**：1023 passed / 0 failed / 3 ignored；fmt/clippy 1.98.1 干净；openspec strict 14；CI run 35468561216 四 job success；三组证伪见 verification。**未完成**：flag/opcode 接线与 golden diagnostics 属 **1.3**；`OutputLevelStatus` 的其它 level 属 **2.x**）
- [x] 1.3 建立现代合法/非法 fixture 和 golden diagnostics，验证结构读取不冒充 dialect/verification 成功（2026-09-20 完成，提交 `6ed448a`。**golden** 照 `p2_golden` 逐字段同形：固定回放表与 JSON 自带表**互比**、每条重建具名生成器并断言 `blake3`+长度、钉整个 header 平面与事实侧投影 + **strict 结论**、文件静态；`illegal` 标志被断言必须与所在文件一致。**非法 fixture 逐字节可重放**（`version_patched` 补版本字段 + `jsr_class`/`invokedynamic_class`/`nest_members_in_a_method`/`constant_value_in_a_method`/`acc_module_class` 五个具名生成器，摘要入 golden）。**接线在事实路径**：`inspect_header` 一字未动；成员属性判位置、opcode 经 `method_body_opcodes` 取真实游标、flag 只陈述 release 主张、tag 不新造 code。**两处上报发现**：① 把 opcode 禁止加到 `inspect_method_bytecode` 会改动 P2 已冻结的 golden（`illegal-version-jsr-in-52`），故改接事实路径——**字节码路径仍不对 release opcode 发言**；② **逐位解析 flag 不成立**：登记的是「哪些 release 引入了哪些 flag」而非结构词表，第一版在真实 `RecordSample.class` 上产 **8 条假诊断**（如把 `ACC_FINAL` 报成只属 `MethodParameters`），故**不作任何 flag 位置主张**、flag 改按名驱动，由 `a_flag_is_answered_by_name_and_never_inferred_from_a_bit` 钉住。**父级独立复核发现 2**：对 11 个真实类跑事实路径 → **flag 诊断 0、诊断总数 0**。**不冒充成功**：对 9 条非法条目逐项断言 `structural_read=Complete` + `verification=NotPerformed` + `output_level=NotEvaluated` + dialect 与记录一致；并断言「本 build **校验**的 release 照样可带违规」「只**探测**的 release 照样被完整读出」。**证据**：1029 passed / 0 failed / 3 ignored；`p4_golden` 6 passed；fmt/clippy 1.98.1 干净；openspec strict 14；**既有断言零改动**；三组证伪见 verification（副本 323/323 校验无差异）。**未完成**：flag 位置主张与完整词表属 1.1/2.x 决定；参数 flag 与成员属性内容归各自 pass；多档 `output_level` 属 2.1/3.3）

## 2. Runtime depth

- [ ] 2.1 实现 RuntimeMatrix、模块/loader policy 和 MR/layout 选择函数，验证 A06/A07 的多 profile 物理保留
- [ ] 2.2 扩展 X2 declaration/dispatch candidate 查询，覆盖缺失依赖、default conflict 和 open-world 状态
- [ ] 2.3 实现有界 X3 reflection/ServiceLoader patterns，验证常量目标与动态输入分别返回 inferred/Unknown

## 3. Plugin and acceptance

- [ ] 3.1 定义 versioned query plugin descriptor、schema、rule/evidence/coverage 和资源预算，验证未注册配置为 Unsupported
- [ ] 3.2 实现至少一个只读 framework/resource plugin fixture，验证不执行 bootstrap、launcher、JNI 或网络
- [ ] 3.3 更新现代支持矩阵、Java 8 output conflict 和 source map，运行 A04–A07/A12 及 adversarial tests
- [ ] 3.4 执行 cargo fmt、clippy、test 与 OpenSpec strict validation，记录结构支持和源码恢复之间的独立状态
