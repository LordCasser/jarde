# Root acceptance

Root rebuilt the current release CLI after formatting and the method-declaration argument cleanup: `/tmp/jarde-cli-member-root-accepted`, SHA-256 `19ede7a6fe88b95530637e9765c7ae02b9f233520dc6a2e2e815f0dca58d0552`. The frozen before scripts were copied to `/tmp/jarde-member-root-after-QSVO0Q/member-annotation-uses`; only their CLI SHA gate and the main fixture's formerly missing Jarde expectation were changed in the copy. Neither Java input nor generated source was edited. Both copied replay scripts and the independently invoked [`verify_invisible_smoke.py`](../../evidence/java-syntax-2026-09-22/member-annotation-uses/post-change-replay/verify_invisible_smoke.py) passed with the root CLI.

The 362 B `MemberTagged.class` retained SHA-256 `63fbb2c76199125f23540adc0611e65232be055e6817899291722c90d301c996`. Original, JADX, and Jarde complete source sets each passed `javac --release 8` and `java -Xverify:all`; every runner printed `true / true / 1 / 5`. The boundary original and controlled patches retained their recorded class hashes, attribute spans and `Code` hashes; the legal original prints `7 / 7 / 0 / 3`. Jarde JSON retains each field/method/descriptor-parameter owner, the duplicate field annotation refusal, and the two-groups-versus-three-parameters refusal. The existing Jarde boundary method body `wideAndVarargs` has no return in both pre-change and accepted output, so that complete generated boundary class does not compile; this is not claimed as an annotation regression or a successful runtime comparison. A separate unedited generated `BoundarySmoke` source round-tripped its runtime-invisible field, method and three parameter values, passed Java 8 compilation and verification, and reproduced the original class byte-for-byte (SHA-256 `842b3757521624079c92f19fda43aa96d1673ca662db5b2a9c9e87c59cd3adf9`).

Root reviewed member shell ownership, parser use of the existing annotation value tree, descriptor parameter position versus local slot, atomically refused spellings, and preserved raw shells on damage/budget/cancellation. No method IR or type-use attribute interpretation was added. The new permanent array-initializer fixture raised the repository class census by exactly one class and 13 `Code` bodies; root updated its count to `(142, 1062, 98, 346, 8)`. Root grouped the method's existing default/throws values in `MemberAttributes` at the spelling boundary, removing the new eight-argument Clippy violation without another abstraction.

Checks after that cleanup: reader lib 160/160 and member reader 7/7; class-source 33/33, annotation-default 8/8, member budget/cancellation 2/2; CLI class-source 16/16, historical corpus 1/1, corpus fingerprint 5 passed/1 ignored; `cargo fmt --all -- --check`, `git diff --check`, and global OpenSpec strict 66/66. Strict reader-lib Clippy passed. Root-lib Clippy passed with the existing `clippy::type_complexity` exception for unrelated `jarde-java/src/region.rs`; that debt remains separate. The prior `p5_bulk_corpus` pin drift is likewise outside this change and was not used as an acceptance gate.
