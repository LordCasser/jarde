## 1. 固定缺口

- [x] 1.1 建立自写 Java 8 fixture 与独立 Rust 回归，覆盖对象/原始数组/引用及多维数组转换、返回/局部/参数/后缀、嵌套顺序和调用效果；记录 javac/jadx/jarde 三方修前证据，直接转换用例应修前失败。

## 2. 复用现有值表达式

- [x] 2.1 等待 special change 移交后，将普通检查接入已有 Cast 与类型名拼写，复用最终消费位置/来源/预算；通过正面呈现与来源测试，既有 bridge 断言不回退。
- [x] 2.2 将转换接入延期消费和失败生产者保留，加入运行时 patch 的 discarded cast 及后续消费拒绝反例；断言检查/调用来源不丢失，不引入新区域或效果 pass。

## 3. 对照与独立验收

- [x] 3.1 实际恢复正文重编译，比较成功值、null、数组元素、嵌套拒绝、调用计数、CCE/NPE；保留同 class 的 jadx 输出及差异说明，以原始程序为准，结果写入 verification.md。
- [ ] 3.2 主代理审查并复跑转换、bridge、旧值、预算与失败保留回归；更新 census/fingerprint，执行 fmt、受影响 clippy 与 OpenSpec strict，分别记录新增问题和既存门禁状态，只按实测验收。
