# Current conditional-value replay for AssertCore

The frozen Java 8 source, runner, and class remain in `tests/fixtures/p3-assert-core/`. `AssertCore.java` SHA-256 is `cf3be5621023c8164e1022b5a23518c677e5ac71533ec68f52bcfd9c090f980f`; the class generated by `javac --release 8 -g` is 1187 B with SHA-256 `71753fbec0b66a6511bbb2b66855c50589060b729bc963cb6cd78377b75f4aaf`. The source-only `AssertRunner.java` is SHA-256 `c063b5ee22b78443ed3496871dcb7b2ed33a3c1616c37243dd3e594d75d184b2`; it compiles to 1007 B, SHA-256 `b821b8f12c8b4bdddeb36271dc283f37a6f0cc016499cc6ed1b67050afc8bb66`. The existing `replay.py` rebuilt and checked both class hashes while using current CLI SHA-256 `a3a29b59aa85c2d174b06033f874eb7d60a104dbccf1d20f5d88908273e92dd5`.

The full replay summary is `replay-summary.json`. Original, JADX, and current Jarde classes all compiled and passed `-Xverify:all`; all produced `1|0;bad|2|1` under `-ea` and `0|0;0|0` under `-da`. The wrong-owner patch changes only `<clinit>` BCI 0's class literal; its SHA-256 is `af3c1190da44c938138da327a6232220acd3b8e8161808e2d9d517f11a4e71dc`. Original, JADX, and Jarde all preserve its selective `-ea:AssertCore -da:java.lang.StringBuilder` output `0|0;0|0`.

The second valid patch, `AssertCore-non01-arms.class`, has SHA-256 `b52d39dd6ae7943d70b510c4925f23faadcc2fcc64c5cb7a2c309ae450de4e2c`; javap confirms only `<clinit>` BCI 8/12 constants change from `1/0` to `2/3`. The original verifies and outputs `0|0;0|0` under `-ea`, `1|0;bad|2|1` under `-da`. Current Jarde presents the field assignment as `(!AssertCore.class.desiredAssertionStatus() ? 2 : 3) % 2 != 0`; its rebuilt class verifies and matches both outputs. Current JADX output matches the frozen `JadxAssertCore-non01-arms.java`, but its source fails javac because `2`/`3` cannot be assigned to Java `boolean`. Raw original and current Jarde outputs are in the `non01-*-ea/da.txt` files; the JADX compile diagnostic is retained alongside them.

The existing `openspec/evidence/java-syntax-2026-09-22/assert-syntax/` remains the 1346 B source-level assertion probe. Its `root-after-class-25bf/` record preserves the frozen CLI's BCI 13 final-field RED; this is historical baseline evidence and is not replaced by the current AssertCore replay.
