## 1. Region 所有权、局部证明与正文构建

- [x] 1.1 在 Region 构造进入普通嵌套 `If` 前，以 CFG 证据识别共用 false 生产者的有限形状，由限定的 `Region::ShortCircuitValue` 一次认领两次分支、两个生产者及消费块，避免 BCI 34 重访被错记为循环。同步在 builder 的全部穷尽匹配接入此节点的**默认完整引用**契约：SSA/发射证明尚未实现时，节点整体可见地引用分支、生产者和字段写入，不得被旧 `If` 发射路径忽略或标为完整恢复。消费块引用须逐指令包含 BCI 34、37、38、41；现有 `covered_bcis` 只列原始块起点，不能用于声明整块来源完整。验证：冻结 `Base()` 的 Region 无误诊 `Loop`、各物理块归属一次，正文及 source map 都保留写入和后缀 BCI；邻近普通 `If` 与真实循环 Region 不变。此任务只处理所有权与安全拒绝，不宣布 SSA 值可发射。架构师独立复跑 `jarde-java` lib 151、匿名侧车 4、`class_source` 47 项通过；[冻结 Base CLI 与行为对照](../../evidence/java-syntax-2026-09-25/anonymous-super-dispatch/evidence.md)记录了 34/37/38/41 同一缺口和当前仍不等价的重编结果。
- [x] 1.2 在该 Region 上以 SSA/CFG 证明两次短路分支、唯一 1/0 栈 Phi、消费块中唯一 `putstatic` 读者、字段身份、无额外入口/异常边/不可嵌入效果，并按 `O(E + I + Phi)` 预收费、沿用深度界限。验证：冻结 BCI 12/15、23/26、29/33、34 正例通过；增加额外入口或第二消费者时拒绝，拒绝不发布部分折叠计划。架构师独立复跑 `jarde-java` lib 155、匿名侧车 4、`class_source` 47 项，以及格式检查、两个相关 OpenSpec strict validation，均通过；当前证明尚未接入发射，`Base` 的字段写入仍保留完整引用。
- [x] 1.3 用现有 `Conditional` AST 和字段写入路径原子发射一次静态赋值；右侧只在左侧为真时求值，分支、生产者、字段写入 BCI 都保留于来源，消费块其余指令依原顺序继续。验证：`Base()` 正文含静态目标字段写入，必要指令无未声明缺口，效果不重复。架构师独立复跑 `jarde-java` lib 156、匿名侧车 4、条件值回归 2、`class_source` 47、CLI `class_source_cli` 16 项；冻结 `Base` 单类重编且替换运行输出与原件逐行一致，JSON 报告为 `structured`、`fallbacks=[]`；[实测证据](../../evidence/java-syntax-2026-09-25/anonymous-super-dispatch/evidence.md)记录赋值和 BCI 来源。左侧为假、非规范 `Z` 等控制仍留给 2.1/2.2。
- [ ] 1.4 对多入口、额外效果、异常边、非唯一 Phi 消费或字段身份不明的近邻形状保持保守拒绝；完整引用分支、值生产者及原 `putstatic`，不能生成一个省略该效果却声称完整的正文。验证：各拒绝样本的 fallback/source-map 能追到字段写入 BCI，且无虚假的完整质量标记。

  已闭合的子集：非 1/0 生产者、独立测试指令、第二次字段写入、非 `Z` 字段均由各自 fresh recovery 报告验证完整引用与 source map；原先把外层分支重定向到 true 生产者的补丁实为合法 shared-true OR 骨架，现改作成功极性回归，不再冒称“额外入口拒绝”。`javac --release 8` 的重复 Phi 消费者与非规范 2 生产者又提供两个 JVM 可验证的拒绝控制。[真实异常边夹具](../../evidence/java-syntax-2026-09-25/short-circuit-exception-edge/analysis.md)现在一次引用两次测试、1/0、`putstatic` 与后缀，报告为非结构化 `jre_region_exception_edge`，聚焦回归通过；RHS 的调用计数仍显示引用文本不等于恢复行为。真正额外入口的完整所有权仍待独立控制，三测试链已按 [另一 change](../recover-short-circuit-field-chains/tasks.md) 拆分，故本项仍未勾选。

## 2. 三方行为验收

- [x] 2.1 将 Jarde 的 `Base.java` 单独用 `javac --release 8` 重编，运行时只替换 `Base.class`，调用者与匿名类仍用冻结原件；与原 class、JADX 1.5.6 的 `observed=captured-value`、`visibleDuringSuper=true` 逐项对照，并加一个左侧为假的短路副作用/异常控制。验证：原/Jarde 的值、调用次数、求值及异常顺序相同；JADX 差异按事实记录，不充当通过条件。架构师独立重编/运行冻结 `Base`，三方均输出 `observed=captured-value`、`visibleDuringSuper=true`；再独立执行 [左侧为假控制重放](../../evidence/java-syntax-2026-09-25/short-circuit-left-false/report.md)，原/JADX/Jarde 均输出 `false-result=false,calls=0`、`true-result=true,calls=1`。该控制用可见计数而非抛异常，未单独声称异常顺序覆盖所有情形。
- [x] 2.2 复核现有普通双臂 `?:`、断言开关和非规范 `Z` 整数 2/3 对照，确保本短路拓展未把任意整数 Phi 改写成 `true`/`false`，也未改变已有 field/return 的单消费边界。验证：对应聚焦回归与原 class 行为通过。架构师独立复跑普通条件值 2 项、boolean 字段写入 3 项及需 JDK 的 40 行执行控制；完整 `TernaryValues` 类源码重编后 16 行返回/副作用/异常与原件逐行一致。`AssertCore` 原件及 2/3 补丁的当前整类源码均可用 `javac --release 8` 重编，在 `-ea`/`-da` 下分别与原 class 行为一致；补丁 `<clinit>` 保留整数臂 `2 : 3` 并在 `putstatic Z` 消费处做 `% 2 != 0`，没有伪造布尔字面量。精确结果见 [控制复核](verification-controls.md)。
- [ ] 2.3 对本 change 运行 `cargo fmt --all -- --check`、相关定向测试、适用的 Clippy 与 `openspec validate recover-conditional-field-writes --strict`；汇总实际通过/失败及未解决边界到独立 verification 文档。验证：证据中写出精确命令与结果，所有已完成任务才勾选；不得把 2.10/5.3 匿名类修复混进本 change。
