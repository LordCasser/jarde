## 1. 冻结文本与负例

- [x] 1.1 对现有直接数组与安全包装读取 fixture 用当前 CLI 重放 `-g`/`-g:none` 原/JADX/Jarde，固定失用前置声明、增强 `for`、Java 8 重编与执行哈希；验证为可重复的证据记录，不以旧 CLI 文本代替当前工作树。
- [x] 1.2 固定同槽复用后继局部与下标逃逸/错配数组拒绝夹具，记录 javac --release 8 class、javap 槽范围及原类 `-Xverify:all` 输出；明确区分当前同槽异类型源码错误与本清理的误删边界，验证后继声明不被本改动进一步删除，拒绝负例保留计数循环。

## 2. 只清理同次证明消费的声明

- [x] 2.1 从成功的数组增强 `for` 证明取得长度缓存与归纳下标的真实 `LocalVariable`/BCI，与前置无初值声明身份交叉核对并确认 BCI 已并入循环来源；验证直接/安全包装正例匹配，名字相同但变量身份不同或声明带初值时不匹配。
- [x] 2.2 在候选证明和预算计费完成后，把对应前置声明与原赋值/计数循环原子替换为增强 `for`；验证 essential/all 正文一致、两个失用声明消失、source map 保留长度/初值/更新来源，预算/取消不发布部分清理。
- [x] 2.3 运行数组、Iterable、循环转移及局部复用的相邻测试；验证 1.2 的拒绝与后继变量声明保持、同槽基线缺陷未被误当作已修复，现有增强 `for` 准入和运行结果不变。

## 3. 独立执行验收

- [x] 3.1 root 用重新构建的 CLI 对 1.1 的正例原/JADX/Jarde 完整类分别 Java 8 重编、`java -Xverify:all` 比较结果/副作用/异常；对 1.2 的同槽异类型基线如实记录编译失败且确认没有新误删，对其余拒绝负例核对不误投影；审读来源与身份证明，跑 `cargo fmt --all -- --check`、适当定向 Rust/Clippy、`git diff --check` 和 `openspec validate prune-proved-foreach-cache-declarations --strict`，记录剩余债务并清理独立 Cargo target。
