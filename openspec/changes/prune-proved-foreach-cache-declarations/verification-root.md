# Root 独立验收（2026-09-24）

审读 `array_for_each_candidate` 与 `proved_array_cache_declarations`：只在数组增强 `for` 候选成功后，以 `(slot, BCI)` 找出两个缓存的真实 `LocalVariable`，核对声明规划的同一身份、无初值的 AST 声明、来源 BCI 已被增强 `for` 认领，以及后续 SSA 指令不再访问同一变量。提交时一起移除已消费的赋值、计数循环和对应声明；若无法证明则不删除。这里没有按局部变量名、单一槽号做全局清理。

Root 自建 CLI，分别从原源码以 `javac --release 8 -g` 与 `-g:none` 编译 `IntArrayForeach`、`ArrayWrappedBinding`，再用 Jarde 生成完整类并与原 runner 一起 Java 8 重编、`java -Xverify:all`。四组均编译通过，增强 `for` 正例仍存在，两个失用声明消失，规范化 JVM 自动生成的空指针详细文本后运行输出与原类逐行一致。Jarde 源码 SHA-256：直接数组 `-g` / `-g:none` 为 `0bad4b54176b224c6765aa0cd320362e24fe73ffac5a5e48584e93d752bc57ea` / `6d9024ca27bce5b87ce82fc23a85c3644be1c6398923c9e36f20c78c933d2465`；安全包装为 `c1f83ddcbd26c611aba9204f98cf59d12dff008edc9d614a0ad0ef6cbf066cb4` / `b4ba774f2dcb8b1bc12a4cf86f620340283bc923db07b83f48de5b56f43b6062`，与[三方重放记录](../../evidence/java-syntax-2026-09-24/foreach-cache-declarations/analysis.md)一致。Root 又独立运行 JADX 1.5.6 四次并重编 runner：直接数组两组与原类一致；安全包装两组 13 行中有三个副作用顺序结果不一致，Jarde 在这些方法维持计数循环并匹配原类。对应 JADX 源 SHA-256 分别为 `126e3ae61088f0fcc699f737b3c3558cabcb86feabb1ba04d22163fb301f8f95`、`3bda59fe8ca15c73540d1e414d9ca98a347356f0ee3e20c6a440731b87805016`、`f375c56aeb1e560d30b988c40f3cb7918e033dc202ede72f31dfb6f3380c052e`、`ef24805500d3b961b03890088cf4e17d8ac2974c24a71c7edf8ab0a38e0b8a87`。

Root 跑 `p3_array_foreach` 5/5、`p3_wrapped_array_foreach` 4/4、`p3_iterable_foreach` 5/5、`p3_loop_transfers` 5/5、`p3_declarations` 5/5、`p3_local_rewrite` 7/7。`ReuseAfterForEach` 验证了槽 3 后继 `int second;` 仍保留；其完整 Jarde 类仍因槽 2 的数组/整数生命周期混淆无法 javac 重编，属于独立的 [separate-reused-local-lifetimes](../separate-reused-local-lifetimes/proposal.md) change，不能算本清理成功或回归。新增函数参数已压缩为两个 `(slot, BCI)` 缓存对，避免新增 `too_many_arguments` lint。

`cargo fmt --all -- --check`、`git diff --check` 和 `openspec validate prune-proved-foreach-cache-declarations --strict` 通过。严格 Clippy 仍有共享基线的 `useless_conversion`、`too_many_arguments`、`type_complexity`、`needless_option_as_deref` 等错误；屏蔽这些已知 lint 后 `--all-targets` 又因其他 change 中的测试调用缺少 `recover_for_class_source` 布尔参数及一个克隆切片 lint 失败，四个相关包的 `--lib` 检查通过。现有全目标错误暂不混入本 change。独立 Cargo target 验收后清理。
