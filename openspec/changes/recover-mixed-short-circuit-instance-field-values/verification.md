# Verification

- `CARGO_TARGET_DIR=/tmp/jarde-instance-impl-target cargo test -p jarde-java --lib`: 172 passed for the initial implementation. After adding the inherited-owner boundary, `CARGO_TARGET_DIR=/tmp/jarde-instance-boundary-target cargo test -p jarde-java --lib`: 173 passed, including the new exact FieldPlan type refusal assertion.
- Root integration suites for instance field, static field, return, invocation argument, local, transfer gateway, Region owner, and exception-edge/chain consumers: all non-ignored tests passed. The separately ignored Java 8 static-field 16-path execution test also passed.
- `p3_mixed_short_circuit_instance_field`: 4 passed. The frozen original source recompiles byte-identically with `javac --release 8 -g:none`; original and Jarde complete classes both match all 16 frozen `java -Xverify:all` traces. Tests cover source origins, budget/cancellation, and verifier-valid numeric-field, duplicated-value, compound-update, shared-receiver, protected-write, and inherited-owner controls. The inherited-owner class recompiles byte-identically, and its Runner asserts the original field result on all eight inputs under `-Xverify:all`.
- `cargo clippy -p jarde-java --lib --no-default-features` and `cargo clippy --test p3_mixed_short_circuit_instance_field --no-default-features`: exit 0 with existing warnings outside this change. `git diff --check` and strict OpenSpec validation passed. `rustfmt --edition 2024 --check` passes for this change's two Rust files. The latest workspace-wide `cargo fmt --all --check` is blocked solely by concurrent edits in `tests/p3_typed_catch_boundary_return.rs`; the prior workspace-wide check passed before those edits.
- Private targets `/tmp/jarde-instance-impl-target` and `/tmp/jarde-instance-boundary-target` were removed by `cargo clean --target-dir` (5.9 GiB and 2.5 GiB).

Root independently rebuilt the frozen original and retained JADX full-class presentation with `javac --release 8 -g:none`, then ran both with `java -Xverify:all`. Root also built a fresh CLI, exported the complete outer class and its real separately presented nested `Box`, compiled those with the retained Jarde Runner using the same Java 8 target, and ran all 16 paths with `-Xverify:all`. All three outputs match the frozen original exactly, including field values, receiver and b/c counts, and null timing. The root acceptance source, full-evidence JSON, 16-line output, and binary hashes are retained in `openspec/evidence/java-syntax-2026-09-25/mixed-short-circuit-instance-field/jarde-after-instance-field-root-*`; the `one` method reports structured Java with zero fallbacks and all 13 original BCIs in its source map. The existing hashed baseline was not overwritten.

The inherited-owner control isolates the receiver-type gate: the graph and Phi prove, while the published FieldPlan refusal states `DerivedBox` as the receiver type and `BaseBox` as the `Fieldref` owner. A separate external entry into the middle of this expression is not a natural javac lowering of structured Java source; it would require a controlled bytecode rewrite with a matching stack map and independently verified execution. No isolated instance-field external-owner overlap control is retained yet, so task 2.4 remains open.
